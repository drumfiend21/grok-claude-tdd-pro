# TICKETS.md

Ordered backlog. Tickets are sized to ~1 CL each. Later tickets assume earlier ones landed.

| ID         | Title                                              | Scope                                                                                                                                                                                                       | Done when                                                                                                              |
| ---------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| TICKET-001 | Scaffold repo + README + CLAUDE.md pointer         | Create top-level dirs; README states purpose; CLAUDE.md points at claude-tdd-pro/CLAUDE.md and adds the 2-rule harness contract                                                                              | `tree -L 2` shows the layout; README under 60 lines. **DONE 2026-05-24.**                                              |
| TICKET-002 | Handoff contract spec                              | Write `docs/handoff-contract.md` defining Grok→Claude payload schema (ticket-id, accept-criteria, file-scope, context) and Claude→Grok response schema (changed-files, test-results, decision-trail-ref)    | Schema is concrete enough that a stub Grok script could produce it. **DONE 2026-05-24.**                               |
| TICKET-003 | Grok orchestrator prompt templates                 | Populate `.grok/` with prompt templates for research, decomposition, sub-agent dispatch, deploy. Headless `-p` mode assumed                                                                                  | Three named templates exist; each has input vars + output shape documented                                             |
| TICKET-004 | Claude TDD Pro consumption wiring                  | `.claude/` config that loads the three `tdd-pro-*` skills (cl-workflow, batch-cl, bash32-portability) from claude-tdd-pro into prototype-repo sessions                                                       | Running `claude -p` in the prototype repo surfaces the three skills                                                    |
| TICKET-005 | Toy module + Red-Green-Refactor demo target        | Add a tiny module under `examples/` (e.g. string-utils) with obvious R-G-R potential                                                                                                                         | Module compiles/runs; has one obviously-missing behavior to drive a Red test                                           |
| TICKET-006 | End-to-end smoke script                            | Bash or Node script: Grok decompose → emit handoff → invoke `claude -p` with tdd-pro skill → assert green tests → report. No new feature logic — just wiring                                                  | Script exits 0 on the TICKET-005 toy module after one full R-G-R cycle                                                 |
| TICKET-007 | Quality-gate contract                              | `docs/` page formalizing what "green" means for the inner loop (test pass, lint, coverage delta). Borrows §2.X cross-cutting contract names where applicable from claude-tdd-pro                              | Single-page doc; reviewer can checklist against it                                                                     |
| TICKET-008 | Self-healing extension design                      | Design doc only: long-running Grok monitor that watches debt thresholds and re-spawns Claude TDD Pro for refactor cycles. Includes failure modes                                                              | Design doc; no code                                                                                                    |
| TICKET-009 | Demo storyboard for Boston enterprise signals      | `docs/demo-storyboard.md` with a 5-step demo flow for State Street (compliance) + HubSpot (Breeze AI standardization). Includes "what to show on screen at minute N" beats                                    | Storyboard reads like a director's shot list                                                                           |
| TICKET-010 | Provenance + decision-trail bridging               | Design: how Grok's research provenance and Claude's decision trail merge into a single audit artifact per ticket. Cross-references claude-tdd-pro's §2.2 provenance manifest                                  | Design doc; defines the merged artifact's shape                                                                        |

## Conventions

- Commit message format: `TICKET-NNN: <verb> <object>`
- One ticket per CL. If scope grows mid-ticket, split into TICKET-NNN.a / .b rather than expanding the original.
- Closing a ticket means the "Done when" column passes and is verified in the commit body.
