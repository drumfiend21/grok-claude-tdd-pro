S-3|rm -rf usage (verify path is bounded)|.claude/hooks/pre-tool-use-govern.sh:64:    trap 'rm -rf "$scratch" 2>/dev/null' EXIT
S-3|rm -rf usage (verify path is bounded)|scripts/_lib/epoch-gate.sh:108:        base_sorted=$(mktemp -t epoch-base.XXXXXX) || { rm -f "$cur_sorted"; return 2; }
S-3|rm -rf usage (verify path is bounded)|scripts/_lib/epoch-gate.sh:111:        rm -f "$base_sorted"
S-3|rm -rf usage (verify path is bounded)|scripts/_lib/epoch-gate.sh:115:    rm -f "$cur_sorted"
S-3|rm -rf usage (verify path is bounded)|scripts/_lib/test-cache.sh:116:    rm -f "$TC_CACHE_DIR"/*.hash 2>/dev/null
S-3|rm -rf usage (verify path is bounded)|scripts/audit-cross-references.sh:144:rm -f "$current_sorted"
S-3|rm -rf usage (verify path is bounded)|scripts/audit-cross-references.sh:59:trap 'rm -f -- "$findings_file"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|scripts/audit-doc-drift.sh:59:trap 'rm -f -- "$FINDINGS_FILE"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|scripts/audit-manifest.sh:74:trap 'rm -f -- "$findings_file"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|scripts/audit-standards-enforced.sh:160:    rm -f "$div_sorted"
S-3|rm -rf usage (verify path is bounded)|scripts/audit-standards-enforced.sh:91:trap 'rm -f -- "$div_file"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|scripts/emit-manifest.sh:154:trap 'rm -f -- "$TMP_OUT"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|scripts/export-cursor-rules.sh:69:    trap 'rm -rf -- "$OUTDIR"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|scripts/smoke-e2e.sh:190:    rm -f -- "$TEST_OUT"
S-3|rm -rf usage (verify path is bounded)|scripts/smoke-e2e.sh:199:rm -f -- "$TEST_OUT"
S-3|rm -rf usage (verify path is bounded)|scripts/standards-sync.sh:50:trap 'rm -f -- "$TMP_OUT"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|scripts/standards-sync.sh:67:        rm -f "$TMP_OUT.norm" "$TMP_OUT.cur"
S-3|rm -rf usage (verify path is bounded)|scripts/standards-sync.sh:70:    rm -f "$TMP_OUT.norm" "$TMP_OUT.cur"
S-3|rm -rf usage (verify path is bounded)|scripts/sync-plugin.sh:106:trap 'rm -f "$TMP_CSF"' EXIT
S-3|rm -rf usage (verify path is bounded)|scripts/sync-plugin.sh:142:    rm -rf "$CLONE_DIR_E"
S-3|rm -rf usage (verify path is bounded)|scripts/sync-plugin.sh:208:rm -rf "$CLONE_DIR"
S-3|rm -rf usage (verify path is bounded)|tests/integration/test-generative-integration.sh:52:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-all.sh:75:        [ "$CACHE" -eq 1 ] && rm -f "$(tc_cache_dir)/$suite_name.hash" 2>/dev/null
S-3|rm -rf usage (verify path is bounded)|tests/test-app-root.sh:20:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-app-root.sh:35:rm -f "$CFG"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-agent-compact.sh:28:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-agent-compact.sh:59:rm -f "$COMPACT"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-agent-compact.sh:75:rm -f "$ACK"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-app-tree.sh:21:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-app-tree.sh:36:rm -f "$CA"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-applicable-rules.sh:20:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-applicable-rules.sh:39:clear_h() { rm -f "$TMP/h"/*.json; }
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-architecture-crosscheck.sh:21:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-architecture-crosscheck.sh:39:rm -f "$TMP/h"/*.json
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-architecture-crosscheck.sh:82:rm -f "$TMP/h/FEATURE-1.crosscheck.json"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-cross-references.sh:43:rm -f "$TMPDOC"   # restore BEFORE asserting
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-design-phase-md.sh:107:clear_h() { rm -f "$TMP/h"/*.json; }
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-design-phase-md.sh:20:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-development-paths.sh:21:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-eo-governance.sh:109:rm -f "$TMP/handoffs/TICKET-901.req.json" "$TMP/handoffs/TICKET-901.res.json"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-eo-governance.sh:115:rm -f "$TMP/handoffs"/TICKET-*.req.json "$TMP/handoffs"/TICKET-*.res.json
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-eo-governance.sh:137:rm -f "$TMP/handoffs"/TICKET-*.req.json "$TMP/handoffs"/TICKET-*.res.json
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-eo-governance.sh:24:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-eo-governance.sh:87:rm -f "$TMP/handoffs/TICKET-900.req.json"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-eo-governance.sh:94:rm -f "$TMP/handoffs/TICKET-900.res.json"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-metrics.sh:70:    rm -f "$f.bak"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-metrics.sh:74:rm -rf "$TMPDIR"   # restore BEFORE asserting
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-metrics.sh:90:rm -rf "$TMPDIR"   # restore BEFORE asserting
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-plugin-surface.sh:50:rm -f .harness/plugin-cache/claude-tdd-pro/__sneaky_new_dir__
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-plugin-surface.sh:51:rm -rf "$TMPDIR"
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-rules-verified.sh:20:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-rules-verified.sh:33:clear_h() { rm -f "$TMP/h"/*.json; }
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-standards-enforced.sh:22:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-audit-standards-enforced.sh:44:clear_h() { rm -f "$TMP/h"/*.json; }
S-3|rm -rf usage (verify path is bounded)|tests/test-consult.sh:43:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-emit-manifest.sh:119:rm -f .harness/audit/TICKET-042.manifest.json .harness/audit/TICKET-042.manifest.regenerated.json
S-3|rm -rf usage (verify path is bounded)|tests/test-emit-manifest.sh:174:rm -f .harness/audit/TICKET-042.manifest.regenerated.json
S-3|rm -rf usage (verify path is bounded)|tests/test-enforce-standards.sh:23:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-epoch-gate.sh:24:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-gctp-standards-add.sh:21:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-gctp-standards-review.sh:20:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-hook-contracts.sh:89:rm -f "$TMPFIX"
S-3|rm -rf usage (verify path is bounded)|tests/test-install.sh:49:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-orchestrating-swarms.sh:83:        rm -f ".harness/handoffs/${w}.req.json" \
S-3|rm -rf usage (verify path is bounded)|tests/test-post-tool-use-review-gate.sh:76:trap 'rm -rf -- "$PTMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-pre-tool-use-govern.sh:21:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-sarif-aggregate.sh:115:rm -rf ".harness/audit/sarif/raw/$DEFAULT_TICKET" ".harness/audit/sarif/$DEFAULT_TICKET.sarif.json" ".harness/audit/sarif/$DEFAULT_TICKET.summary.json"
S-3|rm -rf usage (verify path is bounded)|tests/test-sarif-aggregate.sh:21:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-source-citations.sh:31:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-standards-refresh.sh:104:rm -f "$C"; run --configure 1d >/dev/null 2>&1
S-3|rm -rf usage (verify path is bounded)|tests/test-standards-refresh.sh:21:trap 'rm -rf -- "$TMP"' EXIT INT TERM
S-3|rm -rf usage (verify path is bounded)|tests/test-standards-refresh.sh:61:rm -f "$CTPFF"; run --configure 6h >/dev/null 2>&1
S-3|rm -rf usage (verify path is bounded)|tests/test-standards-refresh.sh:65:rm -f "$CTPFF"; run --configure bogus >/dev/null 2>&1; rc=$?
S-3|rm -rf usage (verify path is bounded)|tests/test-standards-refresh.sh:76:rm -f "$C" "$TMP/refresh.log"
S-3|rm -rf usage (verify path is bounded)|tests/test-standards-refresh.sh:96:rm -f "$C" "$TMP/refresh.log"; run --configure on-demand >/dev/null 2>&1
S-3|rm -rf usage (verify path is bounded)|tests/test-test-cache.sh:27:trap 'rm -rf -- "$TMP"' EXIT INT TERM
