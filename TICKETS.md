# TICKETS.md

Ordered backlog. Tickets are sized to ~1 CL each. Later tickets assume earlier ones landed.

| ID         | Title                                              | Scope                                                                                                                                                                                                       | Done when                                                                                                              |
| ---------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| TICKET-001 | Scaffold repo + README + CLAUDE.md pointer         | Create top-level dirs; README states purpose; CLAUDE.md points at claude-tdd-pro/CLAUDE.md and adds the 2-rule harness contract                                                                              | `tree -L 2` shows the layout; README under 60 lines. **DONE 2026-05-24.**                                              |
| TICKET-002 | Handoff contract spec                              | Write `docs/handoff-contract.md` defining Grok→Claude payload schema (ticket-id, accept-criteria, file-scope, context) and Claude→Grok response schema (changed-files, test-results, decision-trail-ref)    | Schema is concrete enough that a stub Grok script could produce it. **DONE 2026-05-24.**                               |
| TICKET-003 | Grok orchestrator prompt templates                 | Populate `.grok/` with prompt templates for research, decomposition, sub-agent dispatch, deploy. Headless `-p` mode assumed                                                                                  | Three named templates exist; each has input vars + output shape documented. **DONE 2026-05-25** (3 templates shipped — deploy.md deferred per ADR-0006). |
| TICKET-004 | Claude TDD Pro consumption wiring                  | `.claude/` config that loads the three `tdd-pro-*` skills (cl-workflow, batch-cl, bash32-portability) from claude-tdd-pro into prototype-repo sessions                                                       | Running `claude -p` in the prototype repo surfaces the three skills. **DONE 2026-05-25** (symlinks committed; `sync-plugin.sh --ensure` added; SessionStart hook materializes cache; verified end-to-end in implementation session — see ADR-0007). |
| TICKET-005 | Toy module + Red-Green-Refactor demo target        | Add a tiny module under `examples/` (e.g. string-utils) with obvious R-G-R potential                                                                                                                         | Module compiles/runs; has one obviously-missing behavior to drive a Red test. **DONE 2026-05-25** (`examples/string-utils/`; `node --test` returns 4 pass / 1 fail, exit=1; the failing case is `slugify('  hello world  ')` which lacks `.trim()`). After this, D-5 lockdown applies to all `examples/`. |
| TICKET-006 | End-to-end smoke script                            | Bash or Node script: Grok decompose → emit handoff → invoke `claude -p` with tdd-pro skill → assert green tests → report. No new feature logic — just wiring                                                  | Script exits 0 on the TICKET-005 toy module after one full R-G-R cycle. **DONE 2026-05-25** (`scripts/smoke-e2e.sh`; stub mode default; live-Claude mode deferred per ADR-0008; full wire format real and contract-valid; trap reverts toy to preserve TICKET-005 Red baseline; idempotent across runs). |
| TICKET-007 | Quality-gate contract                              | `docs/` page formalizing what "green" means for the inner loop (test pass, lint, coverage delta). Borrows §2.X cross-cutting contract names where applicable from claude-tdd-pro                              | Single-page doc; reviewer can checklist against it. **DONE 2026-05-25** (`docs/quality-gate.md`; four sub-gates — `tests_must_pass`, `coverage_delta_min`, `lint_clean`, `provenance_complete`; `provenance_complete` added as additive recommended at v1, promoted to required at v2 via future ADR; severity enum borrowed from plugin `§2.1`, provenance gate composed on `§2.8`; reviewer checklist per sub-gate + cross-cutting integrity checks; `handoff-contract.md:58` updated to point at the new doc; see ADR-0010). |
| TICKET-008 | Self-healing extension design                      | Design doc only: long-running Grok monitor that watches debt thresholds and re-spawns Claude TDD Pro for refactor cycles. Includes failure modes                                                              | Design doc; no code. **DONE 2026-05-25** (`docs/self-healing-design.md` — 18 sections covering architecture, 7 signals, severity-based dispatch, 7 named failure modes with structural mitigations, HITL integration for P2 signals, state/config/observability, composition on plugin `§§2.8, 2.11, 2.14, 2.15, 2.17`; implementation deferred to TICKET-008.a..e per §16 sequencing; no new wire format; see ADR-0011). |
| TICKET-009 | Demo storyboard for Boston enterprise signals      | `docs/demo-storyboard.md` with a 5-step demo flow for State Street (compliance) + HubSpot (Breeze AI standardization). Includes "what to show on screen at minute N" beats                                    | Storyboard reads like a director's shot list. **DONE 2026-05-25** (`docs/demo-storyboard.md` — 5 steps × ~1 minute each: cold start, inner-loop discipline, the wire, the long loop, the contract; each step ships beats table + per-audience talking points + contingencies; Q&A reserve cues for 7 likely questions; pre-demo + post-demo checklists; cites the AUTOMATION_INTEL.md pitch hook verbatim). |
| TICKET-010 | Provenance + decision-trail bridging               | Design: how Grok's research provenance (`research_refs` in `.req.json`), Claude's decision trail (`.harness/trails/TICKET-NNN.md`), and the inner-loop response (`.res.json` `gate_results` / `changed_files` / `test_results`) merge into a single audit artifact per ticket. Index-only manifest at `.harness/audit/TICKET-NNN.manifest.json` (R-3 — no content duplication); sha256 per source for tamper detection; `upstream_provenance_manifest_ref` cross-references `claude-tdd-pro/docs/architecture-v1.9.md §2.8 AI Provenance Manifest` (ticket text historically said §2.2 — corrected: §2.2 is the unrelated Detector contract; §2.8 is the AI Provenance Manifest). Design doc only at v1; emitter / validator / `--regenerate` CLI deferred to TICKET-010.a..c. Land ADR-0018 (index-only design; upstream §2.8 referenced by path not duplicated; v1 = design + ADR, emitter deferred). Update `AGENTS.md §5` to enumerate the new doc. Update `scripts/export-cursor-rules.sh` to include the new doc in `.cursor/rules/agent-context.mdc` TIER-2 enumeration; regenerate. | Design doc; defines the merged artifact's shape. Verified: doc exists with §1–§13 sections; every cited primitive resolves; AGENTS.md §5 lists provenance-bridging-design.md; export-cursor-rules.sh updated + `.cursor/rules/agent-context.mdc` regenerated; audit + smoke + cursor-rules-check all exit 0. **DONE 2026-05-26.** |
| TICKET-010.a | Provenance manifest emitter                          | Ship `scripts/emit-manifest.sh` implementing ADR-0018 §6 pseudocode (bash 3.2 + BSD portable; hand-rolled JSON via printf; sha256 via sha256sum/shasum fallback; atomic-rename write per §8 failure-mode-3). CLI: `--ticket <id>` required + `--driver <name>` + `--upstream-ref <path>` + `--quiet` optional. Exit codes: 0 (clean), 1 (warning — response/trail missing or upstream-ref dangling; manifest still written), 2 (hard error). Wire into three call sites defensively (additive evidence, not a gate): `scripts/smoke-e2e.sh` (post-trail-write call); `.cursor/commands/inner-loop.md` (new Step 7 manifest emission; Step 8 renumbered surface); `.claude/skills/orchestrating-swarms/SKILL.md` (Step 5 reads manifest as entry point + synthesis fallback for non-emitter-aware drivers like `claude -p`). `.gitignore` adds `.harness/audit/`. Land ADR-0019 (CLI surface; defensive call-site handling; hand-rolled JSON; gating semantics deferred to quality-gate v2). | `bash -n scripts/emit-manifest.sh` clean; `./scripts/smoke-e2e.sh` exits 0 and produces parseable `.harness/audit/TICKET-042.manifest.json` with status=green and 3 sources; sha256 mismatch detected when source hand-edited; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; ADR-0019 follows numbered template. **DONE 2026-05-26.** |
| TICKET-010.b | Provenance manifest schema validator + F-6 audit pattern | Ship `scripts/audit-manifest.sh` validating every `.harness/audit/*.manifest.json` against the v1 schema per `docs/provenance-bridging-design.md §3`. bash 3.2 orchestration + `node -e` for JSON parse (node already harness-required via smoke-e2e.sh). Checks: 8 required top-level keys present; `schema_version === "1"`; `ticket_id` non-empty string; `created_at` ISO-8601 UTC regex; `status ∈ {green, red, blocked}`; `sources` non-empty array; per-source kind enum + path string + sha256 64-char hex + size_bytes non-negative number; `response_missing` kind has sha256=null + size=0; `upstream_provenance_manifest_ref` null OR existing-path string; `manifest_generator.tool` non-empty string; `signature` null at v1. Two invocation modes: walk all (`./scripts/audit-manifest.sh`) or one-shot (`./scripts/audit-manifest.sh path/to/file.json`). Wire as F-6 pattern in `scripts/audit-doc-drift.sh` (delegates to audit-manifest.sh; defensive skip when missing, matching the F-5 pattern from ADR-0014). Land ADR-0020 (node for JSON parse, not jq/python3/grep; exhaustive v1 schema scope, not lenient; F-6 delegates to standalone script, doesn't reimplement inline). | `bash -n scripts/audit-manifest.sh` clean; clean tree → audit-manifest exit 0; corrupt manifest (schema_version mutated) → audit-manifest exit 1 with specific finding; `./scripts/audit-doc-drift.sh` exit 0 on clean tree + exit 1 with `F-6 (manifest schema): ...` finding on corruption; `./scripts/smoke-e2e.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; ADR-0020 follows numbered template. **DONE 2026-05-26.** |
| TICKET-010.c | Provenance manifest `--regenerate` CLI                 | Add `--regenerate` flag to `scripts/emit-manifest.sh` (one script, two modes per D-9). When set: writes to `.harness/audit/<id>.manifest.regenerated.json` (never overwrites original); diffs each source's sha256 between original and regenerated; emits `<kind>: unchanged (<sha>)` or `<kind>: DRIFT` + original/current sha lines per source; exit 0 on all-match, exit 1 on any drift, exit 2 on missing original. Hand-rolled awk for sha extraction (no jq; matches ADR-0019 convention). Update `docs/provenance-bridging-design.md §6` from "design only" to "shipped." Land ADR-0021 (flag-on-existing not separate script; per-kind diff output not aggregated JSON; exit-1 on drift is CI-detectable; original NEVER overwritten — structural audit-trail integrity). | `bash -n scripts/emit-manifest.sh` clean; `-h` documents `--regenerate`; clean tree `--regenerate` exits 0 with "unchanged" per source; tamper trail → `--regenerate` exits 1 with `decision_trail: DRIFT` + sha lines; original `.manifest.json` untouched throughout; `--regenerate` without original → exit 2; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; ADR-0021 follows numbered template. **DONE 2026-05-26.** |
| TICKET-015.a | PostToolUse review-gate hook                          | Ship `.claude/hooks/post-tool-use-review-gate.sh` materializing ADR-0017's deferred TICKET-015.a (PostToolUse hooks for review gates per the 2026-05-26 Architect Automation Briefing). Bash 3.2 + `node -e` for JSON parsing. Reads Claude Code's hook-protocol JSON envelope from stdin, extracts `tool_name` + `tool_input.file_path`, fires only on `Edit | Write | MultiEdit | NotebookEdit`, checks the path against four forbidden patterns from `AGENTS.md §2` (`.harness/plugin-cache/*`, `claude-tdd-pro/*`, `.claude/skills/tdd-pro-*`, `.cursor/rules/*.mdc`); exit 2 + stderr violation message + rule citation on hit; exit 0 otherwise. Wire into `.claude/settings.json` PostToolUse array alongside existing SessionStart. Defensive: non-edit tools and edits to non-forbidden paths exit 0 (no-op). Land ADR-0022 (PostToolUse not PreToolUse per briefing direction; forbidden-path-only scope per D-8; exit-2 surfaces error but doesn't block agent — review gate not kill switch; defense in depth with F-5 + F-6 catching persistent state at pre-commit). | `bash -n .claude/hooks/post-tool-use-review-gate.sh` clean; positive test (allowed path) → exit 0; negative tests (`.harness/plugin-cache/`, `.cursor/rules/agent-context.mdc`, `claude-tdd-pro/`) → exit 2 + rule-citation message; non-edit tool → exit 0; `.claude/settings.json` parses + adds PostToolUse hook entry; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/smoke-e2e.sh` exit 0; ADR-0022 follows numbered template. **DONE 2026-05-26.** |
| TICKET-016 | Researcher discipline (WebFetch→WebSearch fallback)   | Persist the WebFetch→WebSearch→cross-attribute fallback chain that recovered Sources 4–8 (and lands Source 9 in TICKET-017) as a discoverable TIER-2 operational rulebook. Per the 2026-05-26 user direction: *"Persist the solution in order that the problem never blocks work in this repo again."* Ship `docs/researcher-discipline.md` — 10 sections (purpose, block-class diagnosis via `curl -sIL` header probe, fallback chain, verification-tier mapping, cross-source acceptance bar ≥ 3 sources + primary-operated anchor for T-C, verification-block shape, future de-blocking path, anti-patterns, authority + amendment, CL verification). Modify `AGENTS.md §5` to enumerate the new doc; modify `scripts/export-cursor-rules.sh` to include in agent-context.mdc TIER-2 list; regenerate. Land ADR-0023 (new TIER-2 doc not §1 sub-section per D-6 immutability; no automation script at v1 per D-8 — human judgment load-bearing; strict ≥ 3 sources + primary-operated anchor bar codifies what Sources 4+5 already did; cross-tool — applies to Claude Code/Cursor/Grok Build/headless equally). | `test -f docs/researcher-discipline.md`; §1–§10 grep-detectable; cited primitives resolve; AGENTS.md §5 lists doc; export-cursor-rules.sh regenerated + `--check` exit 0; audit-doc-drift exit 0; smoke-e2e exit 0; audit-manifest exit 0; ADR-0023 follows numbered template; §1 sources untouched; no new D-rules. **DONE 2026-05-26.** |
| TICKET-017 | Elevate "Grok Build Beta" (x.ai/cli) as §1 Source 9    | Append Source 9 to `docs/founder-directives.md §1` (immutable + append-only per D-6 — Sources 1-8 byte-identical). Source 9 = canonical post-launch Grok Build product surface at `x.ai/cli`, distinct from Source 4 (2026-05-14 launch announcement at `x.ai/news/grok-build-cli`). Verbatim recovery via `docs/researcher-discipline.md §3` fallback chain (WebFetch → WebSearch → cross-attribute across 11 indexed secondary sources with `docs.x.ai/build/modes-and-commands` as primary-operated anchor); T-C tier per `docs/researcher-discipline.md §5` acceptance bar. Source 9 content: installer command `curl -fsSL https://x.ai/cli/install.sh \| bash`; parallel subagents architecture (ratifies G-7/G-8/G-9); plan-mode discipline (ratifies G-12); AGENTS.md + plugins + hooks + skills + MCP composition out of the box (ratifies G-10); `/hooks`/`/plugins`/`/skills`/`/mcps`/`/feedback` slash commands documented as cross-tool evidence (not adopted in harness per D-13). Dan (@Daniel_Farinax) X post (x.com/Daniel_Farinax/status/2059002180481204461, 2026-05-25) folded as T-D supplementary inside Source 9's verification block (community-onboarding video; no independent architectural weight; per `docs/researcher-discipline.md §5` single-secondary-source = T-D not T-C). First §1 entry to cite `docs/researcher-discipline.md` by path rather than open-coded prose. Modify CLAUDE.md "Sources currently elevated" list. Append 2026-05-26 entry to AUTOMATION_INTEL.md (Grok Build Beta GA distribution + installer command + cross-vendor pitch hook). Land ADR-0024 (single Source 9 not Source 9+10; T-C tier not T-D — 11 secondary sources + primary-operated anchor; no new D-rules — Source 9 ratifies G-rules without extending; no grok-orchestration-principles.md body amendment per D-8). | Source 9 present in §1; Sources 1-8 byte-identical (git diff verified); T-C verification block explicit + cites researcher-discipline.md; T-D paraphrase tag explicit for Farinax post; CLAUDE.md updated; AUTOMATION_INTEL.md entry added; ADR-0024 follows numbered template; audit-doc-drift exit 0; smoke-e2e exit 0; export-cursor-rules --check exit 0; audit-manifest exit 0; no new D-rules; §3 D-rule bodies untouched; §4 D-checklist untouched. **DONE 2026-05-26.** |
| TICKET-018 | Bump plugin pin to upstream HEAD 23e5c2b              | Investigate the 3-file contract-surface drift that has shown WARN every session since 2026-05-24 (CLAUDE.md, docs/architecture-v1.9.md, .claude/skills/tdd-pro-cl-workflow/SKILL.md). Classify upstream commit `23e5c2b architecture-dev: 39 CLs, 22 CC contracts, §25 fidelity gate, AI-dev corpus (+379 specs) (#2)` against `docs/architecture-principles.md §15` + `docs/researcher-discipline.md §2` diff-classification framework. All 3 changes ADDITIVE (new "PRIMARY RULESET" pointer in upstream's own CLAUDE.md; new §2.25 fidelity-gate contract; new Step 0.5 in tdd-pro-cl-workflow/SKILL.md that fires ONLY for pending-spec promotion CLs — no-op for harness consumers). Update `docs/claude-tdd-pro.lock.yaml` with new pinned_commit + pinned_at + pinned_message + last_synced_at + last_synced_session; bump sha256 for 3 drifted contract files; preserve sha256 for 2 unchanged. Re-materialize cache via `scripts/sync-plugin.sh --ensure`. Verify `--check` exits 0 (in sync). Land ADR-0025 (additive-only classification rationale; pin-bump-only scope — TIER-2 discipline doc rejected per R-3 and `--diff` flag rejected per D-8 as over-engineering). Over-engineering filter applied: 5 prior candidates → 2 (60% cut). | Lockfile updated with new pin + 3 updated sha256s + 2 preserved; `scripts/sync-plugin.sh --check` exits 0 with "pin matches HEAD"; `--ensure` re-materializes cache at 23e5c2b; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest all exit 0); ADR-0025 follows numbered template + names rejected sub-expansions explicitly. **DONE 2026-05-26.** |
| TICKET-019 | Promote `provenance_complete` to REQUIRED             | Land quality-gate v2 promotion of Sub-gate 4 from RECOMMENDED to REQUIRED in `docs/quality-gate.md`, leveraging the structural enforcement bar shipped by the TICKET-010 trilogy (010 design + 010.a emitter + 010.b validator + 010.c --regenerate). Promote at `schema_version=1` WITHOUT bumping the wire format (R-11 tolerant reader + R-5 bilateral schema change preserved) — the corrected mechanism is gate-policy not wire-format. Update 4 sections of quality-gate.md: table row, table footnote (supersede "will be Required at schema_version=2" language), Sub-gate 4 § (definition + severity + override policy + 3 new reviewer checklist items leveraging audit-manifest.sh + emit-manifest.sh --regenerate), Defaults paragraph. Update generator mirror in `scripts/export-cursor-rules.sh gen_quality_gate`; regenerate `.cursor/rules/quality-gate.mdc`. Land ADR-0026 (gate-policy not wire-format; override allowed for red/blocked/error via response_missing source-kind; 5 alternatives REJECTED per over-engineering filter — schema bump, manifest_validates new sub-gate, manifest_freshness new sub-gate, swarm_outcomes_aggregated new sub-gate, per-CL override audit item; defer-indefinitely also rejected because the trilogy was shipped specifically to unblock this promotion). | `provenance_complete` text promoted to REQUIRED across 4 sections of quality-gate.md; generator mirror updated + cursor rules regenerated; export-cursor-rules.sh --check exit 0; audit-doc-drift exit 0; smoke-e2e exit 0 (manifest already emitted per ADR-0019); audit-manifest exit 0; ADR-0026 follows numbered template + names 5 rejected sub-expansions explicitly; no schema_version references modified (wire format stable). **DONE 2026-05-26.** |
| TICKET-020 | README operator-readiness refresh                     | Refresh `README.md` to reflect TICKETS 011-019 shipping (cross-IDE surface, swarm orchestration, provenance trilogy, researcher discipline, Source 9, pin bump, quality-gate v2). Remove pre-shipping language ("stubs filled in by individual tickets"; "Pending: TICKET 010"). Restructure into 4 operator-facing sections per the user's 2026-05-26 direction: (1) What it is + Why use it + How it works (data-flow diagram); (2) Setup — prerequisites + 5-step ready-to-use procedure (clone, sync-plugin --ensure, verify 5 audits, open IDE, smoke the operator journey against `examples/string-utils/`); (3) Daily operator workflow (7 slash commands); (4) Repo map + Status. No ADR (this is Q-DOC-DRIFT-class doc refresh per D-8; doesn't introduce new architectural decisions). | README has Setup section with explicit prerequisites + 5-step procedure; expected `--ensure` output documented; 5 verification commands listed with expected exit codes; Repo map covers AGENTS.md + CLAUDE.md + docs/ + .claude/ + .cursor/ + .grok/ + scripts/ + examples/ + .harness/; status mentions all 26 ADRs + all tickets DONE; full audit chain still exits 0 post-refresh; F-1..F-6 don't trigger on new README content. **DONE 2026-05-26.** |
| TICKET-021 | Wayfair / 2026 harness-engineering intel — filtered extension | Branch off main; consider 2026-05-26 architect briefing on Wayfair's AI efforts + 2026 industry harness-engineering trends (OpenAI Codex, Anthropic agent harnesses, Meta REA, Microsoft Azure SRE, Google Jetski, Stripe/Shopify/Airbnb cohort). Apply the over-engineering filter established by ADR-0025 + ADR-0026 with the strictest reading per the architect's standing "qualify and quantify over-engineering" directive. Evaluate 7 expansion candidates: Next.js demo target, `scripts/audit-metrics.sh`, initializer+incremental TIER-2 doc, cross-session observability surface, TIER-2 competitive-positioning doc, §1 Source 10 elevation, and this ADR itself. REJECT 6 of 7 (~86% cut) with rationale per ADR-0027 §Decision-1. ACCEPT the ADR (to persist the rejection rationale) + an AUTOMATION_INTEL.md append entry (capture T-D intel per ADR-0023 §5 cross-source bar; document metric-derivation one-liners closing C2's gap at zero substrate cost; document deferral triggers per candidate). Zero new substrate; zero new scripts; zero new TIER-2 docs; zero §1 amendments. | ADR-0027 follows numbered template; 6 rejections each cite specific filter criteria; AUTOMATION_INTEL.md gains 2026-05-26 Wayfair-intel entry tagged "T-D paraphrase"; metric-derivation one-liners verifiably runnable against existing `.harness/audit/*.manifest.json`; §1 Sources 1-9 byte-identical (D-6 immutability honored); no `.cursor/rules/` changes; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest all exit 0); 6 deferral triggers explicitly named for future re-triage. **DONE 2026-05-26.** |
| TICKET-022 | Persist QUICKSTART.md as operator entry point          | Ship `QUICKSTART.md` at repo root as the first-encounter operator entry point per the 2026-05-26 user direction ("persist this file in the repo in a location and way that provides for best user experience and in order that they encounter it first"). Content: 8 sections — what it is + reality check; prerequisites (60-sec verification); bootstrap (3-min, 5 verification commands); usage modes table (A inside-repo / B harness-as-template / C pattern-copy); first real cycle (15-min, `/research → /decompose → /dispatch → /inner-loop → /audit` against kebabCase example); 7 slash commands daily-workflow table; 7 common gotchas (PostToolUse Claude-Code-only, smoke trap, /inner-loop prereq, TICKETS append-only, §1 immutable, .harness gitignored, pin-bump ADR); next-steps pointers + 3-action minimum-to-start. Cross-reference from 4 entry points so operators encounter it first regardless of which surface they hit: README.md (prominent top-of-file pointer above "What it is"), AGENTS.md (preamble), CLAUDE.md (preamble), `.cursor/rules/agent-context.mdc` (regenerated via `scripts/export-cursor-rules.sh` `gen_agent_context` update). No ADR (this is operator-UX doc; doesn't introduce architectural decisions — per ADR-0027 §Decision-2 precedent for Q-DOC-DRIFT-class additions). | `test -f QUICKSTART.md`; 8 `## §N ` sections grep-detectable; README + AGENTS.md + CLAUDE.md + `.cursor/rules/agent-context.mdc` all grep-positive for "QUICKSTART"; export-cursor-rules `--check` exit 0 (generator + on-disk in sync); full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest all exit 0); 3-action minimum-to-start named with concrete commands + expected outputs. **DONE 2026-05-26.** |
| TICKET-023 | Substrate-script test discipline + 100% surface coverage | Per 2026-05-26 user directives: (a) "Bring it into A grade in 15 mins" responding to simulated Fowler+team critique (which graded test-discipline-practiced at D+); (b) "Also report code coverage by unit tests. It should be 100% unit test coverage"; (c) standing guardrail "No regressions or drift from architectural design." Ship `tests/test-*.sh` for all 8 substrate surfaces (6 scripts + 2 hooks): sync-plugin, audit-doc-drift, audit-manifest, emit-manifest (21 assertions; most contract surface), export-cursor-rules, smoke-e2e, post-tool-use-review-gate (4 forbidden patterns), session-start (warn-only exit-0 policy). Plus `tests/test-all.sh` aggregator + `tests/README.md` with honest coverage scope statement. Bash 3.2 + BSD portable; native assertions; no `bats`/`shellspec`/`pytest` dependency per D-8 + C-23. Restore-before-assert pattern for any state-mutating test. ~72 total assertions across 8 suites; 8/8 passing at commit. Land ADR-0028 (100% defined honestly as surface coverage NOT line/mutation/exhaustive-input; 8 alternatives REJECTED per filter — kcov tooling, mutation testing, CI integration, F-7 pattern, test framework, refactor for testability, property/fuzz testing, exhaustive input space). **No regressions** verified: full audit chain (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + sync-plugin --check) all exit 0 post-test-shipping. **No architectural drift** verified: §1 of founder-directives byte-identical; D-rule count unchanged; TIER-0 corpus untouched; AGENTS/CLAUDE/QUICKSTART untouched; wire-format schema_version unchanged. | `tests/test-all.sh` exits 0 with 8/8 suites passing; coverage report shows 8/8 substrate surfaces tested (= 100%); `bash -n` clean on all 8 test files; full audit chain green; `git diff docs/founder-directives.md` returns 0 lines; ADR-0028 follows numbered template + names 8 alternatives REJECTED per over-engineering filter + states 100% scope honestly. **DONE 2026-05-26.** |
| TICKET-024 | Fowler critiques #5/#6/#7/#8 closures (A− → A) | Per 2026-05-26 user directive "Bring it up to an A" responding to the regrade of A− post-TICKET-023. Close 4 of the 8 named Fowler critiques per the regrade's priority-ordered closure path; 3 + 1 already closed (#2 via TICKET-023) = 5/8 critiques closed = solid A grade. **Critique #5** (single-reviewer filter): establish `Second voice` field in ADR template; demonstrate by self-reference in ADR-0029 (simulated Fowler+team regrade IS the second voice). **Critique #6** (tamper-evident overstates): rename to "drift-detectable when paired with original-manifest preservation" across 4 operator-visible files (README, QUICKSTART, AUTOMATION_INTEL ×2); ADR-0027's historical occurrence preserved per Nygard append-only; ADR-0029 §Supersedes records the supersession. **Critique #7** (R-11 vs §5 tension): add `docs/researcher-discipline.md §10 Wire-vs-authority strictness layering` naming the coherent defense-in-depth pattern (TOLERANT data plane / STRICT control plane) with worked example (provenance manifest); §10 ↔ §11 swap preserves monotonic ordering. **Critique #8** (self-referential complexity): add explicit "what this solves vs. a CONTRIBUTING.md + a few scripts" paragraph to QUICKSTART §1 — names the multi-tool / multi-session / multi-agent / cross-IDE enforcement boundary as the value-prop; honestly acknowledges when a simpler approach suffices. Land ADR-0029 (5th over-engineering-filter application; 7 alternatives REJECTED — split-into-4-CLs, close-all-8-in-one-CL, edit-ADR-0027-in-place, F-7-audit-pattern, "tamper-resistant"-rename, new-TIER-2-doc, replace-vs-extend-QUICKSTART). Three critiques remain open with named effort estimates: #1 (dead-rulebook audit, 60-90 min); #3 (TIER-hierarchy refactor, 60+ min); #4 (swarm integration test, 30 min). **No regressions / no architectural drift** verified per the standing guardrail. | `grep -c 'tamper-evident' README.md QUICKSTART.md AUTOMATION_INTEL.md` returns 0 (4 operator-visible renames done; ADR-0027 historical preserved); `docs/researcher-discipline.md §10` exists with monotonic section numbering 1..11; QUICKSTART §1 carries the explicit boundary paragraph; ADR-0029 front-matter includes `Second voice` field demonstrating the pattern; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + sync-plugin --check); `tests/test-all.sh` 8/8 suites passing; `git diff docs/founder-directives.md` returns 0 lines; ADR-0029 follows numbered template + names 7 alternatives REJECTED per filter. **DONE 2026-05-26.** |
| TICKET-001.e | Plugin lock file + session-start sync           | Operational mechanism for the plugin-dependency model: `docs/claude-tdd-pro.lock.yaml` + `scripts/sync-plugin.sh` + `.claude/hooks/session-start.sh` + `.claude/settings.json` + `docs/plugin-sync.md` + ADR-0001. Closes R-2/R-5 enforcement gap. | Lock file pinned to upstream HEAD; `sync-plugin.sh --check` exits 0 on a fresh clone; SessionStart hook prints the drift report; ADR-0001 recorded. **DONE 2026-05-25.** |
| TICKET-001.f | Persist founder directives as TIER-1 rulebook    | Persist the 2026-05-24 X posts (@teslayoda + @elonmusk) verbatim in `docs/founder-directives.md` with immutable §1 provenance; derive seven numbered directives (D-1..D-7) for grok-side enterprise-pattern learning, terminal-state loops, progressively harder CLs, production-grade problem instances, and provenance immutability; wire as TIER-1 authority co-equal with the CLAUDE.md prime directive. | `docs/founder-directives.md` exists with §1 verbatim and §3 D-rules; `CLAUDE.md` references it under "Authoritative founder-directives rulebook" before the R-/G-/C-rulebook sections; amendment process points at ADR. **DONE 2026-05-25.** |
| TICKET-001.g | Expand founder-directives from public xAI/Anthropic/Musk-orbit sources | Land ADR-0002. Add five §1 sources (Musk's Algorithm; xAI Grok Build launch; Schluntz/Zhang Building Effective Agents; Amodei Machines of Loving Grace; Karpathy agentic-engineering shift). Introduce a verification-tier model (T-A direct primary / T-B screenshot / T-C search-engine-indexed / T-D substantive paraphrase) since `WebFetch` was blocked across all primary URLs. Derive five new D-rules (D-8 deletion discipline; D-9 simple-composable-patterns; D-10 TDD + supervision; D-11 design FOR agent-CLI primitives; D-12 production-grade trustability is the harness's value). Broaden the rulebook's scope statement to cover Anthropic leadership. | `docs/founder-directives.md` §1 has Sources 3-7 with explicit Verification tier; §3 has D-1..D-12; §4 checklist covers all 12; `CLAUDE.md` reflects the new count; ADR-0002 records the decision and verification-tier model. **DONE 2026-05-25.** |
| TICKET-001.h | Persist Anthropic "Best practices for Claude Code" as Source 8 (first T-A) | Land ADR-0003. Append Source 8 to §1 of `docs/founder-directives.md` with full verbatim article text (heading-demotion-by-two as the only transcription transformation, documented in the entry metadata). Derive D-13 (context-as-fundamental-constraint + five named failure patterns). Update §3 header to D-1..D-13, extend §4 checklist with D-13 item, update CLAUDE.md count and source summary. Establish the canonical T-A capture pattern for the forthcoming "I am getting the others" series of primary-text additions. | Source 8 present in §1 at tier T-A; D-13 present in §3; CLAUDE.md updated; ADR-0003 records the decision and canonicalizes the T-A transcription discipline; `grep founder-directives CLAUDE.md` still ≥4. **DONE 2026-05-25.** |
| TICKET-001.i | Adopt AI engineering corpus as third TIER-1 rulebook | Land ADR-0004. Persist drumfiend21's compiled "AI Software Development Best Practices Corpus" verbatim in new sibling file `docs/ai-engineering-corpus.md` with framing (authority tier, provenance chain cross-referencing founder-directives §1 Sources 3/5/6/7/8, amendment process, pre-commit corpus checklist). Elevate to TIER-1 sibling to the prime directive and founder-directives. Add "Authoritative AI engineering corpus rulebook" section to CLAUDE.md (between founder-directives and architectural rulebook sections). Update founder-directives §5 authority-tier table to list three TIER-1 rulebooks and generalize the conflict-resolution rule. Derive NO new D-rules (corpus operates at procedural-playbook level, not directive level — adding D-rules would conflate the categories). | `docs/ai-engineering-corpus.md` exists with verbatim body + framing; CLAUDE.md references it under a new TIER-1 section; founder-directives §5 table lists three TIER-1 entries; ADR-0004 records the decision and the alternatives considered. **DONE 2026-05-25.** |
| TICKET-001.j | Elevate AI engineering corpus to TIER 0 (supreme operating directive) | Land ADR-0005. Per drumfiend21's explicit instruction (2026-05-25), make `docs/ai-engineering-corpus.md` the highest-priority ruleset and instruction for architecture, planning, and development. Introduce TIER 0 as a new top tier in the authority hierarchy: corpus = TIER 0; prime directive + founder-directives = TIER 1 beneath; R-/G-/C-rulebooks = TIER 2. Restructure CLAUDE.md so the corpus section sits at the top under "Supreme operating directive (TIER 0)" framing, before the prime directive. Update conflict-resolution rule: corpus wins over everything; only legitimate override is an ADR-landed amendment to the corpus itself. Update founder-directives §5 tier table and ai-engineering-corpus.md authority statement to reflect TIER 0. No D-rule edits. | CLAUDE.md TIER 0 section is the first authority section, before prime directive; tier table in founder-directives §5 shows TIER 0/1/1/2/2/2; ai-engineering-corpus.md authority section declares TIER 0 and references ADR-0005; `grep founder-directives CLAUDE.md` still ≥4. **DONE 2026-05-25.** |
| TICKET-006.a | Drift correction from architectural audit             | Realign repo contents with the design substrate after an architectural drift audit found 6 P0/P1 items: (F-1) README.md §Status claimed "Design-only. No code yet." despite TICKETS 001–006 done; (F-2) README.md said "Wiring is defined in TICKET-004" in future tense; (F-3) `scripts/sync-plugin.sh --help` omitted the `--ensure` mode added by ADR-0007; (F-4) `docs/plugin-sync.md` lines 19–20 omitted `--ensure` and described the SessionStart hook as calling only `--check`; (F-5) `docs/skill-consumption.md` was an orphan stub "filled in by TICKET-004" though TICKET-004 had shipped; (F-7) `docs/architecture.md` §"Handoff contract" duplicated a stale partial schema in violation of R-3 and used future tense for TICKET-002. The agent's one finding (TICKET-001.f scope "D-1..D-7") was a false positive — accurate historical-ledger entry. No new architectural decisions; no new ADR. | All 6 drift findings closed; `bash -n` clean on edited scripts; smoke still exits 0; README.md no longer claims "Design-only"; `sync-plugin.sh --help` lists `--ensure`. **DONE 2026-05-25.** |
| TICKET-006.b | Doc-drift prevention mechanism                        | Ensure the TICKET-006.a class of drift cannot recur silently. Three-layer fix per ADR-0009: (1) **Mechanical** — new `scripts/audit-doc-drift.sh` catching the four patterns observed in the audit: F-1 stale stubs, F-2 stale README framing, F-3 future-tense for done tickets, F-4 sync-plugin.sh mode parity (impl vs --help). Exit 0 clean, 1 with findings. (2) **Procedural** — new checklist item `Q-DOC-DRIFT` in `docs/founder-directives.md §4` under joint D-12 + D-13 authority requiring the audit (or equivalent inspection) before every commit. (3) **Structural** — ADR-0009 captures the lesson + alternatives. Not wired to SessionStart hook (would bloat startup; doc-drift is a pre-commit concern). | `scripts/audit-doc-drift.sh` exits 0 on the post-TICKET-006.a clean state; reintroducing any of F-1/F-2/F-4 produces exit 1 + a labeled finding; `docs/founder-directives.md §4` lists Q-DOC-DRIFT; ADR-0009 lands. **DONE 2026-05-25.** |
| TICKET-011 | AGENTS.md as cross-tool agent-binding surface         | Ship `AGENTS.md` at repo root per the LF agent-binding-subset schema. Eight sections, each path-anchored pointer (no duplication per R-3): build/test commands, file-scope rules, wire-format pointer, skill enumeration, authority-doc enumeration (TIER 0/1/2), outer-loop template pointers, session-start ritual, pre-commit gates. Closes long-standing `docs/grok-orchestration-principles.md §8` gap. Land ADR-0012 (AGENTS.md as cross-tool surface; agent-binding subset shipped; LF-draft advanced fields deferred). Land ADR-0013 (D-1 bidirectional attribution; symmetric reading; parallel §4 checklist item under D-1). Modify `CLAUDE.md` with one-line "Cross-tool agent-binding surface" pointer to AGENTS.md. Modify `docs/founder-directives.md §4` to add D-1 reverse checklist item (§3 D-rule bodies and §1 provenance untouched per D-6). | `test -f AGENTS.md`; required markers grep-detectable (`handoff-contract.md`, `skills`, `TIER 0`, `TIER 1`, `TIER 2`, `.grok/templates`, `sync-plugin`, `audit-doc-drift`); `./scripts/audit-doc-drift.sh` exit 0; both ADRs follow numbered template; `docs/founder-directives.md §4` lists the new D-1 reverse item; CLAUDE.md gains "Cross-tool agent-binding surface" section. **DONE 2026-05-26.** |
| TICKET-012 | Cursor integration design doc                         | Ship `docs/cursor-integration.md` as the single TIER-2 operational rulebook explaining how the harness is used INSIDE the Cursor IDE. Nine sections mirroring `docs/self-healing-design.md`'s structure: purpose, position in harness (operator stack), surfaces table mapping every harness feature → Cursor driver, Cursor's chat agent as inner-loop driver (primary), headless Claude Code as alternative inner-loop driver, outer-loop templates from Cursor, session-start ritual under Cursor (always-loaded-rule equivalent to Claude Code's push-hook), failure modes (8 named with structural mitigations), sequencing to TICKETS 013-014. Land ADR-0015 (Cursor as host IDE — operator stack: Cursor → Grok/Claude → harness primitives; TIER-2 not TIER-1). | `test -f docs/cursor-integration.md`; nine `## §N ` sections grep-detectable; every cited primitive resolves to a real file; `./scripts/audit-doc-drift.sh` exit 0; zero duplication of upstream content (R-3); ADR-0015 follows numbered template. **DONE 2026-05-26.** |
| TICKET-013 | .cursor/rules/ generator + F-5 audit pattern          | Ship `scripts/export-cursor-rules.sh` and four generator-output `.cursor/rules/*.mdc` files (agent-context, harness-overview, quality-gate, d-rules). Materializes Cursor's always-loaded-rule equivalent to Claude Code's SessionStart hook (`docs/cursor-integration.md §7`). Each file carries `# Generated by ...` header; hand-edits forbidden. Wire generator into `scripts/sync-plugin.sh --ensure` as additive step after cache materialization. Extend `scripts/audit-doc-drift.sh` with F-5 pattern that re-runs the generator in `--check` mode and emits a finding for any drift. `.gitignore` adds `.harness/cursor-sessions/` as forward-looking reserve; `.cursor/rules/` is NOT gitignored. Land ADR-0014 (generator-output-only; F-5 audit pattern; hand-edits forbidden; `quality-gate.mdc` content is hand-maintained mirror at v1 with rationale). | Fresh `--ensure` produces all four `.mdc` files; each carries the "Generated by" header; running generator twice produces zero diff (`--check` exit 0); appending a hand-edit triggers `F-5 (cursor-rules drift):` finding; regeneration clears the finding; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/smoke-e2e.sh` exit 0; `bash -n` clean on all three scripts; `sync-plugin.sh --help` unchanged (F-4 still passes). **DONE 2026-05-26.** |
| TICKET-014 | .cursor/commands/ slash commands (operator surface)   | Ship seven slash-command markdown files at `.cursor/commands/` across three classes: Class A terminal wrappers (`sync.md`, `smoke.md`, `audit.md`), Class B outer-loop drivers (`research.md`, `decompose.md`, `dispatch.md`), Class C inner-loop driver (`inner-loop.md`). Each file follows the uniform Purpose / Inputs / Steps / Success criteria / Composition template. `/inner-loop` makes Cursor's chat agent the default inner-loop driver inside Cursor; headless `claude -p` remains the documented alternative per `docs/cursor-integration.md §5`. Update `docs/cursor-integration.md §3` surfaces table to surface the seven slash commands in present tense (drop "Once shipped:" hedges). Append "Hybrid Harness v0.2 reconciliation complete" entry to `AUTOMATION_INTEL.md` per v0.2 spec §5. Land ADR-0016 (Cursor slash commands as operator surface; Cursor's chat agent as default inner-loop driver; uniform five-section template; MCP server deferred to TICKET-017). | All seven `.cursor/commands/*.md` exist; each contains the five required sections (Purpose / Inputs / Steps / Success criteria / Composition); each cites the script-wrapped or template-driven primitive plus the D-1 reverse trailer; `docs/cursor-integration.md §3` surfaces the commands in present tense; `AUTOMATION_INTEL.md` gains the v0.2 reconciliation entry; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/smoke-e2e.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; Q-DEMO step is operator-attested per the smoke-script pattern. **DONE 2026-05-26.** |
| TICKET-015 | orchestrating-swarms skill (worker-fanout)            | Ship `.claude/skills/orchestrating-swarms/SKILL.md` materializing G-rules §§4, 9, 10 + G-7, G-8, G-9, G-16 — the orchestrator-worker pattern. Worker-fanout mode only (composes on Grok's outer-loop decomposition per G-7; does NOT replace it). Steps 0–7: pre-flight G-rule extraction, decomposition acceptance, file-scope conflict check (overlap → serialize per G-8), bounded fan-out (≤8 per G-9), per-worker spawn (one worktree per worker per G-8; each worker runs existing `tdd-pro-cl-workflow` R-G-R), output collection, synthesis (lead surfaces; does NOT auto-retry — that's self-healing's territory per ADR-0011), worktree cleanup. Architect / Builder / Validator from the briefing map sequentially within ONE worker to R-G-R, not as three parallel sub-agents per ticket. Driver compatibility: Claude Code Task tool = native parallel; Cursor degrades to serial; documented honestly. Add §4 fourth bullet to `AGENTS.md` framing the skill as orchestration-tier sibling (NOT a fourth tdd-pro-core skill). Append `## 2026-05-26 — Swarm orchestration v1 (MVP)` entry to `AUTOMATION_INTEL.md` per briefing #2 action. Land ADR-0017 (worker-fanout mode chosen; MVP scope; deferrals named — PostToolUse hooks, self-healing tests, lead-orchestrator mode, Apple/Google triggers, weekly debt cron, hierarchical multi-supervisor, daemon worktree cleanup). | `test -f .claude/skills/orchestrating-swarms/SKILL.md`; YAML frontmatter parseable (`name: orchestrating-swarms`, non-empty description); all eight Step markers grep-detectable (`## Step 0` through `## Step 7`); all cited primitives resolve to real paths; D-1 reverse attribution trailer present (per ADR-0013); `AGENTS.md §4` lists four skills (3 inner-loop + 1 orchestration); `AUTOMATION_INTEL.md` gains 2026-05-26 entry; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/smoke-e2e.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; ADR-0017 follows numbered template; Q-DEMO step is operator-attested per ADR-0008 / ADR-0016 pattern. **DONE 2026-05-26.** |

## Conventions

- Commit message format: `TICKET-NNN: <verb> <object>`
- One ticket per CL. If scope grows mid-ticket, split into TICKET-NNN.a / .b rather than expanding the original.
- Closing a ticket means the "Done when" column passes and is verified in the commit body.
