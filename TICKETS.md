# TICKETS.md

Ordered backlog. Tickets are sized to ~1 CL each. Later tickets assume earlier ones landed.

| ID         | Title                                              | Scope                                                                                                                                                                                                       | Done when                                                                                                              |
| ---------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| TICKET-001 | Scaffold repo + README + CLAUDE.md pointer         | Create top-level dirs; README states purpose; CLAUDE.md points at claude-tdd-pro/CLAUDE.md and adds the 2-rule harness contract                                                                              | `tree -L 2` shows the layout; README under 60 lines. **DONE 2026-05-24.**                                              |
| TICKET-002 | Handoff contract spec                              | Write `docs/handoff-contract.md` defining Grok→Claude payload schema (ticket-id, accept-criteria, file-scope, context) and Claude→Grok response schema (changed-files, test-results, decision-trail-ref)    | Schema is concrete enough that a stub Grok script could produce it. **DONE 2026-05-24.**                               |
| TICKET-003 | Grok orchestrator prompt templates                 | Populate `.grok/` with prompt templates for research, decomposition, sub-agent dispatch, deploy. Headless `-p` mode assumed                                                                                  | Three named templates exist; each has input vars + output shape documented. **DONE 2026-05-25** (3 templates shipped — deploy.md deferred per ADR-0006). |
| TICKET-004 | Claude TDD Pro consumption wiring                  | `.claude/` config that loads the three `tdd-pro-*` skills (cl-workflow, batch-cl, bash32-portability) from claude-tdd-pro into prototype-repo sessions                                                       | Running `claude -p` in the prototype repo surfaces the three skills. **DONE 2026-05-25** (symlinks committed; `sync-plugin.sh --ensure` added; SessionStart hook materializes cache; verified end-to-end in implementation session — see ADR-0007). |
| TICKET-005 | Toy module + Red-Green-Refactor demo target        | Add a tiny module under `examples/` (e.g. string-utils) with obvious R-G-R potential                                                                                                                         | Module compiles/runs; has one obviously-missing behavior to drive a Red test. **DONE 2026-05-25** (`examples/string-utils/`; `node --test` returns 4 pass / 1 fail, exit=1; the failing case is `slugify('  hello world  ')` which lacks `.trim()`). After this, D-5 lockdown applies to all `examples/`. |
| TICKET-006 | End-to-end smoke script                            | Bash or Node script: Grok decompose → emit handoff → invoke `claude -p` with tdd-pro skill → assert green tests → report. No new feature logic — just wiring                                                  | Script exits 0 on the TICKET-005 toy module after one full R-G-R cycle. **DONE 2026-05-25** (`scripts/smoke-e2e.sh`; stub mode default; live-Claude mode deferred per ADR-0008; full wire format real and contract-valid; trap reverts toy to preserve TICKET-005 Red baseline; idempotent across runs). |
| TICKET-007 | Quality-gate contract                              | `docs/` page formalizing what "green" means for the inner loop (test pass, lint, coverage delta). Borrows §2.X cross-cutting contract names where applicable from claude-tdd-pro                              | Single-page doc; reviewer can checklist against it. **DONE 2026-05-25** (`docs/quality-gate.md`; four sub-gates — `tests_must_pass`, `coverage_delta_min`, `lint_clean`, `provenance_complete`; `provenance_complete` added as additive recommended at v1, promoted to required at v2 via future ADR; severity enum borrowed from plugin `§2.1`, provenance gate composed on `§2.8`; reviewer checklist per sub-gate + cross-cutting integrity checks; `handoff-contract.md:58` updated to point at the new doc; see ADR-0010). |
| TICKET-008 | Self-healing extension design                      | Design doc only: long-running Grok monitor that watches debt thresholds and re-spawns Claude TDD Pro for refactor cycles. Includes failure modes                                                              | Design doc; no code. **DONE 2026-05-25** (`docs/self-healing-design.md` — 18 sections covering architecture, 7 signals, severity-based dispatch, 7 named failure modes with structural mitigations, HITL integration for P2 signals, state/config/observability, composition on plugin `§§2.8, 2.11, 2.14, 2.15, 2.17`; implementation deferred to TICKET-008.a..e per §16 sequencing; no new wire format; see ADR-0011). |
| TICKET-009 | Demo storyboard for Boston enterprise signals      | `docs/demo-storyboard.md` with a 5-step demo flow for State Street (compliance) + HubSpot (Breeze AI standardization). Includes "what to show on screen at minute N" beats                                    | Storyboard reads like a director's shot list. **DONE 2026-05-25** (`docs/demo-storyboard.md` — 5 steps × ~1 minute each: cold start, inner-loop discipline, the wire, the long loop, the contract; each step ships beats table + per-audience talking points + contingencies; Q&A reserve cues for 7 likely questions; pre-demo + post-demo checklists; cites the AUTOMATION_INTEL.md pitch hook verbatim). |
| TICKET-010 | Provenance + decision-trail bridging               | Design: how Grok's research provenance (`research_refs` in `.req.json`), Claude's decision trail (`.harness/trails/TICKET-NNN.md`), and the inner-loop response (`.res.json` `gate_results` / `changed_files` / `test_results`) merge into a single audit artifact per ticket. Index-only manifest at `.harness/audit/TICKET-NNN.manifest.json` (R-3 — no content duplication); sha256 per source for tamper detection; `upstream_provenance_manifest_ref` cross-references `claude-tdd-pro/docs/architecture-v1.9.md §2.8 AI Provenance Manifest` (ticket text historically said §2.2 — corrected: §2.2 is the unrelated Detector contract; §2.8 is the AI Provenance Manifest). Design doc only at v1; emitter / validator / `--regenerate` CLI deferred to TICKET-010.a..c. Land ADR-0018 (index-only design; upstream §2.8 referenced by path not duplicated; v1 = design + ADR, emitter deferred). Update `AGENTS.md §5` to enumerate the new doc. Update `scripts/export-cursor-rules.sh` to include the new doc in `.cursor/rules/agent-context.mdc` TIER-2 enumeration; regenerate. | Design doc; defines the merged artifact's shape. Verified: doc exists with §1–§13 sections; every cited primitive resolves; AGENTS.md §5 lists provenance-bridging-design.md; export-cursor-rules.sh updated + `.cursor/rules/agent-context.mdc` regenerated; audit + smoke + cursor-rules-check all exit 0. **DONE 2026-05-26.** |
| TICKET-010.a | Provenance manifest emitter                          | Ship `scripts/emit-manifest.sh` implementing ADR-0018 §6 pseudocode (bash 3.2 + BSD portable; hand-rolled JSON via printf; sha256 via sha256sum/shasum fallback; atomic-rename write per §8 failure-mode-3). CLI: `--ticket <id>` required + `--driver <name>` + `--upstream-ref <path>` + `--quiet` optional. Exit codes: 0 (clean), 1 (warning — response/trail missing or upstream-ref dangling; manifest still written), 2 (hard error). Wire into three call sites defensively (additive evidence, not a gate): `scripts/smoke-e2e.sh` (post-trail-write call); `.cursor/commands/inner-loop.md` (new Step 7 manifest emission; Step 8 renumbered surface); `.claude/skills/orchestrating-swarms/SKILL.md` (Step 5 reads manifest as entry point + synthesis fallback for non-emitter-aware drivers like `claude -p`). `.gitignore` adds `.harness/audit/`. Land ADR-0019 (CLI surface; defensive call-site handling; hand-rolled JSON; gating semantics deferred to quality-gate v2). | `bash -n scripts/emit-manifest.sh` clean; `./scripts/smoke-e2e.sh` exits 0 and produces parseable `.harness/audit/TICKET-042.manifest.json` with status=green and 3 sources; sha256 mismatch detected when source hand-edited; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; ADR-0019 follows numbered template. **DONE 2026-05-26.** |
| TICKET-010.b | Provenance manifest schema validator + F-6 audit pattern | Ship `scripts/audit-manifest.sh` validating every `.harness/audit/*.manifest.json` against the v1 schema per `docs/provenance-bridging-design.md §3`. bash 3.2 orchestration + `node -e` for JSON parse (node already harness-required via smoke-e2e.sh). Checks: 8 required top-level keys present; `schema_version === "1"`; `ticket_id` non-empty string; `created_at` ISO-8601 UTC regex; `status ∈ {green, red, blocked}`; `sources` non-empty array; per-source kind enum + path string + sha256 64-char hex + size_bytes non-negative number; `response_missing` kind has sha256=null + size=0; `upstream_provenance_manifest_ref` null OR existing-path string; `manifest_generator.tool` non-empty string; `signature` null at v1. Two invocation modes: walk all (`./scripts/audit-manifest.sh`) or one-shot (`./scripts/audit-manifest.sh path/to/file.json`). Wire as F-6 pattern in `scripts/audit-doc-drift.sh` (delegates to audit-manifest.sh; defensive skip when missing, matching the F-5 pattern from ADR-0014). Land ADR-0020 (node for JSON parse, not jq/python3/grep; exhaustive v1 schema scope, not lenient; F-6 delegates to standalone script, doesn't reimplement inline). | `bash -n scripts/audit-manifest.sh` clean; clean tree → audit-manifest exit 0; corrupt manifest (schema_version mutated) → audit-manifest exit 1 with specific finding; `./scripts/audit-doc-drift.sh` exit 0 on clean tree + exit 1 with `F-6 (manifest schema): ...` finding on corruption; `./scripts/smoke-e2e.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; ADR-0020 follows numbered template. **DONE 2026-05-26.** |
| TICKET-010.c | Provenance manifest `--regenerate` CLI                 | Add `--regenerate` flag to `scripts/emit-manifest.sh` (one script, two modes per D-9). When set: writes to `.harness/audit/<id>.manifest.regenerated.json` (never overwrites original); diffs each source's sha256 between original and regenerated; emits `<kind>: unchanged (<sha>)` or `<kind>: DRIFT` + original/current sha lines per source; exit 0 on all-match, exit 1 on any drift, exit 2 on missing original. Hand-rolled awk for sha extraction (no jq; matches ADR-0019 convention). Update `docs/provenance-bridging-design.md §6` from "design only" to "shipped." Land ADR-0021 (flag-on-existing not separate script; per-kind diff output not aggregated JSON; exit-1 on drift is CI-detectable; original NEVER overwritten — structural audit-trail integrity). | `bash -n scripts/emit-manifest.sh` clean; `-h` documents `--regenerate`; clean tree `--regenerate` exits 0 with "unchanged" per source; tamper trail → `--regenerate` exits 1 with `decision_trail: DRIFT` + sha lines; original `.manifest.json` untouched throughout; `--regenerate` without original → exit 2; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; ADR-0021 follows numbered template. **DONE 2026-05-26.** |
| TICKET-015.a | PostToolUse review-gate hook                          | Ship `.claude/hooks/post-tool-use-review-gate.sh` materializing ADR-0017's deferred TICKET-015.a (PostToolUse hooks for review gates per the 2026-05-26 Architect Automation Briefing). Bash 3.2 + `node -e` for JSON parsing. Reads Claude Code's hook-protocol JSON envelope from stdin, extracts `tool_name` + `tool_input.file_path`, fires only on `Edit | Write | MultiEdit | NotebookEdit`, checks the path against four forbidden patterns from `AGENTS.md §2` (`.harness/plugin-cache/*`, `claude-tdd-pro/*`, `.claude/skills/tdd-pro-*`, `.cursor/rules/*.mdc`); exit 2 + stderr violation message + rule citation on hit; exit 0 otherwise. Wire into `.claude/settings.json` PostToolUse array alongside existing SessionStart. Defensive: non-edit tools and edits to non-forbidden paths exit 0 (no-op). Land ADR-0022 (PostToolUse not PreToolUse per briefing direction; forbidden-path-only scope per D-8; exit-2 surfaces error but doesn't block agent — review gate not kill switch; defense in depth with F-5 + F-6 catching persistent state at pre-commit). | `bash -n .claude/hooks/post-tool-use-review-gate.sh` clean; positive test (allowed path) → exit 0; negative tests (`.harness/plugin-cache/`, `.cursor/rules/agent-context.mdc`, `claude-tdd-pro/`) → exit 2 + rule-citation message; non-edit tool → exit 0; `.claude/settings.json` parses + adds PostToolUse hook entry; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/smoke-e2e.sh` exit 0; ADR-0022 follows numbered template. **DONE 2026-05-26.** |
| TICKET-016 | Researcher discipline (WebFetch→WebSearch fallback)   | Persist the WebFetch→WebSearch→cross-attribute fallback chain that recovered Sources 4–8 (and lands Source 9 in TICKET-017) as a discoverable TIER-2 operational rulebook. Per the 2026-05-26 user direction: *"Persist the solution in order that the problem never blocks work in this repo again."* Ship `docs/researcher-discipline.md` — 10 sections (purpose, block-class diagnosis via `curl -sIL` header probe, fallback chain, verification-tier mapping, cross-source acceptance bar ≥ 3 sources + primary-operated anchor for T-C, verification-block shape, future de-blocking path, anti-patterns, authority + amendment, CL verification). Modify `AGENTS.md §5` to enumerate the new doc; modify `scripts/export-cursor-rules.sh` to include in agent-context.mdc TIER-2 list; regenerate. Land ADR-0023 (new TIER-2 doc not §1 sub-section per D-6 immutability; no automation script at v1 per D-8 — human judgment load-bearing; strict ≥ 3 sources + primary-operated anchor bar codifies what Sources 4+5 already did; cross-tool — applies to Claude Code/Cursor/Grok Build/headless equally). | `test -f docs/researcher-discipline.md`; §1–§10 grep-detectable; cited primitives resolve; AGENTS.md §5 lists doc; export-cursor-rules.sh regenerated + `--check` exit 0; audit-doc-drift exit 0; smoke-e2e exit 0; audit-manifest exit 0; ADR-0023 follows numbered template; §1 sources untouched; no new D-rules. **DONE 2026-05-26.** |
| TICKET-017 | Elevate "Grok Build Beta" (x.ai/cli) as §1 Source 9    | Append Source 9 to `docs/founder-directives.md §1` (immutable + append-only per D-6 — Sources 1-8 byte-identical). Source 9 = canonical post-launch Grok Build product surface at `x.ai/cli`, distinct from Source 4 (2026-05-14 launch announcement at `x.ai/news/grok-build-cli`). Verbatim recovery via `docs/researcher-discipline.md §3` fallback chain (WebFetch → WebSearch → cross-attribute across 11 indexed secondary sources with `docs.x.ai/build/modes-and-commands` as primary-operated anchor); T-C tier per `docs/researcher-discipline.md §5` acceptance bar. Source 9 content: installer command `curl -fsSL https://x.ai/cli/install.sh \| bash`; parallel subagents architecture (ratifies G-7/G-8/G-9); plan-mode discipline (ratifies G-12); AGENTS.md + plugins + hooks + skills + MCP composition out of the box (ratifies G-10); `/hooks`/`/plugins`/`/skills`/`/mcps`/`/feedback` slash commands documented as cross-tool evidence (not adopted in harness per D-13). Dan (@Daniel_Farinax) X post (x.com/Daniel_Farinax/status/2059002180481204461, 2026-05-25) folded as T-D supplementary inside Source 9's verification block (community-onboarding video; no independent architectural weight; per `docs/researcher-discipline.md §5` single-secondary-source = T-D not T-C). First §1 entry to cite `docs/researcher-discipline.md` by path rather than open-coded prose. Modify CLAUDE.md "Sources currently elevated" list. Append 2026-05-26 entry to AUTOMATION_INTEL.md (Grok Build Beta GA distribution + installer command + cross-vendor pitch hook). Land ADR-0024 (single Source 9 not Source 9+10; T-C tier not T-D — 11 secondary sources + primary-operated anchor; no new D-rules — Source 9 ratifies G-rules without extending; no grok-orchestration-principles.md body amendment per D-8). | Source 9 present in §1; Sources 1-8 byte-identical (git diff verified); T-C verification block explicit + cites researcher-discipline.md; T-D paraphrase tag explicit for Farinax post; CLAUDE.md updated; AUTOMATION_INTEL.md entry added; ADR-0024 follows numbered template; audit-doc-drift exit 0; smoke-e2e exit 0; export-cursor-rules --check exit 0; audit-manifest exit 0; no new D-rules; §3 D-rule bodies untouched; §4 D-checklist untouched. **DONE 2026-05-26.** |
| TICKET-018 | Bump plugin pin to upstream HEAD 23e5c2b              | Investigate the 3-file contract-surface drift that has shown WARN every session since 2026-05-24 (CLAUDE.md, docs/architecture-v1.9.md, .claude/skills/tdd-pro-cl-workflow/SKILL.md). Classify upstream commit `23e5c2b architecture-dev: 39 CLs, 22 CC contracts, §25 fidelity gate, AI-dev corpus (+379 specs) (#2)` against `docs/architecture-principles.md §15` + `docs/researcher-discipline.md §2` diff-classification framework. All 3 changes ADDITIVE (new "PRIMARY RULESET" pointer in upstream's own CLAUDE.md; new §2.25 fidelity-gate contract; new Step 0.5 in tdd-pro-cl-workflow/SKILL.md that fires ONLY for pending-spec promotion CLs — no-op for harness consumers). Update `docs/claude-tdd-pro.lock.yaml` with new pinned_commit + pinned_at + pinned_message + last_synced_at + last_synced_session; bump sha256 for 3 drifted contract files; preserve sha256 for 2 unchanged. Re-materialize cache via `scripts/sync-plugin.sh --ensure`. Verify `--check` exits 0 (in sync). Land ADR-0025 (additive-only classification rationale; pin-bump-only scope — TIER-2 discipline doc rejected per R-3 and `--diff` flag rejected per D-8 as over-engineering). Over-engineering filter applied: 5 prior candidates → 2 (60% cut). | Lockfile updated with new pin + 3 updated sha256s + 2 preserved; `scripts/sync-plugin.sh --check` exits 0 with "pin matches HEAD"; `--ensure` re-materializes cache at 23e5c2b; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest all exit 0); ADR-0025 follows numbered template + names rejected sub-expansions explicitly. **DONE 2026-05-26.** |
| TICKET-019 | Promote `provenance_complete` to REQUIRED             | Land quality-gate v2 promotion of Sub-gate 4 from RECOMMENDED to REQUIRED in `docs/quality-gate.md`, leveraging the structural enforcement bar shipped by the TICKET-010 trilogy (010 design + 010.a emitter + 010.b validator + 010.c --regenerate). Promote at `schema_version=1` WITHOUT bumping the wire format (R-11 tolerant reader + R-5 bilateral schema change preserved) — the corrected mechanism is gate-policy not wire-format. Update 4 sections of quality-gate.md: table row, table footnote (supersede "will be Required at schema_version=2" language), Sub-gate 4 § (definition + severity + override policy + 3 new reviewer checklist items leveraging audit-manifest.sh + emit-manifest.sh --regenerate), Defaults paragraph. Update generator mirror in `scripts/export-cursor-rules.sh gen_quality_gate`; regenerate `.cursor/rules/quality-gate.mdc`. Land ADR-0026 (gate-policy not wire-format; override allowed for red/blocked/error via response_missing source-kind; 5 alternatives REJECTED per over-engineering filter — schema bump, manifest_validates new sub-gate, manifest_freshness new sub-gate, swarm_outcomes_aggregated new sub-gate, per-CL override audit item; defer-indefinitely also rejected because the trilogy was shipped specifically to unblock this promotion). | `provenance_complete` text promoted to REQUIRED across 4 sections of quality-gate.md; generator mirror updated + cursor rules regenerated; export-cursor-rules.sh --check exit 0; audit-doc-drift exit 0; smoke-e2e exit 0 (manifest already emitted per ADR-0019); audit-manifest exit 0; ADR-0026 follows numbered template + names 5 rejected sub-expansions explicitly; no schema_version references modified (wire format stable). **DONE 2026-05-26.** |
| TICKET-020 | README operator-readiness refresh                     | Refresh `README.md` to reflect TICKETS 011-019 shipping (cross-IDE surface, swarm orchestration, provenance trilogy, researcher discipline, Source 9, pin bump, quality-gate v2). Remove pre-shipping language ("stubs filled in by individual tickets"; "Pending: TICKET 010"). Restructure into 4 operator-facing sections per the user's 2026-05-26 direction: (1) What it is + Why use it + How it works (data-flow diagram); (2) Setup — prerequisites + 5-step ready-to-use procedure (clone, sync-plugin --ensure, verify 5 audits, open IDE, smoke the operator journey against `examples/string-utils/`); (3) Daily operator workflow (7 slash commands); (4) Repo map + Status. No ADR (this is Q-DOC-DRIFT-class doc refresh per D-8; doesn't introduce new architectural decisions). | README has Setup section with explicit prerequisites + 5-step procedure; expected `--ensure` output documented; 5 verification commands listed with expected exit codes; Repo map covers AGENTS.md + CLAUDE.md + docs/ + .claude/ + .cursor/ + .grok/ + scripts/ + examples/ + .harness/; status mentions all 26 ADRs + all tickets DONE; full audit chain still exits 0 post-refresh; F-1..F-6 don't trigger on new README content. **DONE 2026-05-26.** |
| TICKET-021 | Wayfair / 2026 harness-engineering intel — filtered extension | Branch off main; consider 2026-05-26 architect briefing on Wayfair's AI efforts + 2026 industry harness-engineering trends (OpenAI Codex, Anthropic agent harnesses, Meta REA, Microsoft Azure SRE, Google Jetski, Stripe/Shopify/Airbnb cohort). Apply the over-engineering filter established by ADR-0025 + ADR-0026 with the strictest reading per the architect's standing "qualify and quantify over-engineering" directive. Evaluate 7 expansion candidates: Next.js demo target, `scripts/audit-metrics.sh`, initializer+incremental TIER-2 doc, cross-session observability surface, TIER-2 competitive-positioning doc, §1 Source 10 elevation, and this ADR itself. REJECT 6 of 7 (~86% cut) with rationale per ADR-0027 §Decision-1. ACCEPT the ADR (to persist the rejection rationale) + an AUTOMATION_INTEL.md append entry (capture T-D intel per ADR-0023 §5 cross-source bar; document metric-derivation one-liners closing C2's gap at zero substrate cost; document deferral triggers per candidate). Zero new substrate; zero new scripts; zero new TIER-2 docs; zero §1 amendments. | ADR-0027 follows numbered template; 6 rejections each cite specific filter criteria; AUTOMATION_INTEL.md gains 2026-05-26 Wayfair-intel entry tagged "T-D paraphrase"; metric-derivation one-liners verifiably runnable against existing `.harness/audit/*.manifest.json`; §1 Sources 1-9 byte-identical (D-6 immutability honored); no `.cursor/rules/` changes; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest all exit 0); 6 deferral triggers explicitly named for future re-triage. **DONE 2026-05-26.** |
| TICKET-022 | Persist QUICKSTART.md as operator entry point          | Ship `QUICKSTART.md` at repo root as the first-encounter operator entry point per the 2026-05-26 user direction ("persist this file in the repo in a location and way that provides for best user experience and in order that they encounter it first"). Content: 8 sections — what it is + reality check; prerequisites (60-sec verification); bootstrap (3-min, 5 verification commands); usage modes table (A inside-repo / B harness-as-template / C pattern-copy); first real cycle (15-min, `/research → /decompose → /dispatch → /inner-loop → /audit` against kebabCase example); 7 slash commands daily-workflow table; 7 common gotchas (PostToolUse Claude-Code-only, smoke trap, /inner-loop prereq, TICKETS append-only, §1 immutable, .harness gitignored, pin-bump ADR); next-steps pointers + 3-action minimum-to-start. Cross-reference from 4 entry points so operators encounter it first regardless of which surface they hit: README.md (prominent top-of-file pointer above "What it is"), AGENTS.md (preamble), CLAUDE.md (preamble), `.cursor/rules/agent-context.mdc` (regenerated via `scripts/export-cursor-rules.sh` `gen_agent_context` update). No ADR (this is operator-UX doc; doesn't introduce architectural decisions — per ADR-0027 §Decision-2 precedent for Q-DOC-DRIFT-class additions). | `test -f QUICKSTART.md`; 8 `## §N ` sections grep-detectable; README + AGENTS.md + CLAUDE.md + `.cursor/rules/agent-context.mdc` all grep-positive for "QUICKSTART"; export-cursor-rules `--check` exit 0 (generator + on-disk in sync); full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest all exit 0); 3-action minimum-to-start named with concrete commands + expected outputs. **DONE 2026-05-26.** |
| TICKET-023 | Substrate-script test discipline + 100% surface coverage | Per 2026-05-26 user directives: (a) "Bring it into A grade in 15 mins" responding to simulated Fowler+team critique (which graded test-discipline-practiced at D+); (b) "Also report code coverage by unit tests. It should be 100% unit test coverage"; (c) standing guardrail "No regressions or drift from architectural design." Ship `tests/test-*.sh` for all 8 substrate surfaces (6 scripts + 2 hooks): sync-plugin, audit-doc-drift, audit-manifest, emit-manifest (21 assertions; most contract surface), export-cursor-rules, smoke-e2e, post-tool-use-review-gate (4 forbidden patterns), session-start (warn-only exit-0 policy). Plus `tests/test-all.sh` aggregator + `tests/README.md` with honest coverage scope statement. Bash 3.2 + BSD portable; native assertions; no `bats`/`shellspec`/`pytest` dependency per D-8 + C-23. Restore-before-assert pattern for any state-mutating test. ~72 total assertions across 8 suites; 8/8 passing at commit. Land ADR-0028 (100% defined honestly as surface coverage NOT line/mutation/exhaustive-input; 8 alternatives REJECTED per filter — kcov tooling, mutation testing, CI integration, F-7 pattern, test framework, refactor for testability, property/fuzz testing, exhaustive input space). **No regressions** verified: full audit chain (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + sync-plugin --check) all exit 0 post-test-shipping. **No architectural drift** verified: §1 of founder-directives byte-identical; D-rule count unchanged; TIER-0 corpus untouched; AGENTS/CLAUDE/QUICKSTART untouched; wire-format schema_version unchanged. | `tests/test-all.sh` exits 0 with 8/8 suites passing; coverage report shows 8/8 substrate surfaces tested (= 100%); `bash -n` clean on all 8 test files; full audit chain green; `git diff docs/founder-directives.md` returns 0 lines; ADR-0028 follows numbered template + names 8 alternatives REJECTED per over-engineering filter + states 100% scope honestly. **DONE 2026-05-26.** |
| TICKET-024 | Fowler critiques #5/#6/#7/#8 closures (A− → A) | Per 2026-05-26 user directive "Bring it up to an A" responding to the regrade of A− post-TICKET-023. Close 4 of the 8 named Fowler critiques per the regrade's priority-ordered closure path; 3 + 1 already closed (#2 via TICKET-023) = 5/8 critiques closed = solid A grade. **Critique #5** (single-reviewer filter): establish `Second voice` field in ADR template; demonstrate by self-reference in ADR-0029 (simulated Fowler+team regrade IS the second voice). **Critique #6** (tamper-evident overstates): rename to "drift-detectable when paired with original-manifest preservation" across 4 operator-visible files (README, QUICKSTART, AUTOMATION_INTEL ×2); ADR-0027's historical occurrence preserved per Nygard append-only; ADR-0029 §Supersedes records the supersession. **Critique #7** (R-11 vs §5 tension): add `docs/researcher-discipline.md §10 Wire-vs-authority strictness layering` naming the coherent defense-in-depth pattern (TOLERANT data plane / STRICT control plane) with worked example (provenance manifest); §10 ↔ §11 swap preserves monotonic ordering. **Critique #8** (self-referential complexity): add explicit "what this solves vs. a CONTRIBUTING.md + a few scripts" paragraph to QUICKSTART §1 — names the multi-tool / multi-session / multi-agent / cross-IDE enforcement boundary as the value-prop; honestly acknowledges when a simpler approach suffices. Land ADR-0029 (5th over-engineering-filter application; 7 alternatives REJECTED — split-into-4-CLs, close-all-8-in-one-CL, edit-ADR-0027-in-place, F-7-audit-pattern, "tamper-resistant"-rename, new-TIER-2-doc, replace-vs-extend-QUICKSTART). Three critiques remain open with named effort estimates: #1 (dead-rulebook audit, 60-90 min); #3 (TIER-hierarchy refactor, 60+ min); #4 (swarm integration test, 30 min). **No regressions / no architectural drift** verified per the standing guardrail. | `grep -c 'tamper-evident' README.md QUICKSTART.md AUTOMATION_INTEL.md` returns 0 (4 operator-visible renames done; ADR-0027 historical preserved); `docs/researcher-discipline.md §10` exists with monotonic section numbering 1..11; QUICKSTART §1 carries the explicit boundary paragraph; ADR-0029 front-matter includes `Second voice` field demonstrating the pattern; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + sync-plugin --check); `tests/test-all.sh` 8/8 suites passing; `git diff docs/founder-directives.md` returns 0 lines; ADR-0029 follows numbered template + names 7 alternatives REJECTED per filter. **DONE 2026-05-26.** |
| TICKET-025 | Swarm collection-contract integration test (closes Fowler #4) | Per 2026-05-26 user directive "Proceed through three focused, triggered, filter-disciplined CLs to secure A+" — first of three CLs per Kua's ordering (#4 first per smallest-effort-removes-visible-footnote rationale). Ship `tests/test-orchestrating-swarms.sh` exercising the COLLECTION CONTRACT of Step 5 of `.claude/skills/orchestrating-swarms/SKILL.md`: synthesize 3 workers (TICKET-SWARM-001..003) with non-overlapping `file_scope` per G-8 using inline-heredoc fixtures; emit a manifest per worker via `emit-manifest.sh`; validate all 3 via `audit-manifest.sh`; verify `--regenerate` drift detection per worker; assert cross-contamination absence (tamper worker 2; assert workers 1 and 3 still clean). 19 assertions across 6 contract sub-steps (5a–5f); runs in ~3 seconds; restore-before-assert via `trap cleanup EXIT INT TERM`. Worktree spawn (Step 4) remains operator-validated per ADR-0030 §Decision-1 honest scope (git owns worktree mechanism; Claude Code Task tool owns sub-agent spawn; testing those would re-implement their own coverage). Update `.claude/skills/orchestrating-swarms/SKILL.md` Step 5 with "contract is tested" paragraph citing TICKET-025 / ADR-0030. Update `tests/README.md` coverage table to 9/9 testable surfaces (~91 assertions). Land ADR-0030 (closes Fowler #4 for collection contract specifically; supersedes ADR-0017 §Verification line 126 "operator-attested" framing for the collection contract; 7 alternatives REJECTED — real worktree spawn, single worker, 8 workers, worktree-spawn-in-scope, externalized fixtures, bats/shellspec, defer-until-bitten). Uses ADR-0029's `Second voice` field pattern (regrade IS the second voice). | `bash -n tests/test-orchestrating-swarms.sh` clean; 19/19 assertions passing; `tests/test-all.sh --quiet` shows 9/9 suites passing; full audit chain green; SKILL.md updated; tests/README.md coverage table extended; ADR-0030 follows numbered template + `Second voice` field present + names 7 alternatives REJECTED. **DONE 2026-05-26.** |
| TICKET-026 | Rulebook coverage audit (closes Fowler #1) | Per 2026-05-26 user directive "Proceed through three focused, triggered, filter-disciplined CLs to secure A+" — second of three CLs per Kua's ordering (#1 second per "biggest grade lift but longest effort"). Ship `scripts/audit-rulebook-coverage.sh` (bash 3.2 + BSD portable; --detail / --quiet flags; ~100 lines; deterministic per commit) + `tests/test-audit-rulebook-coverage.sh` (12 assertions). Generate `docs/rulebook-coverage-audit.md` TIER-2 report (§1..§8) capturing 2026-05-26 state: **40 of 78 rules (51%) have zero external citations** — Fowler's critique numerically validated. Breakdown: D-rules 1/13 dead (D-4); R-rules 12/20 dead (mostly microservices-pattern rules — R-4 R-6..10 R-13..18); G-rules 7/21 dead (G-2 G-3 G-6 G-11 G-14 G-17 G-20); C-rules 20/24 dead (delegated to upstream plugin per CLAUDE.md prime directive). Audit-only scope; actual deletion DEFERRED to subsequent CLs per D-8 (sequencing: C-rule consolidation → R-rule cohort → G-rule rationalization → D-rule retention; each with its own ADR per architecture-principles §19). Add the report to AGENTS.md §5 + cursor-rules generator; regenerate. Land ADR-0031 (audit-only closure; substrate-script-test discipline followed; 7 alternatives REJECTED — mass deletion, defer-audit-entirely, inline-findings-in-ADR, sophisticated-categorization, lower-threshold, skip-test, skip-AGENTS-enumeration; --update mode for automated deletion EXPLICITLY REJECTED per D-12). Uses ADR-0029's `Second voice` field pattern (regrade IS the second voice; second application after ADR-0030). | `bash -n scripts/audit-rulebook-coverage.sh` clean; `bash -n tests/test-audit-rulebook-coverage.sh` clean; audit script exits 0 on default/--detail/--quiet; test suite 12/12 passing; `tests/test-all.sh --quiet` shows 10/10 suites passing; `docs/rulebook-coverage-audit.md` exists with §1..§8; AGENTS.md §5 + cursor-rule regenerated; full audit chain green; `git diff docs/founder-directives.md` returns 0 lines; ADR-0031 follows numbered template + `Second voice` field present. **DONE 2026-05-26.** |
| TICKET-027 | Cross-reference audit (closes Fowler #3) | Per 2026-05-26 user directive "Proceed through three focused, triggered, filter-disciplined CLs to secure A+" — third of three CLs per Kua's ordering (#3 last per "most invasive; defer unless concrete trigger fires"). Closes Fowler #3 (TIER hierarchy strong coupling between docs) via the LIGHTER-WEIGHT regrade-named option: ship a tool that walks the cross-reference graph + catches broken links. The full canonical-authority refactor is DEFERRED per ADR-0032 §Decision-1 (no operationally-bitten trigger). Ship `scripts/audit-cross-references.sh` walking TIER-1/2 markdown + AGENTS/CLAUDE/QUICKSTART/README/TICKETS/AUTOMATION_INTEL/orchestrating-swarms-SKILL/tests/README; extracts `[label](path)` markdown links + `` `path.ext` `` inline-code paths matching known directory prefixes; filters template-placeholders (`<...>`, `{...}`, `...`, `TICKET-NNN`, `TICKET-DEMO`) + `.harness/` runtime artifacts. Approval-testing baseline pattern (`tests/cross-references-baseline.txt`): 24 legacy known-broken refs captured (mostly ADR §Alternatives entries naming REJECTED items per Nygard append-only; plus future-dated placeholder filenames); audit exits 0 when findings match baseline, 1 only when NEW refs break (regression). Plus `tests/test-audit-cross-references.sh` (8 assertions; injects new broken ref to verify exit-1 path; restore-before-assert). Land ADR-0032 (audit-tool option chosen over full refactor; approval-baseline pattern documented; 8 alternatives REJECTED — full refactor, pure-detect mode, auto-fix mode, skip-test, aggressive false-positive filter, directed-graph analysis, --update-baseline mode, baseline-shrinkage-in-this-CL). Uses ADR-0029's `Second voice` field pattern (regrade IS the second voice; third application). | `bash -n scripts/audit-cross-references.sh` clean; `bash -n tests/test-audit-cross-references.sh` clean; audit exits 0 on baseline-matched state; test suite 8/8 passing including injected-new-broken-ref test; `tests/test-all.sh --quiet` shows 11/11 suites passing; `tests/cross-references-baseline.txt` has 24 entries; full audit chain green; `git diff docs/founder-directives.md` returns 0 lines; ADR-0032 follows numbered template + `Second voice` field present. **DONE 2026-05-26.** |
| TICKET-035 | Static context injection SUPERSEDES architecture-consult (closes ADR-0039 via ADR-0040) | Per 2026-06-06 adversarial review of TICKET-034: *"Right diagnosis, wrong prescription. Static context injection at session start solves the same problem with no new orchestration, no new schema, no round-trip cost, no coupling increase."* The reviewer named four critiques of the dynamic consult: framework-itis, static-vs-dynamic data mismatch, coupling cost, unfalsifiable success criterion. The plugin upstream confirmed the simpler approach by publishing `docs/PROJECT_CONTEXT_FOR_PLANNER.md` + ADR-0006 at `b3e17c0`. Ships harness side: `scripts/sync-plugin.sh --ensure` extended to defensively copy `PROJECT_CONTEXT_FOR_PLANNER.md` from the pinned plugin cache into `.harness/context/PROJECT_CONTEXT_FOR_PLANNER.md` (no-op when source absent at current pin; operator-visible deferral log line). `.gitignore` excludes `.harness/context/`. SUPERSEDED markers added at top of `.grok/templates/architecture-consult.md`, `docs/handoff-contract.md §Architecture-Consult`, and the `architecture_consult` input variable in `.grok/templates/decomposition.md` per Nygard append-only convention. AGENTS.md §6 + cursor rules generator updated. Land ADR-0040 (10th application of `Second voice` field — adversarial reviewer IS the second voice; quotes reviewer's verdict verbatim; 7 alternatives REJECTED: keep both mechanisms, hard-delete, embed in CLAUDE.md, vendor, wait for pin bump, combine with pin bump, A/B test). Cross-repo ADR pairing: plugin ADR-0006 publishes the contract; harness ADR-0040 consumes it. Activation requires pin bump (separate follow-on CL with its own ADR per architecture-principles §15, precedent ADR-0025) — current pin `23e5c2b` predates `PROJECT_CONTEXT_FOR_PLANNER.md`. | `scripts/sync-plugin.sh --ensure` emits `context : PROJECT_CONTEXT_FOR_PLANNER.md not present at this pin (defer to pin bump per ADR-0040)` (defensive wire verified); `tests/test-all.sh --quiet` 18/18 passing; full audit chain green (10 audits + smoke-e2e); `git diff docs/founder-directives.md` returns 0 lines (D-6 honored); ADR-0040 follows numbered template + `Second voice` field present (10th application); SUPERSEDED markers visible at top of all 3 deprecated artifacts. **DONE 2026-06-06.** |
| TICKET-034 | Architecture-consult phase: Grok → Claude-TDD-Pro before decomposition (SUPERSEDED by TICKET-035 / ADR-0040 — see ADR-0040 §Context for the reviewer's four critiques) | Per 2026-06-06 operator directive *"when the planning is being done, it should consider how Claude TDD Pro is going to plan the architecture. ... how can you size something if you don't know the actual technical approach?"* Verified by greppping `.grok/templates/decomposition.md`: 0 references to consult/technical-approach/inner-loop. Grok was decomposing blind. Grok itself reviewed the proposal and ratified ship with 4 refinements (caching, operator toggle, advisory-not-gospel, schema discipline). Ships: `.grok/templates/architecture-consult.md` (NEW; 6 questions: test-shape, decomposition, sequencing, scope, applicable_rules, complexity; cache key = sha256(research_bundle + brief); operator toggle `consult_toggle: off` for trivial tickets; operator-visible only on cache miss); `docs/handoff-contract.md §Architecture-Consult` schema with `recommended_tickets`, `prior_decisions`, `cache_key`, skip semantics; `.grok/templates/decomposition.md` updated to REQUIRE `architecture_consult` input variable + pre-emit checks (applicable_rules must resolve in `active.json`; `prior_decisions[].kind == "delete"` forbids scope; `kind == "adr_required"` enforces ADR-landing in acceptance criteria); `.gitignore` excludes `.harness/cache/` (architecture-consult cache; invalidated by plugin pin bumps). Land ADR-0039 (9th application of `Second voice` field — Grok IS the second voice this time; 7 alternatives REJECTED: bake into decomposition, treat as gospel, skip caching, no toggle, run async with compare, lightweight applicable_rules-only consult, skip evals at v1). End-to-end planning flow becomes: research → architecture-consult (NEW) → decomposition (consumes consult) → dispatch (populates applicable_rules from consult) → Claude R-G-R. Decomposition is no longer blind; tickets get sized correctly the first time. | `.grok/templates/architecture-consult.md` exists with 6 questions + schema + cache contract + pre-emit + failure modes; `docs/handoff-contract.md §Architecture-Consult` section present with full schema; `.grok/templates/decomposition.md` 3rd input variable + pre-emit checks naming consult's `prior_decisions`; `.gitignore` excludes `.harness/cache/`; `tests/test-all.sh --quiet` 18/18 passing; full audit chain green (10 audits + smoke-e2e + test-all); `git diff docs/founder-directives.md` returns 0 lines (D-6 honored); ADR-0039 follows numbered template + `Second voice` field present (9th application; Grok IS the second voice). **DONE 2026-06-06.** |
| TICKET-033 | Full plugin-feature wire: peer reviews + formatters + compliance/AIBOM (closes the named partial-answer gap) | Per 2026-06-06 operator directive *"Is it working this way now between the two plugins?"* — the honest answer after TICKET-032 was *partial*. Three plugin surfaces were rationale-only entries in `docs/plugin-surface-consumption.md` claiming CONSUMED with no real wire: `pr-corpus/`, `formatters/`, `compliance/`. This CL replaces forward-claim with real wires across three batches. **Batch 6 — Peer review surface:** `.claude/hooks/post-tool-use-review-gate.sh` extended to surface up to 3 DEFERRED findings per touched file to stderr with a `[peer-review]` prefix; DEFERRED findings are the rubric's PR-pattern-derived rules requiring agent review (`g-eng-001-design-belongs-here`, `g-eng-002-yagni`, `g-eng-006-no-bundled-refactor-and-feature`, etc.) synthesized from `pr-corpus/` PR-extracted patterns. Non-blocking — agent must address in response trail before ticket closes. **Batch 7 — Formatter auto-apply:** PostToolUse hook invokes `formatters/cli.sh --file <REL_PATH> --apply` after rubric P0 check passes for app-code extensions. Defensive; no-ops when no project formatter config exists. **Batch 8 — AIBOM emit:** `scripts/smoke-e2e.sh` invokes `compliance/aibom-emit.sh` after manifest write; every green ticket produces `.harness/audit/TICKET-NNN.aibom.json` as a fourth audit artifact alongside manifest + decision trail + req/res. Update `docs/plugin-surface-consumption.md` rows for the three surfaces to name the real wires. Land ADR-0038 (8th application of `Second voice` field — operator's binary question IS the second voice; 5 alternatives REJECTED: block on DEFERRED findings, run plugin's evals from harness, vendor a separate peer-review pattern set, make AIBOM a smoke gate, defer formatters). | `bash -n .claude/hooks/post-tool-use-review-gate.sh` clean; `bash -n scripts/smoke-e2e.sh` clean; smoke-e2e emits `aibom: .harness/audit/TICKET-042.aibom.json` line; AIBOM artifact created on disk; `tests/test-post-tool-use-review-gate.sh` 8/8 passing (file-fence preserved); `tests/test-hook-contracts.sh` 15/15 passing (payload contract preserved); `tests/test-all.sh --quiet` 18/18 passing; full audit chain green; `docs/plugin-surface-consumption.md` rows for pr-corpus + formatters + compliance name real wires (no "planned Batch" language); `git diff docs/founder-directives.md` returns 0 lines (D-6 honored); ADR-0038 follows numbered template + `Second voice` field present (8th application). **DONE 2026-06-06.** |
| TICKET-032 | Standards pipeline consumption: wire plugin's rubric into the harness (SHIPPED in 5 batches of 15 min) | Per 2026-06-06 operator directive *"when I provide input to the harness of what I want built ... I need it built by the inner-loop according to the engineering rules I've already built (Google, owasp, federal government, etc) ... If it's not working this way now between the two plugins then describe how to make it work. And make it happen now in batches of 15 mins of work."* Closes the structural gap discovered this session: the harness was treating the plugin as a 3-SKILL surface while the plugin had grown to 39 top-level surfaces including the full standards / rubric / generated-code-quality-standards pipeline. Five batches: (1) `scripts/audit-plugin-surface.sh` + `docs/plugin-surface-consumption.md` (11 CONSUMED + 28 DECLARED-NOT-CONSUMED) + `tests/test-audit-plugin-surface.sh` (9 assertions); (2) `scripts/standards-sync.sh` invoking `rubric/aggregator.sh` → `.harness/rules/active.json` (28 rules from 9 namespaces — google, node, owasp, react, slsa, typescript, w3c, web-vitals, _community) + `tests/test-standards-sync.sh` (16 assertions); SessionStart hook calls standards-sync + plugin-surface audit; (3) `docs/handoff-contract.md` extended with `applicable_rules` (req) + `rules_verified` (res); `docs/quality-gate.md §lint_clean` updated to consume rubric output; `CLAUDE.md` new TIER-1 section "Operator-declared standards"; `AGENTS.md §7` names standards-sync as second session-start action; (4) `.claude/hooks/post-tool-use-review-gate.sh` extended to run `rubric/runner.sh --diff --severity P0` after Edit/Write/MultiEdit on app-code extensions (js/jsx/ts/tsx/py/go/rs/java/kt/swift); exit 2 on P0 finding for the touched file; (5) `docs/deviations.md` append-only registry (mirrors §1 immutability pattern) + `scripts/audit-standards-conformance.sh` (pre-commit; approval-baseline pattern matching ADR-0032 / 0034) + `tests/test-audit-standards-conformance.sh` (8 assertions). Land ADR-0037 (7th application of `Second voice` field — operator directive IS the second voice; 6 alternatives REJECTED: vendor the rubric, wait for federal-gov sources, block on all severities, run on bash/markdown substrate, skip deviations registry, defer plugin-surface audit). Substrate coverage 15/15 → 18/18 surfaces. | All 5 batches landed; `scripts/audit-plugin-surface.sh` exits 0 (11 CONSUMED + 28 DECLARED + 0 UNKNOWN); `scripts/standards-sync.sh` writes `.harness/rules/active.json` with 28 rules from 9 namespaces; `scripts/audit-standards-conformance.sh` exits 0 on harness substrate; `tests/test-all.sh --quiet` shows 18/18 suites passing; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + audit-cross-references + audit-hook-security + audit-metrics + audit-claude-code-compat + audit-plugin-surface + audit-standards-conformance all exit 0); SessionStart hook emits `[standards-sync] wrote .harness/rules/active.json` + `[plugin-surface] OK`; `git diff docs/founder-directives.md` returns 0 lines (D-6 honored); ADR-0037 follows numbered template + `Second voice` field present (7th application). **DONE 2026-06-06.** |
| TICKET-031 | Claude Code upgrade strategy: pin range + compat audit + hook-contract tests + runbook (SHIPPED) | Per 2026-05-26 user directive "Bring it in line with best practices per the matter" — override of the prior deferral analysis in favor of formal best practices. Mirrors the plugin-pin discipline (ADR-0001, ADR-0025) for the host CLI itself. Ships: `docs/claude-code-compat.yaml` (declared `supported_range` `>=2.0.0,<3.0.0` + `tested_versions` ledger including 2.1.163 captured 2026-05-26 green + `first_known_incompatible: null`); `scripts/audit-claude-code-compat.sh` (~90 lines; bash 3.2 + BSD portable; reads `claude --version`, semver-compares against declared range; `--version <semver>` override flag for testing; exit 0 in-range / 1 out-of-range / 2 error); extension to `.claude/hooks/session-start.sh` fires the compat audit after plugin sync; WARN-not-FAIL stance mirrors ADR-0001. Ships `tests/test-audit-claude-code-compat.sh` (9 assertions: boundary-min inclusive + boundary-max exclusive + below-min + above-max + output-string asserts). Ships `tests/fixtures/hook-payloads/` (4 golden JSON fixtures pinning the Claude Code hook payload contract: allowed-Edit, allowed-Write, forbidden-`.cursor/rules/agent-context.mdc`-edit per ADR-0014, non-edit Read no-op) + README.md documenting the re-capture procedure. Ships `tests/test-hook-contracts.sh` (15 assertions: fixture presence + JSON validity per node + 4 hook scenarios + defensive missing-`tool_name` test). Ships `docs/claude-code-upgrade-runbook.md` (operator procedure; 8 sections: pre-upgrade checklist → upgrade → verification chain → decision tree green/red → rollback → post-upgrade smoke → AUTOMATION_INTEL recording → named anti-patterns). Land ADR-0036 (7th application of `Second voice` field — the user override IS the second voice; 7 alternatives REJECTED: defer per filter, FAIL-not-WARN, auto-bump, pin specific version, use jq, capture all-events fixtures at v1, embed in sync-plugin.sh). Substrate test coverage 13/13 → 15/15 surfaces. | `docs/claude-code-compat.yaml` exists with `supported_range` + `tested_versions`; `scripts/audit-claude-code-compat.sh` executable + exits 0 against current CLI; `tests/test-audit-claude-code-compat.sh` 9/9 passing; `tests/fixtures/hook-payloads/*.json` (4 files + README) present; `tests/test-hook-contracts.sh` 15/15 passing; SessionStart hook emits `[claude-code-compat] OK` line; `tests/test-all.sh --quiet` 15/15 PASS; `docs/claude-code-upgrade-strategy.md` rewritten SHIPPED + listed in AGENTS.md §5; `docs/claude-code-upgrade-runbook.md` exists + listed in AGENTS.md §5; cursor-rule regenerated; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + audit-cross-references + audit-hook-security + audit-metrics + audit-claude-code-compat all exit 0); `git diff docs/founder-directives.md` returns 0 lines (D-6 honored); ADR-0036 follows numbered template + `Second voice` field present (7th application). **DONE 2026-05-26.** |
| TICKET-030 | DORA metrics from manifest corpus (closes Musk #3; lifts grade B → A) | Per 2026-05-26 user directive "Bring it up to an A in less than 30 mins" responding to this session's regrade of TICKET-029 at B which named the #3 metrics deferral as the primary block. Per ADR-0027 §Decision-1 C2 deferral trigger ("operator reports the one-liners insufficient") AND ADR-0034 §Out-of-scope #3 deferral trigger ("operator reports C-24's principle-only DORA framing insufficient") — both fired in this session's regrade. Ship `scripts/audit-metrics.sh` (~140 lines; bash 3.2 + BSD portable; 3 modes: human / `--json` / `--quiet`; `--dir=<path>` flag for testability) reading the existing `.harness/audit/*.manifest.json` corpus + `git log --grep="$ticket_id" --format="%cI"` to compute 3 of the 4 DORA Four Keys (Forsgren/Humble/Kim *Accelerate*): Deployment frequency (green manifests / week), Change failure rate ((red+blocked)/total × 100%), Lead time (median seconds first-commit-mentioning-ticket → manifest created_at). Time-to-restore reported as `n/a` at v1 with v2 trigger named (first restore-event manifest). No fabrication: every number traces to a real manifest field or git log query. Ship `tests/test-audit-metrics.sh` (17 assertions including synthetic 5-ticket fixture: 3 green + 1 red + 1 blocked → 40% CFR verified; restore-before-assert; `--dir` flag enables fixture isolation). Ship `docs/dora-metrics.md` TIER-2 doc (8 sections: Four Keys mapping, operator procedure, honest caveats per *Accelerate*, pre-commit policy, test discipline, authority + amendment, composition, verification). Update AGENTS.md §5 + scripts/export-cursor-rules.sh + regenerate .cursor/rules/agent-context.mdc. Update tests/README.md (12/12 → 13/13). Append 2026-05-26 entry to AUTOMATION_INTEL.md recording #3 closure with same-session-trigger evidence. Land ADR-0035 (6th application of `Second voice` field — regrade letter IS the second voice; 6 alternatives REJECTED — ship --update/threshold-alert at v1, use jq, defer until N≥30, compute lead time from spec-write timestamp, skip JSON mode, wire into pre-commit at v1). Closes ADR-0027 §Decision-1 C2 + ADR-0034 §Out-of-scope #3. | `bash -n scripts/audit-metrics.sh` clean; `bash -n tests/test-audit-metrics.sh` clean; audit script exits 0 + emits documented fields; `--json` mode emits single-line JSON with 9 fields; test suite 17/17 passing; `tests/test-all.sh --quiet` shows 13/13 suites passing; `docs/dora-metrics.md` exists with 8 sections + listed in AGENTS.md §5; cursor-rule regenerated; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + audit-cross-references + audit-hook-security + audit-metrics all exit 0); `git diff docs/founder-directives.md` returns 0 lines (D-6 honored); ADR-0035 follows numbered template + `Second voice` field present (6th application). **DONE 2026-05-26.** |
| TICKET-029 | Musk Engineering Leadership letter closure (closes #4 partially + #5 fully; defers #1 #2 #3) | Per 2026-05-26 user directive "Address and incorporate the feedback from my previous prompt in 30 mins or less" responding to the formal Musk Engineering Leadership review letter (xAI / Tesla / SpaceX lens) with 5 "Do These Now" prescriptions: #1 Benchmark Velocity, #2 Dogfood on Real Project, #3 Add Metrics, #4 Security Review, #5 Simplify Onboarding. Per the over-engineering filter: ship #4 at v1 (composes on `audit-cross-references.sh` approval-baseline pattern from ADR-0032) + #5 fully (one-screen QUICKSTART §0); defer #1, #2, #3 with named triggers (synthetic data would be fabrication). Ship `scripts/audit-hook-security.sh` (~120 lines; bash 3.2 + BSD portable; six pattern classes S-1..S-6 mapped to canonical CWE / OWASP attack vectors: S-1 eval injection, S-2 curl\|bash, S-3 rm -rf, S-4a-e hardcoded credentials, S-5 sudo, S-6 bash -c with unquoted var) + `tests/hook-security-baseline.txt` (15 entries; all S-3 bounded `rm -rf` cleanup; zero findings in S-1, S-2, S-4, S-5, S-6) + `tests/test-audit-hook-security.sh` (9 assertions including injected-eval test + injected-_TOKEN test; restore-before-assert pattern). Ship `docs/security-review.md` TIER-2 doc (9 sections: threat model, scan policy, baseline rationale, operator procedure, test discipline, pre-commit integration policy, named deferrals, authority + amendment, verification). Ship QUICKSTART §0 "Fastest path — your first green ticket in <2 minutes" (3-line clone+sync+smoke path; full §1+ depth path preserved). Update AGENTS.md §5 + scripts/export-cursor-rules.sh + regenerate .cursor/rules/agent-context.mdc to include the new TIER-2 doc. Update tests/test-all.sh to register the new test (11/11 → 12/12 surfaces). Update tests/README.md coverage table. Append 2026-05-26 entry to AUTOMATION_INTEL.md recording letter receipt + named-trigger deferrals for #1, #2, #3. Land ADR-0034 (5th application of `Second voice` field — Musk Engineering Leadership letter is the second voice; 5 alternatives REJECTED — ship all 5 in one CL, defer #4 too, wire into pre-commit at v1, ship cryptographic signing at v1, restructure QUICKSTART entirely). | `bash -n scripts/audit-hook-security.sh` clean; `bash -n tests/test-audit-hook-security.sh` clean; audit script exits 0 on baseline-matched state; test suite 9/9 passing; `tests/test-all.sh --quiet` shows 12/12 suites passing; `docs/security-review.md` exists with 9 sections + listed in AGENTS.md §5; cursor-rule regenerated; QUICKSTART §0 present + grep-discoverable; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + audit-cross-references + audit-hook-security all exit 0); `git diff docs/founder-directives.md` returns 0 lines (D-6 honored); ADR-0034 follows numbered template + `Second voice` field present (5th application). **DONE 2026-05-26.** |
| TICKET-028 | C-rule consolidation (closes Musk #1; lifts Musk grade B+ → A) | Per 2026-05-26 user directive "Address Musk's feedback and bring his rating up to an A" responding to the 4-team panel review (Musk B+ on the grounds that "you measured 40 dead rules and deleted zero; C-rules are 83% dead — delete them; consolidate; ship"). Per `docs/rulebook-coverage-audit.md §4` proposed-sequencing item 1 (architect-signaled trigger now fired). Delete 20 dead C-rules (C-2 through C-21) from `docs/claude-tdd-pro-principles.md §16` table; consolidate to upstream plugin per the CLAUDE.md prime directive (the harness consumes via SKILL.md trio symlinks from the pinned plugin cache; canonical rule bodies live in `claude-tdd-pro/.claude/skills/tdd-pro-cl-workflow/SKILL.md`). Retain 4 harness-side C-rules: C-1 (TDD non-negotiable; meta-rule), C-22 (substrate batching), C-23 (bash 3.2 portability — most-cited C-rule across substrate scripts), C-24 (DORA scoreboard). Retain intellectual provenance §§1-15 of the rulebook (Beck, Bob's Three Laws, Fowler, Feathers, Cohn, London/Chicago, mutation/property, Anthropic, DORA) untouched. Update 5 operator-visible places for the new C-rule range: CLAUDE.md framing paragraph; AGENTS.md §5 TIER-2 enumeration; docs/ai-engineering-corpus.md authority table; docs/founder-directives.md §5 authority table (§1 + §3 untouched per D-6); scripts/export-cursor-rules.sh gen_agent_context TIER-2 list + regenerate .cursor/rules/agent-context.mdc. Update scripts/audit-rulebook-coverage.sh to iterate only active C-rules (1, 22, 23, 24) — the `audit_family` function refactored to take an explicit rule-number list instead of a contiguous range. Append §9 supersession log entry to `docs/rulebook-coverage-audit.md` with before/after summary (rule count 78 → 58; C-rule footprint 24 → 4; zero dead rules across all 4 families post-CL). Land ADR-0033 (Musk #1 closure; 5 alternatives REJECTED — batch with R-rule cohort retire, archive-rule-bodies-in-appendix, delete-rulebook-file-entirely, strikethrough-instead-of-delete, mass-delete-without-ADR). Uses ADR-0029's `Second voice` field pattern (Musk-team regrade IS the second voice; 4th application). | `grep -c "^| C-" docs/claude-tdd-pro-principles.md` returns 5 (header row + 4 active rules + 1 CONSOLIDATED row; was 25 = header + 24 rules); audit shows "C-rules: 4 active" with "0 zero-citation candidates"; tests/test-audit-rulebook-coverage 12/12 passing; tests/test-all.sh 11/11 passing; full audit chain green (audit-doc-drift + smoke-e2e + export-cursor-rules --check + audit-manifest + audit-cross-references all exit 0); §1 + §3 + §4 of founder-directives byte-identical; rulebook-coverage-audit §9 entry present with before/after table; ADR-0033 follows numbered template + `Second voice` field present (4th application). **DONE 2026-05-26.** |
| TICKET-001.e | Plugin lock file + session-start sync           | Operational mechanism for the plugin-dependency model: `docs/claude-tdd-pro.lock.yaml` + `scripts/sync-plugin.sh` + `.claude/hooks/session-start.sh` + `.claude/settings.json` + `docs/plugin-sync.md` + ADR-0001. Closes R-2/R-5 enforcement gap. | Lock file pinned to upstream HEAD; `sync-plugin.sh --check` exits 0 on a fresh clone; SessionStart hook prints the drift report; ADR-0001 recorded. **DONE 2026-05-25.** |
| TICKET-001.f | Persist founder directives as TIER-1 rulebook    | Persist the 2026-05-24 X posts (@teslayoda + @elonmusk) verbatim in `docs/founder-directives.md` with immutable §1 provenance; derive seven numbered directives (D-1..D-7) for grok-side enterprise-pattern learning, terminal-state loops, progressively harder CLs, production-grade problem instances, and provenance immutability; wire as TIER-1 authority co-equal with the CLAUDE.md prime directive. | `docs/founder-directives.md` exists with §1 verbatim and §3 D-rules; `CLAUDE.md` references it under "Authoritative founder-directives rulebook" before the R-/G-/C-rulebook sections; amendment process points at ADR. **DONE 2026-05-25.** |
| TICKET-001.g | Expand founder-directives from public xAI/Anthropic/Musk-orbit sources | Land ADR-0002. Add five §1 sources (Musk's Algorithm; xAI Grok Build launch; Schluntz/Zhang Building Effective Agents; Amodei Machines of Loving Grace; Karpathy agentic-engineering shift). Introduce a verification-tier model (T-A direct primary / T-B screenshot / T-C search-engine-indexed / T-D substantive paraphrase) since `WebFetch` was blocked across all primary URLs. Derive five new D-rules (D-8 deletion discipline; D-9 simple-composable-patterns; D-10 TDD + supervision; D-11 design FOR agent-CLI primitives; D-12 production-grade trustability is the harness's value). Broaden the rulebook's scope statement to cover Anthropic leadership. | `docs/founder-directives.md` §1 has Sources 3-7 with explicit Verification tier; §3 has D-1..D-12; §4 checklist covers all 12; `CLAUDE.md` reflects the new count; ADR-0002 records the decision and verification-tier model. **DONE 2026-05-25.** |
| TICKET-001.h | Persist Anthropic "Best practices for Claude Code" as Source 8 (first T-A) | Land ADR-0003. Append Source 8 to §1 of `docs/founder-directives.md` with full verbatim article text (heading-demotion-by-two as the only transcription transformation, documented in the entry metadata). Derive D-13 (context-as-fundamental-constraint + five named failure patterns). Update §3 header to D-1..D-13, extend §4 checklist with D-13 item, update CLAUDE.md count and source summary. Establish the canonical T-A capture pattern for the forthcoming "I am getting the others" series of primary-text additions. | Source 8 present in §1 at tier T-A; D-13 present in §3; CLAUDE.md updated; ADR-0003 records the decision and canonicalizes the T-A transcription discipline; `grep founder-directives CLAUDE.md` still ≥4. **DONE 2026-05-25.** |
| TICKET-001.i | Adopt AI engineering corpus as third TIER-1 rulebook | Land ADR-0004. Persist drumfiend21's compiled "AI Software Development Best Practices Corpus" verbatim in new sibling file `docs/ai-engineering-corpus.md` with framing (authority tier, provenance chain cross-referencing founder-directives §1 Sources 3/5/6/7/8, amendment process, pre-commit corpus checklist). Elevate to TIER-1 sibling to the prime directive and founder-directives. Add "Authoritative AI engineering corpus rulebook" section to CLAUDE.md (between founder-directives and architectural rulebook sections). Update founder-directives §5 authority-tier table to list three TIER-1 rulebooks and generalize the conflict-resolution rule. Derive NO new D-rules (corpus operates at procedural-playbook level, not directive level — adding D-rules would conflate the categories). | `docs/ai-engineering-corpus.md` exists with verbatim body + framing; CLAUDE.md references it under a new TIER-1 section; founder-directives §5 table lists three TIER-1 entries; ADR-0004 records the decision and the alternatives considered. **DONE 2026-05-25.** |
| TICKET-001.j | Elevate AI engineering corpus to TIER 0 (supreme operating directive) | Land ADR-0005. Per drumfiend21's explicit instruction (2026-05-25), make `docs/ai-engineering-corpus.md` the highest-priority ruleset and instruction for architecture, planning, and development. Introduce TIER 0 as a new top tier in the authority hierarchy: corpus = TIER 0; prime directive + founder-directives = TIER 1 beneath; R-/G-/C-rulebooks = TIER 2. Restructure CLAUDE.md so the corpus section sits at the top under "Supreme operating directive (TIER 0)" framing, before the prime directive. Update conflict-resolution rule: corpus wins over everything; only legitimate override is an ADR-landed amendment to the corpus itself. Update founder-directives §5 tier table and ai-engineering-corpus.md authority statement to reflect TIER 0. No D-rule edits. | CLAUDE.md TIER 0 section is the first authority section, before prime directive; tier table in founder-directives §5 shows TIER 0/1/1/2/2/2; ai-engineering-corpus.md authority section declares TIER 0 and references ADR-0005; `grep founder-directives CLAUDE.md` still ≥4. **DONE 2026-05-25.** |
| TICKET-006.a | Drift correction from architectural audit             | Realign repo contents with the design substrate after an architectural drift audit found 6 P0/P1 items: (F-1) README.md §Status claimed "Design-only. No code yet." despite TICKETS 001–006 done; (F-2) README.md said "Wiring is defined in TICKET-004" in future tense; (F-3) `scripts/sync-plugin.sh --help` omitted the `--ensure` mode added by ADR-0007; (F-4) `docs/plugin-sync.md` lines 19–20 omitted `--ensure` and described the SessionStart hook as calling only `--check`; (F-5) `docs/skill-consumption.md` was an orphan stub "filled in by TICKET-004" though TICKET-004 had shipped; (F-7) `docs/architecture.md` §"Handoff contract" duplicated a stale partial schema in violation of R-3 and used future tense for TICKET-002. The agent's one finding (TICKET-001.f scope "D-1..D-7") was a false positive — accurate historical-ledger entry. No new architectural decisions; no new ADR. | All 6 drift findings closed; `bash -n` clean on edited scripts; smoke still exits 0; README.md no longer claims "Design-only"; `sync-plugin.sh --help` lists `--ensure`. **DONE 2026-05-25.** |
| TICKET-006.b | Doc-drift prevention mechanism                        | Ensure the TICKET-006.a class of drift cannot recur silently. Three-layer fix per ADR-0009: (1) **Mechanical** — new `scripts/audit-doc-drift.sh` catching the four patterns observed in the audit: F-1 stale stubs, F-2 stale README framing, F-3 future-tense for done tickets, F-4 sync-plugin.sh mode parity (impl vs --help). Exit 0 clean, 1 with findings. (2) **Procedural** — new checklist item `Q-DOC-DRIFT` in `docs/founder-directives.md §4` under joint D-12 + D-13 authority requiring the audit (or equivalent inspection) before every commit. (3) **Structural** — ADR-0009 captures the lesson + alternatives. Not wired to SessionStart hook (would bloat startup; doc-drift is a pre-commit concern). | `scripts/audit-doc-drift.sh` exits 0 on the post-TICKET-006.a clean state; reintroducing any of F-1/F-2/F-4 produces exit 1 + a labeled finding; `docs/founder-directives.md §4` lists Q-DOC-DRIFT; ADR-0009 lands. **DONE 2026-05-25.** |
| TICKET-011 | AGENTS.md as cross-tool agent-binding surface         | Ship `AGENTS.md` at repo root per the LF agent-binding-subset schema. Eight sections, each path-anchored pointer (no duplication per R-3): build/test commands, file-scope rules, wire-format pointer, skill enumeration, authority-doc enumeration (TIER 0/1/2), outer-loop template pointers, session-start ritual, pre-commit gates. Closes long-standing `docs/grok-orchestration-principles.md §8` gap. Land ADR-0012 (AGENTS.md as cross-tool surface; agent-binding subset shipped; LF-draft advanced fields deferred). Land ADR-0013 (D-1 bidirectional attribution; symmetric reading; parallel §4 checklist item under D-1). Modify `CLAUDE.md` with one-line "Cross-tool agent-binding surface" pointer to AGENTS.md. Modify `docs/founder-directives.md §4` to add D-1 reverse checklist item (§3 D-rule bodies and §1 provenance untouched per D-6). | `test -f AGENTS.md`; required markers grep-detectable (`handoff-contract.md`, `skills`, `TIER 0`, `TIER 1`, `TIER 2`, `.grok/templates`, `sync-plugin`, `audit-doc-drift`); `./scripts/audit-doc-drift.sh` exit 0; both ADRs follow numbered template; `docs/founder-directives.md §4` lists the new D-1 reverse item; CLAUDE.md gains "Cross-tool agent-binding surface" section. **DONE 2026-05-26.** |
| TICKET-012 | Cursor integration design doc                         | Ship `docs/cursor-integration.md` as the single TIER-2 operational rulebook explaining how the harness is used INSIDE the Cursor IDE. Nine sections mirroring `docs/self-healing-design.md`'s structure: purpose, position in harness (operator stack), surfaces table mapping every harness feature → Cursor driver, Cursor's chat agent as inner-loop driver (primary), headless Claude Code as alternative inner-loop driver, outer-loop templates from Cursor, session-start ritual under Cursor (always-loaded-rule equivalent to Claude Code's push-hook), failure modes (8 named with structural mitigations), sequencing to TICKETS 013-014. Land ADR-0015 (Cursor as host IDE — operator stack: Cursor → Grok/Claude → harness primitives; TIER-2 not TIER-1). | `test -f docs/cursor-integration.md`; nine `## §N ` sections grep-detectable; every cited primitive resolves to a real file; `./scripts/audit-doc-drift.sh` exit 0; zero duplication of upstream content (R-3); ADR-0015 follows numbered template. **DONE 2026-05-26.** |
| TICKET-013 | .cursor/rules/ generator + F-5 audit pattern          | Ship `scripts/export-cursor-rules.sh` and four generator-output `.cursor/rules/*.mdc` files (agent-context, harness-overview, quality-gate, d-rules). Materializes Cursor's always-loaded-rule equivalent to Claude Code's SessionStart hook (`docs/cursor-integration.md §7`). Each file carries `# Generated by ...` header; hand-edits forbidden. Wire generator into `scripts/sync-plugin.sh --ensure` as additive step after cache materialization. Extend `scripts/audit-doc-drift.sh` with F-5 pattern that re-runs the generator in `--check` mode and emits a finding for any drift. `.gitignore` adds `.harness/cursor-sessions/` as forward-looking reserve; `.cursor/rules/` is NOT gitignored. Land ADR-0014 (generator-output-only; F-5 audit pattern; hand-edits forbidden; `quality-gate.mdc` content is hand-maintained mirror at v1 with rationale). | Fresh `--ensure` produces all four `.mdc` files; each carries the "Generated by" header; running generator twice produces zero diff (`--check` exit 0); appending a hand-edit triggers `F-5 (cursor-rules drift):` finding; regeneration clears the finding; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/smoke-e2e.sh` exit 0; `bash -n` clean on all three scripts; `sync-plugin.sh --help` unchanged (F-4 still passes). **DONE 2026-05-26.** |
| TICKET-014 | .cursor/commands/ slash commands (operator surface)   | Ship seven slash-command markdown files at `.cursor/commands/` across three classes: Class A terminal wrappers (`sync.md`, `smoke.md`, `audit.md`), Class B outer-loop drivers (`research.md`, `decompose.md`, `dispatch.md`), Class C inner-loop driver (`inner-loop.md`). Each file follows the uniform Purpose / Inputs / Steps / Success criteria / Composition template. `/inner-loop` makes Cursor's chat agent the default inner-loop driver inside Cursor; headless `claude -p` remains the documented alternative per `docs/cursor-integration.md §5`. Update `docs/cursor-integration.md §3` surfaces table to surface the seven slash commands in present tense (drop "Once shipped:" hedges). Append "Hybrid Harness v0.2 reconciliation complete" entry to `AUTOMATION_INTEL.md` per v0.2 spec §5. Land ADR-0016 (Cursor slash commands as operator surface; Cursor's chat agent as default inner-loop driver; uniform five-section template; MCP server deferred to TICKET-017). | All seven `.cursor/commands/*.md` exist; each contains the five required sections (Purpose / Inputs / Steps / Success criteria / Composition); each cites the script-wrapped or template-driven primitive plus the D-1 reverse trailer; `docs/cursor-integration.md §3` surfaces the commands in present tense; `AUTOMATION_INTEL.md` gains the v0.2 reconciliation entry; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/smoke-e2e.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; Q-DEMO step is operator-attested per the smoke-script pattern. **DONE 2026-05-26.** |
| TICKET-015 | orchestrating-swarms skill (worker-fanout)            | Ship `.claude/skills/orchestrating-swarms/SKILL.md` materializing G-rules §§4, 9, 10 + G-7, G-8, G-9, G-16 — the orchestrator-worker pattern. Worker-fanout mode only (composes on Grok's outer-loop decomposition per G-7; does NOT replace it). Steps 0–7: pre-flight G-rule extraction, decomposition acceptance, file-scope conflict check (overlap → serialize per G-8), bounded fan-out (≤8 per G-9), per-worker spawn (one worktree per worker per G-8; each worker runs existing `tdd-pro-cl-workflow` R-G-R), output collection, synthesis (lead surfaces; does NOT auto-retry — that's self-healing's territory per ADR-0011), worktree cleanup. Architect / Builder / Validator from the briefing map sequentially within ONE worker to R-G-R, not as three parallel sub-agents per ticket. Driver compatibility: Claude Code Task tool = native parallel; Cursor degrades to serial; documented honestly. Add §4 fourth bullet to `AGENTS.md` framing the skill as orchestration-tier sibling (NOT a fourth tdd-pro-core skill). Append `## 2026-05-26 — Swarm orchestration v1 (MVP)` entry to `AUTOMATION_INTEL.md` per briefing #2 action. Land ADR-0017 (worker-fanout mode chosen; MVP scope; deferrals named — PostToolUse hooks, self-healing tests, lead-orchestrator mode, Apple/Google triggers, weekly debt cron, hierarchical multi-supervisor, daemon worktree cleanup). | `test -f .claude/skills/orchestrating-swarms/SKILL.md`; YAML frontmatter parseable (`name: orchestrating-swarms`, non-empty description); all eight Step markers grep-detectable (`## Step 0` through `## Step 7`); all cited primitives resolve to real paths; D-1 reverse attribution trailer present (per ADR-0013); `AGENTS.md §4` lists four skills (3 inner-loop + 1 orchestration); `AUTOMATION_INTEL.md` gains 2026-05-26 entry; `./scripts/audit-doc-drift.sh` exit 0; `./scripts/smoke-e2e.sh` exit 0; `./scripts/export-cursor-rules.sh --check` exit 0; ADR-0017 follows numbered template; Q-DEMO step is operator-attested per ADR-0008 / ADR-0016 pattern. **DONE 2026-05-26.** |
| TICKET-043 | EO §2 vulnerability sub-gate + `audit-vuln-scan.sh` (F-EO-1) | Add a fifth, default-on quality sub-gate `vulnerabilities_remediated` to `docs/quality-gate.md` (promotion follows the ADR-0026 pattern; lands its own ADR). Ship `scripts/audit-vuln-scan.sh` wrapping `npm audit --json` / OSV-style output, ranking **CISA KEV (Known-Exploited-Vulnerabilities)** CVEs ahead of generic high/critical (per ADR-0044) (bash 3.2 portable; tooling-absent → vacuous pass + documented exemption, mirroring `coverage_delta_min`). A CL touching dependencies fails `green` if a dep carries a high/critical CVE without a `docs/deviations.md` row. Wire into `session-start.sh` + CI audit chain + the `rules_verified` response field. Harness-native (no plugin edit). Substrate test `tests/test-audit-vuln-scan.sh` incl. injected-CVE case. Per `docs/eo-2026-ai-innovation-security-alignment.md` F-EO-1 / ADR-0043. | `bash -n scripts/audit-vuln-scan.sh` clean; injected high-severity dep → exit non-zero; tooling-absent → vacuous pass + exemption note; `tests/test-all.sh --quiet` passing; full audit chain green; sub-gate documented in `quality-gate.md`; its own promotion ADR landed; `git diff docs/founder-directives.md` == 0 (D-6). |
| TICKET-044 | EO §2 clearinghouse-style cyber report emitter (F-EO-2) | Ship `scripts/emit-cyber-report.sh` aggregating vuln-gate output (TICKET-043) + S-1..S-6 hook findings + manifest provenance into a CISA/OMB-shareable `cyber-report.json` (finding id, CWE/CVE, severity, remediation status, provenance ref) plus a **security-score + vuln-MTTR metric** reusing `scripts/audit-metrics.sh` / `docs/dora-metrics.md` (per ADR-0044; no new metrics substrate). Local analogue of the EO's voluntary AI Cybersecurity Clearinghouse feed — produces a shareable report; never auto-uploads. Defensive wire into `smoke-e2e.sh`. Substrate test. Per design doc F-EO-2 / ADR-0043. Depends on TICKET-043. | `bash -n scripts/emit-cyber-report.sh` clean; emits parseable `cyber-report.json` with finding rows + provenance refs; no outbound network call; `tests/test-emit-cyber-report.sh` passing; audit chain green; `git diff docs/founder-directives.md` == 0 (D-6). |
| TICKET-045 | EO §2/§3 IP protection: manifest signing + SBOM (F-EO-4/F-EO-5) | Activate the reserved `signature: null` manifest field via optional Sigstore/cosign keyless signing (off by default; on when `cosign` present + opted-in — honors over-engineering filter for airgapped operators); add verification to `scripts/audit-manifest.sh`; emit an **in-toto attestation** targeting **SLSA Build L3+** when signing is enabled (per ADR-0044; cosign stays opt-in/off-by-default for airgapped portability). Enrich `aibom.json` into a full CycloneDX (SPDX operator-selectable) SBOM covering dep tree + AI-BOM lineage, leveraging the `slsa` standards namespace. Harness-native. Substrate tests. Per design doc F-EO-4/F-EO-5 / ADR-0043. | `cosign`-present path populates `signature` + audit verifies it; `cosign`-absent path no-ops cleanly (default); valid CycloneDX/SPDX SBOM emitted; `tests/test-*` passing; audit chain green; `signature: null` extension point honored (no schema_version bump); `git diff docs/founder-directives.md` == 0 (D-6). |
| TICKET-046 | EO §3 frontier-model readiness + secure-deployment checklist (F-EO-6/F-EO-8) | Ship `docs/frontier-model-readiness.md` (a "covered frontier model" voluntary-framework self-assessment: public cyber-capability proxy self-screen; confidentiality/insider-risk/IP controls for sharing with government/trusted partners; trusted-partner selection rationale) + a `.harness/handoffs`-shaped self-assessment template, and `docs/secure-deployment-checklist.md` (adversarial tests for the TDD agents; insider-risk mitigations; external-reviewer confidentiality controls; composes on `docs/security-review.md`). Explicitly NOT a classified-benchmark replica. Docs-only. Per design doc F-EO-6/F-EO-8 / ADR-0043. | Both docs exist, follow the TIER-2 doc template, cross-link `security-review.md` + design doc; template parses; cross-reference audit green; `git diff docs/founder-directives.md` == 0 (D-6). |
| TICKET-047 | EO §4 misuse-resistance profile + defensive-coding examples (F-EO-9/F-EO-3) | Ship a harness-side compliance profile filtering `.harness/rules/active.json` into an EO-§4 / OWASP-LLM subset (prompt injection, model extraction, insecure output handling), asserted for security-flagged tickets via `applicable_rules`. Add `examples/` scaffolds demonstrating the harness generating/testing defensive code under R-G-R: input-validation hardening, agent-action sandboxing, prompt-injection resistance. No new plugin rule (consumer-side filter only). Per design doc F-EO-9/F-EO-3 / ADR-0043. | Profile filters active.json to the named subset and asserts on flagged tickets; example scaffolds pass their own R-G-R tests; audit chain green; no `claude-tdd-pro` path touched (prime directive); `git diff docs/founder-directives.md` == 0 (D-6). |
| TICKET-048 | EO §3 red-team/blue-team adversarial swarm composition (F-EO-7) | Document an outer-loop workflow composing the existing `orchestrating-swarms` skill into a paired red-team (adversarial prompts, model-extraction probes, prompt-injection) vs blue-team (hardening fixes under R-G-R) exercise on isolated worktrees — mirroring the EO trusted-partner early-access-for-hardening intent. Composition only (composes-on per G-7; no plugin edit, no new skill in this repo). The red-team/blue-team *skills* are filed as a v1.11 plugin amendment proposal separately. Per design doc F-EO-7 / ADR-0043. | Workflow doc exists and cites only existing skill primitives; G-7 composes-on (not replaces) honored; no plugin/skill file added here; cross-reference audit green; `git diff docs/founder-directives.md` == 0 (D-6). |
| TICKET-049 | EO compliance profile + standards mapping (F-EO-10) | Ship `docs/compliance-profiles/eo-2026.md` mapping harness gates → NIST AI RMF / NIST SSDF / CISA BOD-aligned controls + the EO §2/§3/§4 thrusts, as a traceability matrix (control → harness asset → enforcing audit/gate) suitable for government/enterprise review. Add a README "EO-2026 alignment" pointer + CHANGELOG entry. Docs-only. Per design doc F-EO-10 / ADR-0043. | Profile + traceability matrix exist; every row resolves to a real harness asset/audit; README + CHANGELOG updated; cross-reference + doc-drift audits green; `git diff docs/founder-directives.md` == 0 (D-6). |
| TICKET-050 | EO-2026 governance-layer enforcement spine (makes EO always-on across every ticket; closes the "siloed feature" gap) | Per ADR-0045, wire the EO from a feature set into a cross-cutting governance layer using the **existing** operator-declared-standards machinery (ADR-0037) — no new enforcement mechanism, no harness-native EO *rules* (rule content is sourced from `claude-tdd-pro`'s in-flight EO work via `active.json` on a future pin bump). Spine = (a) `docs/quality-gate.md`: EO sub-gates (vuln F-EO-1, provenance/signing F-EO-4) documented as standing `green` requirements; (b) `docs/handoff-contract.md`: EO-namespaced rules are **non-exemptible** in `applicable_rules` (no ticket may drop them; reinforces the existing fail-closed "absent ⇒ all apply" default); (c) `.grok/templates/decomposition.md` + dispatch: always include the EO subset when populating `applicable_rules`; (d) `CLAUDE.md` + `AGENTS.md` standing-directive entry (CLAUDE.md landed in ADR-0045 CL; AGENTS.md mirrored here); (e) **two-phase enforcement (ADR-0046):** the handoff requires EO conformance at BOTH the inner loop's design-before-code phase AND the code phase, the response/decision-trail MUST attest design-phase EO conformance, and `green` is refused on code alone if the pre-code design carries no EO attestation (plugin enforces at both phases per its in-flight EO work; harness demands + verifies via the contract — rides existing decision-trail + `rules_verified` + gate, no new mechanism). Content-agnostic: activates whatever EO rules appear in `active.json`; full rule-content enforcement is pin-bump-gated (separate CL + ADR per architecture-principles §15). Substrate test asserting EO-subset non-exemptibility. Per design doc §9 / ADR-0045. | EO sub-gates documented as standing in `quality-gate.md`; handoff-contract marks EO subset non-exemptible; decomposition/dispatch templates always inject the EO subset; `AGENTS.md` mirrors the CLAUDE.md standing directive; spine is content-agnostic (no harness-native EO rule invented); `tests/test-*` asserting non-exemptibility passing; full audit chain green; no `claude-tdd-pro` path touched (prime directive); plugin pin unchanged; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-14** (`scripts/audit-eo-governance.sh` content-agnostic spine — non-exemptibility + two-phase `eo_design_conformance` attestation; `tests/test-audit-eo-governance.sh` 10/10; `handoff-contract.md` + `quality-gate.md` + `AGENTS.md` + `.grok/templates/{decomposition,dispatch}.md` + `session-start.sh` (WARN-only) + CI hard gate wired; ADR-0048; `tests/test-all.sh` 19/19; vacuous pass until plugin EO rules land via pin bump). |
| TICKET-051 | Authoritative-source citation-integrity gate (widen + deepen the conformance suite) | Per 2026-06-15 operator directive ("Widen and Deepen all tests to ensure that all architecture, design and coding done by the plugin, fullstack and cloud, conforms to cited rules from all authoritative sources"). The conformance suite verified behavior but never verified that the rule corpus the harness enforces is itself fully cited and intact, and the TIER-0 supreme corpus was in no coverage audit. Ship `scripts/audit-source-citations.sh` — PART A (operator-standards registry `.harness/rules/active.json`): A1 every enforced rule has a non-empty `provenance[]`; A2 no empty provenance `source`; A3 every `namespaces_seen` namespace non-empty unless allow-empty (`_community`); A4 the canonical authoritative set is intact — fullstack (react/typescript/node/web-vitals/w3c) AND cloud/security (owasp/slsa); A5 [informational] compliance-`controls[]` coverage among security P0 rules (surfaced, not gated — plugin-owned content). PART B (authoritative-source docs named in `CLAUDE.md`, incl. the TIER-0 corpus): B1 each exists; B2 each cross-referenced ≥1×. Additive (ADR-0047); content-agnostic on rule semantics (prime directive). Wire WARN-only at session start + hard gate in CI. Deepen `tests/test-audit-eo-governance.sh` (green-only-bite + `deviated`-tolerance) + `tests/test-audit-standards-conformance.sh` (registry breadth/namespace coverage). Per ADR-0049. | `audit-source-citations.sh` exits 0 on real registry+docs (28/28 rules cited; 8 namespaces present; TIER-0 corpus wired); `tests/test-source-citations.sh` passing; existing suites deepened; full audit chain green; session-start WARN + CI gate wired; no `claude-tdd-pro` path touched (prime directive); pin + `schema_version` unchanged; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-15** (`scripts/audit-source-citations.sh` PART A A1–A5 + PART B B1–B2; `tests/test-source-citations.sh` 17/17; EO-governance test 12, standards-conformance test 17; `session-start.sh` WARN + `test.yml` hard gate; ADR-0049; `tests/test-all.sh` 20/20; full audit chain green; A5 surfaces `g-node-010` slsa controls gap informational). |
| TICKET-052 | Generative-function integration tests (non-technical user → world-class delivery; cloud + fullstack) | Per 2026-06-15 operator directive ("integration tests for all generative functions (cloud architecture and fullstack development)… simulate a user building various software with no technical experience and the plugin delivering world class software to satisfy the unique usecase of the user"). Add a `tests/integration/` tier: 6 plain-English personas across both domains (fullstack: recipe app/home cook, budget SPA/bakery owner, event board/volunteer; cloud: photo-resize API/photographer, order pipeline/shop owner, site+CDN+auth/nonprofit). `simulate.mjs` emits a contract-valid req/res pair simulating a world-class inner-loop delivery (every active rule for the detected stack enforced via `applicable_rules` — react/typescript/node/web-vitals/w3c + owasp/slsa — plus the non-exemptible two-phase EO layer; green; rules_verified all pass; `eo_design_conformance` present), validates the wire schema + world-class stack-coverage, and supports negative modes. `test-generative-integration.sh` drives all scenarios, runs the REAL `audit-eo-governance.sh` + `audit-source-citations.sh` over the artifacts, and includes negative scenarios (drop critical stack family; omit EO attestation; omit EO rule) proving the gates reject sub-world-class delivery. STUB mode (live-LLM e2e deferred per ADR-0008); harness owns contract+gates, plugin owns generation (prime directive). Wired into `test-all.sh` + CI. Per ADR-0050. | 6 scenarios across cloud+fullstack each deliver world-class (full stack coverage + EO two-phase + citations); negative cases rejected by the gates; `tests/integration/test-generative-integration.sh` passing; `tests/test-all.sh` discovers the tier; no `claude-tdd-pro` path touched (prime directive); pin + `schema_version` unchanged; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-15** (`tests/integration/{simulate.mjs,scenarios/*.json (6),test-generative-integration.sh,README.md}`; 17/17 integration assertions; `tests/test-all.sh` 21/21; real EO + citation gates exercised over emitted artifacts; 3 negative gate-rejection modes; ADR-0050). |
| TICKET-053 | End-to-end demonstration doc tree (surface the elicitation→architecture flow as a first-encounter doc) | Per 2026-06-15 operator directive ("create a directory structure and folder for this end-to-end demonstration and persist it in the repo and surface it to documentation as one of the first things the user encounters"). Persist the Shop-owner (multi-region order pipeline) worked example — the outer-loop requirements elicitation, the plugin's plain-language explanations guiding technical decisions, and the technical decision-making that completes the architecture — as a navigable doc tree under `docs/end-to-end-demo/`: a landing index + a `shop-owner-order-pipeline/` folder split into the four phases (requirements elicitation, guided technical decisions, architecture synthesis, handoff & standards). Cross-link the executable `tests/integration/` scenario it mirrors and map every guarantee to the enforced `g-*` standard (incl. the `slsa` rule the user could never name). Surface prominently: a "▶ See it end-to-end" callout near the top of `README.md`, a ToC entry, a repo-map entry, plus reciprocal pointers from `QUICKSTART.md` and `tests/integration/README.md`. Docs-only. | Doc tree exists + navigable (landing + 4 phases, all cross-linked); README surfaces it near the top + ToC + repo map; QUICKSTART + integration README point to it; cross-reference + doc-drift audits green; no `claude-tdd-pro` path touched (prime directive); `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-15** (`docs/end-to-end-demo/{README.md, shop-owner-order-pipeline/{README,1-requirements-elicitation,2-guided-technical-decisions,3-architecture-synthesis,4-handoff-and-standards}.md}`; README "▶ See it end-to-end" callout + ToC item 0 + repo-map entry; QUICKSTART + `tests/integration/README.md` reciprocal links; full audit chain green; `tests/test-all.sh` 21/21). |
| TICKET-054 | First-time install reliability — `install.sh` + Node-version-robust smoke + read-only `--check` | Per 2026-06-15 operator directive ("install in less than 30 seconds from a simple install script. Make it happen") + a bug report that the documented setup failed silently on Node 24. Three fixes: (1) **`install.sh`** at repo root — one command materializes the pinned plugin + runs the end-to-end self-check with plain-language ✓/✗, prerequisite checks, `--quick`, and a "what now" pointer; (2) **`smoke-e2e.sh` Node-version robustness** — force `node --test --test-reporter=tap` (Node ≥24 defaults to the `spec` reporter `ℹ pass N`, which the `^# pass ` parse couldn't read and which died silently under `pipefail` before the script's own error handler) + `|| true` on the parse so format drift fails loudly; (3) **`sync-plugin.sh --check` read-only restore** — `--check` re-cloned the real cache at branch HEAD and never restored the pin, parking operators' skill symlinks on the wrong plugin version and making `test-all` non-idempotent (newly visible once upstream `main` added a top-level `examples/`). Regression guards in `test-install.sh` (new), `test-smoke-e2e.sh`, `test-sync-plugin.sh`. Per ADR-0051. | `./install.sh` finishes with `✅ Done` (~20–30s, mostly the one-time download) on any recent Node; smoke prints `smoke OK`; `--check` leaves cache at the pin; `tests/test-all.sh` idempotent across back-to-back runs; full audit chain green; no `claude-tdd-pro` path touched (prime directive); pin + `schema_version` unchanged; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-15** (`install.sh` + `tests/test-install.sh` 9/9; `smoke-e2e.sh` TAP pin + defensive parse; `sync-plugin.sh --check` pin-restore; regression guards added; `tests/test-all.sh` 22/22 twice; ADR-0051). |
| TICKET-055 | First-time guide incorporating CTP's real install instructions | Per 2026-06-15 operator directive ("Considering the GCTP utilizes CTP, please incorporate the latest install instructions from CTP"). Ship `docs/first-time-guide.md` grounded in the **pinned** CTP cache (`bba77df`, CTP v0.4.0) — verified against the cache, not invented: Path A = the full harness in one command (`./install.sh`, which materializes the pinned CTP automatically — no separate CTP install); Path B = CTP standalone via its real one-liner `curl -fsSL …/scripts/install.sh | bash` with subcommands (`init`/`upgrade`/`doctor`/`uninstall`), profiles (standard/strict/financial/regulated/government/react/node/library), `--with-grok`/`--with-lsp`/`--yes`, and preflight (bash≥3.2/node≥18/ruby≥3.0/git). Real CTP commands (`/architect`, `/analyze`, `/onboard`, `/feature`, `/doctor`, `/help`) described truthfully from the cache; links CTP's canonical docs **by reference** (R-3, no vendoring) and GCTP's own demo (NOT the upstream-only `examples/dog-walker-marketplace`, which is absent at the pin). Honest version note (pin vs latest; pin bumps ADR-gated). Surfaced: README "🟢 First time" callout + ToC item 0 + repo-map entry; QUICKSTART pointer. Docs-only. | `docs/first-time-guide.md` exists, accurate to the pinned CTP surface, no broken/upstream-only links; README + QUICKSTART surface it near the top; cross-reference + doc-drift audits green; no `claude-tdd-pro` path touched (prime directive); pin + `schema_version` unchanged; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-15** (`docs/first-time-guide.md` Paths A/B grounded in cache `bba77df`; README callout + ToC + repo map; QUICKSTART pointer; full audit chain green; `tests/test-all.sh` 22/22). |

| TICKET-056 | Plugin pin bump `bba77df` → `4354903` (adopt latest CTP main) | Per 2026-06-15 operator directive ("work out the compatibility in order that both plugins are utilizing the latest commit on main"). CTP `main` was 70 commits / 8 days ahead of the pin; the dog-walker demo and cloud-architect/EO content were invisible to GCTP. Compatibility **proven** (not assumed): the 70-commit span is purely additive — `architecture-v1.9.md` +492/−0, `CLAUDE.md` +14/−0, the 3 consumed skills byte-identical, 0 commands removed, 0 files deleted, only `examples/` added; CTP `main` green. Bump: update `docs/claude-tdd-pro.lock.yaml` (pin + 2 changed contract hashes; 3 skill hashes unchanged); declare the new `examples/` surface in `docs/plugin-surface-consumption.md`; teach `audit-source-citations.sh` A3 that CTP's new cloud/governance **guidance** namespaces (`aws`/`azure`/`gcp`/`hashicorp`/`linux-foundation`/`security-governance`/`us-government`) are legitimately rule-empty (they feed `/architect`, not the rubric detectors); refresh pin/namespace references in `README.md` + `docs/first-time-guide.md`. Rubric rule set stays 28 (the new namespaces are guidance, not detectors); no `eo` namespace yet, so the EO spine stays armed-but-vacuous. ADR-0052. | Lockfile at `4354903` with refreshed hashes; `sync-plugin.sh --check` reports 0 contract drift / pin matches HEAD; `examples/` declared; full audit chain green; `tests/test-all.sh` 22/22; `smoke-e2e` green; no `claude-tdd-pro` path touched (prime directive); `schema_version` + 3 skill hashes unchanged; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-15** (pin `bba77df`→`4354903`; ADR-0052; `examples/` surface + guidance-namespace allow-list; audit chain green; 22/22). |

| TICKET-057 | Plugin pin bump `4354903` → `3432b52` (Layer-A: cloud/EO sources become enforced detector rules) | Per 2026-06-15 operator direction to keep GCTP on the latest CTP `main`. One commit past ADR-0052's `4354903`: CTP CL-475 (§28.15) promotes the cloud/governance source corpora from guidance-only into **enforced detector rules** (Layer-A). Verified additive: `architecture-v1.9.md` +12/−0, `CLAUDE.md` + 3 skills unchanged, 0 deletions, no new surfaces. Effect: `active.json` grows 28 → **42 rubric rules** — the 7 namespaces (`aws`/`azure`/`gcp`/`hashicorp`/`linux-foundation`/`security-governance`/`us-government`) now carry 2 grounded, provenance-bearing detector rules each. Bump: lockfile pin + `architecture-v1.9.md` hash; **revert ADR-0052's A3 allow-list** (those namespaces are rule-bearing now, so A3 guards them again — only `_community` stays allow-empty); refresh README/first-time-guide counts. Clarifies the EO framing: there is still no literal `eo` namespace (EO is enforced under `security-governance` + CTP-side H-14/conventions/citation-conformance), so `audit-eo-governance.sh` (which keys on `eo`) stays vacuous — a measurement property of that guard, NOT an enforcement gap. ADR-0053. | Lockfile at `3432b52`; `--check` 0 drift / pin matches HEAD; `active.json` 42 rules; A3 allow-list reverted to `_community`; full audit chain green; `tests/test-all.sh` 22/22 (idempotent); `smoke-e2e` green; no `claude-tdd-pro` path touched; `schema_version` + 3 skill hashes unchanged; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-15** (pin `4354903`→`3432b52`; ADR-0053; 14 cloud/governance detector rules now enforced; audit chain green; 22/22). |

| TICKET-058 | Correct first-time-guide install/command claims + record upstream CTP findings | Per 2026-06-15 fresh-machine install test (operator-executed). The test verified Path A (`./install.sh`) is clean, but found the first-time-guide overclaimed: (1) `/architect` is NOT a slash command (no `commands/architect.md` in CTP — it's a skill/agent/script); (2) `/help` likewise; (3) slash commands are a **Cursor** surface — neither Path A (3 skills + hooks, empty `.claude/commands/`) nor Path B (Cursor-targeted installer) surfaces CTP slash commands in a plain Claude Code chat; (4) Path B's ruby ≥3.0 preflight hard-stops (`exit 3`) on no-ruby cloud targets AND stock macOS (ruby 2.6). All verified against pin `3432b52`. GCTP-side fix: rewrite `docs/first-time-guide.md` — remove `/architect`+`/help`, reframe the command surface as Cursor (with the verified `.md` commands), and add a strong ruby/Cursor warning to Path B. CTP-side findings (the `conflicts[@]` bash-3.2 crash at install.sh:233, ruby-preflight UX, architect-as-slash-command, Claude-Code registration) recorded in new `docs/upstream-ctp-proposals.md` as v1.11 proposals — NOT patched here (prime directive). Docs-only. | first-time-guide accurate to the verified command surface (no `/architect`/`/help` slash claims; Cursor-vs-Claude-Code stated; ruby caveat loud); upstream findings recorded + routed; cross-reference + doc-drift audits green; no `claude-tdd-pro` path touched (prime directive); `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-15** (`docs/first-time-guide.md` corrected; `docs/upstream-ctp-proposals.md` P-1..P-4; audit chain green; `tests/test-all.sh` 22/22). |

| TICKET-059 | Plugin pin bump `3432b52` → `6d2fe13` (adopt CL-476 install fixes from GCTP's own test feedback) | Closes the cross-repo loop: GCTP's install test (TICKET-058) found CTP-side bugs → routed via `docs/upstream-ctp-proposals.md` → CTP fixed them in CL-476 → adopt here. Verified additive single commit: `architecture-v1.9.md` +10/−0, `CLAUDE.md` + 3 skills unchanged, 0 deletions, no new surfaces, 42 rules unchanged. CL-476 adds `commands/architect.md` (P-3 — `/architect` is now a real slash command) + the `install.sh` `${#conflicts[@]} -gt 0` guard (P-1 — no bash-3.2 crash on stock macOS). Bump: lockfile pin + `architecture-v1.9.md` hash; **restore `/architect` in `docs/first-time-guide.md`** (partly reverses TICKET-058, which removed it when no `commands/architect.md` existed); update `docs/upstream-ctp-proposals.md` (P-1/P-3 ADOPTED, P-2 not-a-defect, P-4 clarified); refresh README + guide pin refs. ADR-0054. | Lockfile at `6d2fe13`; `--check` 0 drift / pin matches HEAD; `commands/architect.md` present; `active.json` 42 rules; full audit chain green; `tests/test-all.sh` 22/22; `smoke-e2e` green; no `claude-tdd-pro` path touched; `schema_version` + `CLAUDE.md` + 3 skill hashes unchanged; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-16** (pin `3432b52`→`6d2fe13`; ADR-0054; `/architect` restored; P-1/P-3 adopted; audit chain green; 22/22). |

| TICKET-060 | Activate the EO governance spine against `security-governance` (complete the specified EO architecture) | Per 2026-06-16 operator directive ("complete the architecture that I have specified"). At pin `6d2fe13` CTP's EO authorities are live as `security-governance` detector rules (`require-provenance` P1, `no-known-exploited-ingress` P0), so the EO spine (ADR-0045/0046, built content-agnostic and armed-but-vacuous because it keyed on a literal `eo` namespace CTP never used) can finally bind. Change: `audit-eo-governance.sh` default `EO_NAMESPACES` `eo` → `eo security-governance`; make the harness's own e2e EO-compliant (`smoke-e2e.sh` req carries the 2 EO rules in `applicable_rules`, res carries a two-phase `eo_design_conformance`); extend `.grok/templates/{decomposition,dispatch}.md` to require EO-governance rules from both namespaces; add a regression test that the default set recognizes `security-governance`. Docs synced: `CLAUDE.md` + `handoff-contract.md` + `quality-gate.md` + `eo-2026-…-alignment.md` (no longer "vacuous/empty"). ADR-0055. | `audit-eo-governance.sh` green + non-vacuous (2 EO rules active; smoke handoff compliant); `tests/test-audit-eo-governance.sh` 13/13; full audit chain green; `tests/test-all.sh` 22/22; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-16** (spine live against `security-governance`; smoke EO-compliant; ADR-0055; 22/22). |
| TICKET-061 | Claude Code command surface (`.claude/commands/`) for fresh-chat testability | Per 2026-06-16 operator directive ("get this plugin ready for testing again in a new claude code chat"). GCTP's operator slash commands lived only in `.cursor/commands/`, so a fresh Claude Code chat surfaced none (the gap TICKET-058's install test found). Ship `.claude/commands/{research,decompose,dispatch,inner-loop,sync,smoke,audit}.md` mirroring the Cursor commands and delegating to the same templates/scripts/skills (EO non-exemptibility carried into decompose/dispatch/inner-loop). Correct `docs/first-time-guide.md` (Path A now has Claude Code slash commands — reverses TICKET-059's "no commands in Claude Code" note) + README repo map. ADR-0055. | 7 `.claude/commands/*.md` present + path-resolving (cross-references clean); first-time-guide + README updated; doc-drift + cross-reference audits green; `tests/test-all.sh` 22/22. **DONE 2026-06-16** (7 Claude Code commands; first-time-guide + README synced; ADR-0055; audit chain green). |

| TICKET-062 | GCTP↔CTP architecture-consult loop — governance + contract (Phase 1, additive) | Per 2026-06-16 operator spec (the crossroads/translator looped-consult model) + the standing "additive, not destructive, easy rollback" constraint. Records the model and its contract **additively**: new ADR-0056 (supersedes ADR-0040's mechanism choice via Nygard append — ADR-0040/0039 untouched); **new** `.grok/templates/architecture-consult-loop.md` (the old `architecture-consult.md` left as history); **appended** `docs/handoff-contract.md` sections (§Architecture-Consult-Loop / §Architecture-Cross-Check / §Roadmap) leaving the SUPERSEDED section intact; **appended** CLAUDE.md crossroads-loop subsection composing on the two harness rules. Locks the 5 decisions (loop-not-call; GCTP-as-translator; dual enforcement; Ruby≥3.0 hard prereq; cross-check failure → bounded re-consult → deviation). Governance + contract only — no command/hook wiring this CL (held for review). Per ADR-0056. | New ADR + new template + appended contract sections + appended CLAUDE.md subsection; ADR-0040/0039 + existing SUPERSEDED §Architecture-Consult byte-unchanged (additive/rollback-safe); full audit chain green; `tests/test-all.sh` 22/22; no `claude-tdd-pro` path touched (prime directive); `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-16** (ADR-0056; loop contract + crossroads model recorded additively; wiring deferred to later CLs). |

| TICKET-063 | Architecture-consult loop — wiring CL 1: `/consult` command + preflight spine (additive) | Per 2026-06-16 operator go-ahead to wire Phase 2 (additively). First wiring CL realizing ADR-0056's loop: **new** `scripts/consult.sh` — the deterministic, testable spine (`--preflight` = ruby≥3.0 hard-prereq + CTP engine-presence per D-D; `--engine-path <name>` = allowlisted locator for CTP's `architect-session.sh`/`business-intake.sh`/`architect-recommend.sh`/`well-architected-review.sh`; it locates+gates, never invokes/mutates); **new** `.claude/commands/consult.md` + `.cursor/commands/consult.md` driving the per-juncture crossroads/translator loop (preflight → intake → consult CTP → translate → prompt → decide → size+ticket → cross-check → write `FEATURE-NNN.architecture.json`+cross-check+roadmap per the contract). All additive — no existing command/template/script edited; `/decompose` still uses its ADR-0040 path (consuming the consult is a later CL). `tests/test-consult.sh` (12) covers the exit-code contract. Per ADR-0056. | `scripts/consult.sh` exits 0/1/2 per contract (ruby-absent/engine-absent → 1; bad engine name → 1; usage → 2); `/consult` present on both surfaces; `tests/test-consult.sh` 12/12; full audit chain green; `tests/test-all.sh` 23/23; no `claude-tdd-pro` path touched (engine consumed by reference); `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-16** (consult preflight/locator + command surface; 12/12; 23/23; ADR-0056; wiring of decompose-consumes-consult + cross-check gate + roadmap presentation = subsequent additive CLs). |

| TICKET-064 | Architecture-consult loop — wiring CL 2: `/decompose` consumes the consult artifact (additive) | Per 2026-06-16 operator go-ahead (continue the wiring, additively). A-4 of PROPOSAL-001: when a `/consult` artifact `.harness/handoffs/FEATURE-NNN.architecture.json` is present, `/decompose` derives per-ticket `complexity` (sizing) + `applicable_rules` + grounding from its `decisions[]` (CTP's grounded technical reality) instead of a planner estimate — gated by a new `scripts/consult.sh --validate` mode (schema_version, `needs_grounding==0`, every decision sized + carrying rules; node-parsed). Fully **additive + fallback-safe**: absent/invalid artifact ⇒ the ADR-0040 static-context path is used exactly as before (the legacy `architecture_consult` *variable* row is unchanged). Touches: `scripts/consult.sh` (+`--validate`), `tests/test-consult.sh` (12→19), `.grok/templates/decomposition.md` (+consult-artifact-consumption section + input row + pre-emit check, no existing text removed), `.claude/commands/decompose.md` + `.cursor/commands/decompose.md` (+step 0). Per ADR-0056. | `consult.sh --validate` exits 0 on a contract-valid artifact, 1 on invalid (needs_grounding≠0 / unsized / no rules / non-JSON), 2 on usage; `tests/test-consult.sh` 19/19; decompose template + commands consume-when-present with static fallback; full audit chain green; `tests/test-all.sh` 23/23; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-16** (A-4 wired additively; `--validate` gate + 7 new tests; static path preserved as fallback; ADR-0056). |

| TICKET-065 | Architecture-consult loop — wiring CL 3: cross-check gate (GCTP dual-enforcement on CTP) | Per 2026-06-16 operator go-ahead (continue the wiring, additively). Stage 5 of the loop / PROPOSAL-001's "GCTP checks CTP's output against its own rules": new `scripts/audit-architecture-crosscheck.sh` verifies, for any present consult artifact `.harness/handoffs/FEATURE-NNN.architecture.json`, that (1) every `decisions[*].applicable_rules` id resolves in `active.json`, (2) every decision includes the non-exemptible EO-governance rules (`eo`/`security-governance`), and (3) any `FEATURE-NNN.crosscheck.json` record carries only `pass`/`deviated`/`reconsulted` — a `fail` without a deviation row is rejected (ADR-0056 D-E). Content-agnostic + **vacuous until `/consult` runs** (armed, not biting). Wired WARN-only at session start + hard gate in CI. `tests/test-audit-architecture-crosscheck.sh` (10). Additive — adds a gate, relaxes nothing. Per ADR-0056. | audit vacuous-passes with no artifacts; flags unresolved rule ids / missing EO rules / un-deviated cross-check fails (exit 1); `tests/test-audit-architecture-crosscheck.sh` 10/10; session-start WARN + CI gate wired; full audit chain green; `tests/test-all.sh` 24/24; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-16** (Stage-5 dual-enforcement gate; 10/10; 24/24; ADR-0056). |

| TICKET-066 | Architecture-consult loop — wiring CL 4: roadmap presentation (Stage 7) | Per 2026-06-16 operator go-ahead (continue the wiring, additively). Stage 7 of the loop / PROPOSAL-001's "present real tickets — sized, sequenced, planned — to the user": new `scripts/consult.sh --roadmap <artifact>` mode renders a contract-valid `FEATURE-NNN.architecture.json` into the §Roadmap shape — maps `decisions[]` → prospective tickets (keyed by juncture; `/decompose` assigns final TICKET-NNN ids), topologically sequences over `depends_on` (Kahn; cycle ⇒ exit 1), pulls grounding from the recommended option. Re-validates first (never renders an invalid artifact). Emits human-readable roadmap + the §Roadmap JSON. New `/roadmap` command (.claude + .cursor) drives it as Stage 7 of `/consult` and feeds `/decompose`. Additive — adds a renderer + command, relaxes nothing. Per ADR-0056. | `--roadmap` renders valid artifacts (exit 0) with correct topological sequence; refuses invalid artifacts + dependency cycles (exit 1); usage errors exit 2; `tests/test-consult.sh` 30/30; README repo map + Cursor/Claude command counts (9) corrected; coverage table updated; full audit chain green; `tests/test-all.sh` 24/24; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-16** (Stage-7 roadmap renderer + `/roadmap`; 30/30; 24/24; ADR-0056). |

| TICKET-067 | Code-time enforcement — Proposal B harness-side: the `rules_verified` handoff gate | Per 2026-06-16 operator go-ahead. The plugin's `rubric/runner.sh` already runs detectors against the diff at commit time (`scripts/audit-standards-conformance.sh`, TICKET-032); this closes the other half of Proposal B — the harness statically verifies the inner-loop quality gate the plugin produces. New `scripts/audit-rules-verified.sh`: for every `.harness/handoffs/*.{req,res}.json` pair, a `green` response MUST verify every request `applicable_rules` id as `pass`/`deviated` (deviations need a `docs/deviations.md` row); any `fail` or missing key forces red (contract §rules_verified, lines 104-109). Non-green responses are not gated; requests without `applicable_rules` use the fail-closed default and are noted, not gated (EO slice stays with `audit-eo-governance.sh`). Content-agnostic + vacuous until a handoff carries applicable_rules; passes the canonical TICKET-042 green pair. Wired WARN at session start + CI hard gate (11th lens). `tests/test-audit-rules-verified.sh` (13). Harness owns the enforcement spine; plugin owns rule content + detectors (prime directive). Additive — adds a gate, relaxes nothing. Per ADR-0037. | audit passes the real TICKET-042 pair + vacuous-passes with no gated pairs; flags green-with-fail, green-with-missing-key, green-deviated-without-row, invalid enum value (exit 1); non-green not gated; `tests/test-audit-rules-verified.sh` 13/13; session-start WARN + CI gate wired; badge 11/11; full audit chain green; `tests/test-all.sh` 25/25; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-16** (Proposal-B harness-side rules_verified gate; 13/13; 25/25; ADR-0037). |
| TICKET-068 | Agent operating compact — fail-closed behavioral binding on the GCTP driver | Per 2026-06-18 operator directive (*"persisted in the GCTP repo and presented to the user … prompting their agreement, and enforced on Claude Code every time the plugin is installed and used"*). Persist the agent's affirmed four-point commitment (act only as GCTP's user; do not self-architect; no direct line to CTP; nothing un-generated enters the app repo) + the honest caveat (translation under `/consult` is irreducible agent cognition) + the scope boundary (governs app architecture *through GCTP*, NOT harness self-maintenance) as `docs/agent-operating-compact.md` (TIER-1 binding). New `scripts/accept-compact.sh` records operator acceptance at `.harness/agent-compact-ack.json` keyed to the compact's SHA-256 (any amendment ⇒ re-accept). New `scripts/audit-agent-compact.sh` fail-closed lens: compact present + wired into CLAUDE.md + AGENTS.md + currently accepted, else exit 1. Wired into session-start (STOP banner when unaccepted/stale — deliberate ADR-0001 warn-only exception), the pre-commit chain, and CI (13th lens). Mirrored binding into CLAUDE.md + AGENTS.md. `tests/test-audit-agent-compact.sh` (17). Harness self-maintenance plane (not a CTP edit; prime directive intact). Per ADR-0057. | compact + accept script + audit lens + ack exist; audit passes the real accepted tree + flags missing/unwired/unaccepted/stale/invalid-JSON (exit 1); accept round-trips + amendment invalidates acceptance; `tests/test-audit-agent-compact.sh` 17/17; session-start STOP-banner + pre-commit + CI gate wired; full audit chain green; `tests/test-all.sh` all suites pass; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). |
| TICKET-069 | Plugin pin bump `6d2fe13`→`eb7b2af` — adopt Fix E/F/G + universal-coverage foundation (kata-feedback loop) | §15-gated pin bump adopting CTP CL-477..481 (§28.17–28.21): the entire CTP-side scope of the enforcement-spine repair surfaced by GCTP's O'Reilly kata test build. Lands `rubric/enforce.sh` (Fix E — catalog-keyed, 4-state `pass/fail/not_applicable/not_enforced` + `files_evaluated`, the frozen contract GCTP's Fix B/C consume), `g-doc-001/002` prose detectors (Fix F), the `no-any` comment-strip (Fix G), and the `g-universal-*` apply-by-default foundation. Contract drift is additive-only (`architecture-v1.9.md` +51/−0; the 3 skills + CLAUDE.md byte-identical). Lockfile re-hashed by hand under ADR-0058; cross-repo loop recorded in `docs/upstream-ctp-proposals.md` (P-5/P-6/P-7 ADOPTED). No `claude-tdd-pro` path edited (prime directive). Per ADR-0058. | `sync-plugin --check` 0 drift + pin matches HEAD; `standards-sync` 42→46 rules (+`_universal`,`documentation` namespaces); `audit-plugin-surface` 56/56; full audit chain green; `tests/test-all.sh` 26/26; `smoke-e2e` green; `enforce.sh` exercised live from the cache (catalog dispatch + 4-state + `files_evaluated`); `git diff docs/founder-directives.md` == 0 (D-6); no `claude-tdd-pro` path touched. **DONE 2026-06-19** (pin `eb7b2af`; chain green; 26/26; ADR-0058). |
| TICKET-070 | Fix D — first-class `app_root` external-working-tree model + vacuous-green hard guard | Per PROPOSAL-002 (GCTP-side of the kata-feedback loop). Introduce `app_root`: the external app tree GCTP builds + enforces CTP standards on, distinct from `.harness/*`. New `scripts/app-root.sh` resolves it from operator-local `.harness/app.json` (gitignored; tracked `.example`) — exit 0 (configured+exists+non-empty, abs path) / 1 (unconfigured) / 2 (missing or **empty** → refuse). Hard guard: an absent/empty tree is exit 2, never a silent green (consumer mirror of `enforce.sh`'s `not_applicable`-vs-pass). Git-agnostic; relative/absolute paths. Documented in `docs/handoff-contract.md §App-Root`; `/inner-loop` + `/decompose` reference it (file_scope is app_root-relative). Foundation for Fix A/B/C. `tests/test-app-root.sh` (14). Harness self-maintenance plane. Per ADR-0059. | `scripts/app-root.sh` + `.harness/app.json.example` + tests exist; resolves valid relative/absolute roots (0); refuses missing/empty/`.git`-only trees (2); unconfigured + no-key → 1; invalid JSON → 2; `--validate` contract; `tests/test-app-root.sh` 14/14; full audit chain green; `tests/test-all.sh` all suites; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-19** (Fix D; 14/14; ADR-0059). |
| TICKET-071 | Fix A — `/decompose` unions the ruleset; `applicable_rules` under-scoping is a static RED | Per PROPOSAL-002 (GCTP-side kata loop). The kata's root failure: code tickets scoped only `g-node-001/008`; the full language ruleset was never named, so never run. `/decompose` (template + `.claude`/`.cursor` commands) now produces `applicable_rules` = language-filtered(active.json) ∪ every `g-universal-*` (apply-by-default) ∪ every EO rule ∪ consult rules; prefers typed globs; over-scoping is safe (`enforce.sh`→`not_applicable`). New `scripts/audit-applicable-rules.sh` makes under-scoping RED: universal floor (every `g-universal-*`) + language floor per typed glob (`.ts`→g-ts-*+g-node-*, `.tsx`→+g-react-*, `.md`→g-doc-*, `.tf`→g-hashicorp-*, `.yaml`→g-linux-foundation-*); content-agnostic (reads active.json live); vacuous on no-reqs/missing registry. `smoke-e2e` updated to model the union (dynamic universal+EO, req↔res mirror). Wired WARN at session start + pre-commit + CI hard gate (14th lens). `tests/test-audit-applicable-rules.sh` (14). Harness self-maintenance plane. Per ADR-0060. | gate flags a `.ts` ticket omitting g-ts-*/g-node-* + a ticket omitting any g-universal-* (exit 1); passes a fully-unioned ticket; vacuous on no-reqs/missing active.json; `tests/test-audit-applicable-rules.sh` 14/14; smoke + eo-governance + rules-verified all agree on the regenerated TICKET-042; full audit chain green; `tests/test-all.sh` all suites; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-19** (Fix A; 14/14; ADR-0060). |
| TICKET-072 | Plugin pin bump `eb7b2af`→`7a7f74d` — adopt refresh-driven universal coverage | §15-gated pin bump adopting CTP CL-482..483 (§28.22–28.23): refresh-driven universal coverage (effort proportional to upstream source delta) + begin-refreshing-on-install with operator-chosen cadence. Additive-only (`architecture-v1.9.md` +22/−0; CLAUDE.md + 3 skills byte-identical; `enforce.sh` **byte-identical** — the Fix B/C contract is stable). active.json 46→46 (refresh mechanism, no new rules). Lockfile re-hashed by hand under ADR-0061. No `claude-tdd-pro` path edited (prime directive). Per ADR-0061. | `sync-plugin --check` 0 drift + pin matches HEAD; `enforce.sh` byte-identical eb7b2af→7a7f74d; `standards-sync` 46→46; `audit-plugin-surface` 56/56; full audit chain green; `tests/test-all.sh` 28/28; smoke green; `git diff docs/founder-directives.md` == 0 (D-6); no `claude-tdd-pro` path touched. **DONE 2026-06-19** (pin `7a7f74d`; chain green; 28/28; ADR-0061). |
| TICKET-073 | Fix B — inner loop produces `rules_verified` from REAL detector runs (`enforce-standards.sh`) | Per PROPOSAL-002 (GCTP-side kata loop). The kata core failure: `rules_verified` was asserted, never produced by a detector run. New `scripts/enforce-standards.sh --ticket <id>`: resolves `app_root` (app-root.sh, Fix D), reads the ticket's `applicable_rules`, runs CTP's frozen `enforce.sh --root <app> --rules <ids> --json`, and maps the 4-state verdict straight into `rules_verified` (no re-implementing detector dispatch — prime directive). Exit 0 green / 1 red(fail) / 3 incomplete(not_enforced) / 2 error. Wire contract extended additively: `rules_verified` enum `pass/fail/deviated` → +`not_applicable` (neutral) +`not_enforced` (red); `schema_version` stays "1". `audit-rules-verified.sh` honors it (green allows pass/deviated/not_applicable; fail/not_enforced → red). `/inner-loop` (Claude+Cursor) gains step 4a: write `rules_verified` straight from `enforce-standards.sh`, never hand-assert. `tests/test-enforce-standards.sh` (13, hermetic stub) + `tests/test-audit-rules-verified.sh` (13→16). Ruby≥3.0 prerequisite (ADR-0056). Harness self-maintenance plane. Per ADR-0062. | `enforce-standards.sh` maps pass/fail/not_applicable/not_enforced → status/exit (0/1/3/2); refuses empty app_root (2); `--json` faithful mapping + `files_evaluated`; real integration: leaking fixture → `g-universal-no-hardcoded-secrets` fail/red; `audit-rules-verified` accepts green+not_applicable, rejects green+not_enforced; `tests/test-enforce-standards.sh` 13/13 + `tests/test-audit-rules-verified.sh` 16/16; full audit chain green; `tests/test-all.sh` all suites; smoke green; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-19** (Fix B; 13/13 + 16/16; ADR-0062). |
| TICKET-074 | Fix C — dynamic re-run gate: `rules_verified` claims must match a live detector run | Per PROPOSAL-002 (final GCTP-side CL). The static `rules-verified` gate only checked claims were *complete*, never *true* — a green response could assert `pass` on violating code. New `scripts/audit-standards-enforced.sh`: for every green `.res.json` whose request carries `applicable_rules` + a resolvable `app_root`, re-runs detectors via `enforce-standards.sh` and asserts (1) every claimed verdict equals the live verdict (claim `pass` on a live `fail` ⇒ red), (2) every live `pass` has `files_evaluated > 0` (no vacuous pass). Converts "claims complete" → "claims true". Vacuous until `.harness/app.json` is configured (no external app ⇒ nothing to re-verify) — so CI stays green until an operator points at a real tree, then full teeth. Reuses Fix B's spine (no re-implementing dispatch — prime directive). Wired WARN at session start + pre-commit + CI hard gate (15th lens). `tests/test-audit-standards-enforced.sh` (10, hermetic stub). Harness self-maintenance plane. Per ADR-0063. | gate re-runs + accepts matching claims (0); flags claim-pass-on-live-fail, vacuous-pass (files 0), claimed-na-but-live-pass (exit 1); skips non-green / no-applicable_rules; vacuous on no app_root / no green res; `tests/test-audit-standards-enforced.sh` 10/10; full audit chain green; `tests/test-all.sh` all suites; smoke green; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-19** (Fix C; 10/10; ADR-0063; completes GCTP-side Fix A–D). |
| TICKET-075 | Begin refreshing standards sources at session start, on an operator-chosen cadence | Per 2026-06-19 operator directive. GCTP consumes CTP as a pinned snapshot, so CTP's begin-refreshing-on-install (§28.23) never fires in the harness. New `scripts/standards-refresh.sh` drives CTP's refresh entrypoints via the contract surface (no re-implementing scraping/grammar — prime directive): `--check` (session start) re-scrapes the cited sources (OWASP/Google/NIST/SLSA/AWS WA/EO/…) via `standards/initial-refresh.sh` on the configured cadence + re-aggregates active.json; `--configure <N>m\|h\|d\|w\|mo` validated by CTP's `set-refresh-frequency.sh` grammar authority; default `1d` (every active day); surfaces the source→enforcement significance + prompts when unconfigured. Cadence in `.harness/standards-refresh.json` (gitignored; `.example` tracked). Honest scope: cadence enforced at session boundaries (a hook is not a daemon); non-fatal + offline-tolerant. `commands` surface flipped CONSUMED. Wired into session-start. `docs/standards-refresh.md`. `tests/test-standards-refresh.sh` (17). Harness self-maintenance plane. Per ADR-0064. | `--significance` explains source→enforcement; `--configure` accepts m/h/d/w/mo + calendar, rejects malformed (2); cadence math drives refresh on first-run + after-interval, skips within-window, `--force` always; on-demand manual; missing entrypoint non-fatal; drives the real CTP initial-refresh.sh (offline-tolerant) + set-refresh-frequency.sh; `tests/test-standards-refresh.sh` 17/17; full audit chain green; `tests/test-all.sh` all suites; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-19** (cadence refresh; 17/17; ADR-0064). |
| TICKET-076 | Two-layer cadence alignment — CTP's fetch registry honors the operator interval too | Per 2026-06-19 operator follow-up (*"make sure both GCTP and CTP are re-scraping at the user-specified interval"*). ADR-0064's `--configure` used CTP's `set-refresh-frequency.sh` only as a validator (throwaway path), so CTP could still self-gate at its own default. Now `--configure <freq>` persists the validated cadence into CTP's own S-22 registry `.claude-tdd-pro/FETCH-FREQUENCIES.yaml` (the path CTP's fetch layer reads) via `set-refresh-frequency.sh`, aligning BOTH the GCTP drive cadence and CTP's per-source fetch cadence. `.claude-tdd-pro/` gitignored. Invalid freq writes neither layer. Round-trip verified against the real CTP command + resolver. `tests/test-standards-refresh.sh` 17→19. Prime directive intact (CTP's own command writes its registry; no CTP code edited). Per ADR-0065. | `--configure 6h` writes `.claude-tdd-pro/FETCH-FREQUENCIES.yaml` with the freq; invalid freq writes neither layer; `tests/test-standards-refresh.sh` 19/19; full audit chain green; `tests/test-all.sh` all suites; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-19** (two-layer alignment; 19/19; ADR-0065). |
| TICKET-077 | CL-A — `audit-applicable-rules.sh` gains g-md-* + `applies_to_prose` floor (ADR-0066 D-B) | Two additions to `scripts/audit-applicable-rules.sh`: (1) g-md-* joins the `.md` language-floor row (additive, backward-compatible); (2) NEW prose-projection dimension — any `.md` `may_edit` glob requires every `applies_to_prose:true` rule in `active.json` to be in `applicable_rules`. Both vacuous-pass before PROPOSAL-003 lands (no g-md-*/prose rules in the registry then). Harness self-maintenance plane. | 6 new named tests in `tests/test-audit-applicable-rules.sh` (20/20): md-underscope→red, full-union→green, prose-floor red/vacuous, extensionless-glob not gated, `.ts`-scope not triggered; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-19** (ADR-0066 D-B). |
| TICKET-078 | CL-B — `sarif-aggregate.sh` SARIF 2.1.0 bus + 23-test suite (ADR-0066 D-E) | Adds `scripts/sarif-aggregate.sh` merging per-rule SARIF 2.1.0 outputs from `.harness/audit/sarif/raw/TICKET-NNN/*.sarif.json` into one per-ticket log, preserving distinct `runs[]` per tool (markdownlint vs prose-judge vs cloud-guidance stay attributable). Flags `--ticket/--inputs/--output/--schema/--strict/--summary`; pragmatic validation now, full OASIS schema when the sarif namespace lands (PROPOSAL-003 wave 2). Exit 0 ok / 1 aggregation error / 2 strict+invalid. | 23 named assertions across the 8 ADR-0066 criteria (all green); fixtures under `tests/fixtures/sarif/`; two bounded rm patterns added to `tests/hook-security-baseline.txt`; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-19** (ADR-0066 D-E). |
| TICKET-079 | CL-C — design-phase MD gate `audit-design-phase-md.sh` (ADR-0066 D-D, phase 1) | Adds `scripts/audit-design-phase-md.sh` blocking `/dispatch` from emitting a request when an architectural `.md` proposes a design that would violate an `applies_to_prose:true` rule. Architectural detection: path heuristic (`docs/architecture/**`, `docs/adr/**`, `docs/decisions/**`) + `kind:` frontmatter. Per-rule verdict GREEN / DEVIATED (matching `deviations.md` row) / NOT_ENFORCED→RED when `prose-judge.sh` absent (no silent green, ADR-0066 D-C). Vacuous when no prose rules / no app_root / no reqs. Wired into `dispatch.md` (Claude+Cursor) as pre-emit step 5. | 11 named tests in `tests/test-audit-design-phase-md.sh` covering 7/8 ADR-0066 criteria; code-phase drift test deferred to PROPOSAL-003; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-19** (ADR-0066 D-D). |
| TICKET-080 | CL-D — operator docs for the design-phase MD gate | Three docs: `docs/deviations-template.md` (NEW — copyable per-rule-per-ticket deviation rows matching the gate heading `## Deviation — <RULE-ID> on <TICKET-ID>` + two worked examples); `docs/kata-runbook.md` (appended PATH C — rewrite the prose or file a deviation); `docs/first-time-guide.md` ("When dispatch is blocked by design-phase MD scoring"). All note the gate is vacuous-pass until the PROPOSAL-003 pin bump activates it. | Three docs present + cross-linked; gate workflow documented in plain English; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-19** (ADR-0066 CL-D). |
| TICKET-081 | CL-E — write-time + arch-gen enforcement surfaces | Closes the two enforcement-surface gaps the operator named ("enforce the rules when generating all architecture and at write time"). WRITE-TIME: extends the rubric-runner ext list in `.claude/hooks/post-tool-use-review-gate.sh` to add `.md .yaml .yml .tf .tfvars .json` (rubric runner fires post-Edit/Write at P0). ARCH-GEN: `.claude/hooks/session-start.sh` also invokes `audit-design-phase-md.sh` after `audit-applicable-rules.sh` (WARN policy). Both vacuous today; activate automatically on the PROPOSAL-003 pin bump. | 6 new tests in `test-post-tool-use-review-gate.sh` (14/14; each new ext vacuous-passes today, case branch verified); hook-security baseline unchanged; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-19** (ADR-0066 CL-E). |
| TICKET-082–085 | (reserved / superseded numbering — no work landed) | Numbers reserved by the 2026-06-20 kata re-validation (`1cf84bb`), which proposed "6 fix tickets (TICKET-082..087)" to remediate app-tree hygiene findings in the kata `app_root` via `/inner-loop`. That plan was superseded: 086 and 087 were reused for harness pin bumps (see below), and **no commit or `.harness/handoffs/` artifact was ever created under 082–085**. Recorded here so the ledger carries no unexplained gap between 081 and 086. | N/A — never materialized (no commit, no handoff artifact, no `active.json` change). **SUPERSEDED 2026-06-20** (overtaken by the pin-bump path; see commit `1cf84bb`). |
| TICKET-086 | Plugin pin bump `7a7f74d`→`39903da` — adopt PROPOSAL-003 / CTP-ADR-0007 | §15-gated pin bump adopting CTP CL-484..487 (§28.24-28.27): YAML/JSON/MD corpora + prose-as-code + write/arch-gen enforcement content, per `claude-tdd-pro/docs/handoff-gctp-adr-0007.md`. Activates the vacuously-authored CL-A..E wiring (TICKET-077..081) — vacuous-pass flips live on the next session start once `sync-plugin.sh --ensure` + `standards-sync.sh` re-materialize cache + `active.json`. Per ADR-0067. | Pin + lockfile hash updated; wiring surfaces activate on re-materialization; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-20** (pin `39903da`; ADR-0067). |
| TICKET-087 | CL-A — plugin pin bump `39903da`→`230e99d` — adopt CTP composite engine + auto-classification (ADR-0070) | Activates CTP §28.28–§28.39 (CL-488..499): CTP-ADR-0008 composite engine + 4-axis canonical vocabulary + CTP-ADR-0009 auto-classification pipeline + commercial-license gate + P-8 fix. CTP self-suite 4510/4510 green at pin. Integration-only: new entrypoints (composite-dispatch/composite-audit/enforce-file/sarif-aggregate) sit available but unwired; wiring lands CL-B..E (ADR-0068+0069). Binding no-rewrites discipline (§1 of ADR-0070) — `.harness/handoffs/*.req.json` NOT regenerated (prior mass-regen caused a 249-violation cascade). | Lockfile pin + `architecture-v1.9.md` hash bumped (ADR required); smoke gates green (applies-to-parity rules=118 parity_fail=0; commercial-license violations=0); 11 green audits + 3 documented deferred reds (205 applicable-rules / 57 cross-ref / 2 standards — all in ADR-0070 "Known follow-up"); `tests/test-all.sh` 32/33; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-23** (pin `230e99d`; ADR-0070). |
| TICKET-088 | CL-B — ADR-0068 W-A + W-B wiring (composite-engine consumer; marker-gated + narrowed) | Wires the GCTP-side composite-engine consumer; promotes ADR-0068 Proposed→Accepted with a hard-require paragraph (CTP-ADR-0008 binding: `required:true` tool absence is hard-fail, no silent fallback). W-A: 4-axis `applies_to` floor in `audit-applicable-rules.sh`, OPT-IN via `req.applies_to_floor_version >= 2` marker (legacy handoffs grandfathered on the 3 legacy floors — no-rewrites discipline). W-B: `--changed-files` narrowed mode in `enforce-standards.sh` invoking CTP `rubric/enforce-file.sh` per file, auto-wired in `audit-standards-enforced.sh`; closes ADR-0070 deferred #3 via narrowing. Extension-only; zero `.harness/*` state changes. | 5 W-A + 9 W-B new unit tests (25/25 + 22/22); audit chain 11 green + 3 documented deferred reds (205 / 57 / 5); `tests/test-all.sh` 32/33; ADR-0068 KFU documents the W-B prose-as-code coverage gap on TICKETS 002/003/006/007/010; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-23** (ADR-0068 W-A/W-B). |
| TICKET-089 | CL-C — ADR-0068 W-C + W-D + W-E (write-time hook + design-phase MD gate + deviation docs) | Wires the remaining 3 ADR-0068 deliverables, extension-only. W-C: `post-tool-use-review-gate.sh` invokes CTP `rubric/composite-dispatch.sh` on the written file with parse-then-block discipline (blocks only on an authoritative `status=red` summary line — bare exit codes non-authoritative, avoids false blocks from upstream bash bugs). W-D: `audit-design-phase-md.sh` invokes `composite-dispatch.sh` per architectural `.md` (auto-attaches the markdownlint+Vale+remark+prose-judge bundle). W-E: `deviations-template.md` + `first-time-guide.md` updated for the composite-engine rule-ID/stderr workflow. | Existing `test-audit-design-phase-md.sh` (11/11) + `test-post-tool-use-review-gate.sh` (14/14) cover the surfaces; audit chain unchanged vs CL-B (11 green + 3 deferred: 205 / 57 / 5); `audit-design-phase-md.sh` 126 dispatch-time violations (operator-closed via deviations/rewrite, not in pre-commit chain); no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-23** (ADR-0068 W-C/W-D/W-E). |
| TICKET-090 | CL-D — ADR-0069 W-F + W-G + W-I (operator-facing standards CLI + namespaces.yaml validation) | Wires the operator-facing surface for CTP's auto-classification pipeline (CTP-ADR-0009); promotes ADR-0069 Proposed→Accepted. W-F/W-G/W-I cover the `gctp standards` CLI (add-from-URL with cost preview + budget cap, review queue, inspect/accept/batch-accept/reject drafted rules, coverage reports, validate-before-deploy) and `namespaces.yaml` validation. Extension-only; zero `.harness/*` state changes. | Operator CLI surface + namespaces validation wired; audit chain green modulo the standing deferred reds; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-23** (ADR-0069 W-F/W-G/W-I). |
| TICKET-091 | CL-E — ADR-0069 W-H + commercial-license CI gate (final ADR-0068+0069 wiring CL) + P-9 proposal | Final ADR-0069 deliverable: W-H operator docs — `docs/operator-runbook.md` (NEW; 10-step "Adding standards from a URL") + `first-time-guide.md` worked example. Commercial-license CI gate (handoff §5.6): two new `.github/workflows/test.yml` steps after smoke — `audit-applies-to-parity.sh` (118/118 rules carry parity-preserving `enforced_by[]`) + `audit-commercial-license.sh` (bundled FOSS toolchain has zero copyleft/AGPL conflicts). Later addendum (2026-06-30, `7b4598b`): `docs/upstream-ctp-proposals.md` P-9 — consumer-compatibility contract, retrospective on ADR-0070. | Both CI gates green (applies-to-parity rules=118 parity_fail=0 unrouted=0; commercial-license bundled=4 tools=16 violations=0); operator-runbook present; P-9 entry present in upstream-ctp-proposals.md; no `claude-tdd-pro` path touched; D-6 clean. **DONE 2026-06-23** (ADR-0069 W-H); P-9 addendum **2026-06-30**. |
| TICKET-092 | Reconcile `TICKETS.md` ledger — backfill rows 077–091 (readiness CL-β) | The ledger stopped at TICKET-076 while commits landed through TICKET-091: ADR-0066 wiring (077–081), pin bumps (086 ADR-0067, 087 ADR-0070), composite-engine + auto-classification wiring (088–091) + the P-9 addendum. This CL backfills faithful rows from `git log`, documents the superseded 082–085 numbering (kata-fix plan `1cf84bb`, never landed), and re-contiguates the ledger. Docs-only; harness self-maintenance plane. First readiness CL ahead of the ADR-0072 pin bump to CTP `4668c2e` (β-first per operator directive, so this takes 092; epoch-gating CL-α becomes 093). | Rows 077–092 present + 082–085 gap documented; highest ledger ID == highest commit-referenced ID; full audit chain shows NO NEW reds vs the `230e99d` baseline (the 205 applicable-rules / 5 standards-enforced / cross-ref reds are pre-existing per ADR-0070); no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (readiness CL-β). |
| TICKET-093 | CL-α — ADR-0071 epoch-aware enforcement (shared `epoch-gate.sh` + wire all 17 audits + pin-keyed baselines) | Consumer-side dual of P-9 ("schema-additive with epoch + default"). New `scripts/_lib/epoch-gate.sh` centralizes two grandfathering mechanisms: (1) **pin-keyed baselines** `tests/<stem>-baseline.<pin>.txt` (generalizes the ADR-0032 approval-testing pattern; pin = epoch, read from the lockfile); (2) a **generalized epoch marker** `epoch_req_gated` (lifts W-A's `applies_to_floor_version >= 2` opt-in to a shared helper). Wired into **all 17** `audit-*.sh` per operator directive: Tier-1 baseline audits (`cross-references`, `hook-security`) route baseline resolution + new-vs-baseline diff through the lib (behavior-identical today via legacy-flat fallback); the other 15 source the side-effect-free API for uniformity/future-proofing. Re-baseline is an explicit pin-bump step (`docs/plugin-sync.md`). Roots the ADR-0070 retro-violation cascade ahead of the ADR-0072 bump. Harness self-maintenance plane. | `tests/test-epoch-gate.sh` 18/18 (hermetic); all 17 audits stdout+exit byte-identical to HEAD (side-effect-free wiring; Tier-1 verified via legacy fallback); `tests/test-all.sh` 34/36 — the 2 failures (`test-audit-cross-references` + `test-audit-hook-security`) are **pre-existing** stale-baseline reds on `main`, confirmed unchanged by this CL (CL-γ/CL-δ scope); ADR-0071 + `epoch-gate.sh` introduce zero new cross-ref reds; no `claude-tdd-pro` path touched (prime directive); `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (readiness CL-α; ADR-0071). |
| TICKET-094 | CL-γ — cross-reference baseline triage (grandfather 26 shipped-ADR / CTP-internal / app-tree refs) | Triaged the 26 NEW cross-ref reds surfaced post-composite-engine adoption (ADR-0070 "Known follow-up" #2). Verified **none is a real harness gap** — five categories (path prefixes omitted below so this row does not itself trip the audit): (a) illustrative glob/example prose in shipped **ADR-0066/0070** (adr/-glob, notes/foo.md, notes/random.md) — Nygard append-only, ADRs not edited; (b) **CTP plugin-internal** script paths named in ADR-0069/0070 (classify-from-url.sh, review-queue.sh, md-tokenize.sh) — the audit's backtick extraction drops the "CTP's" qualifier; the harness's own wrappers `scripts/gctp-standards-add.sh` + `scripts/gctp-standards-review.sh` exist; (c) **app-tree** paths (architecture/adr/0010 residency ADR + app-tree fixture-nested docs) that resolve in `app_root`, not the harness; (d) ADR-0066 **example** fixtures/scripts (req-md-only fixtures, dispatch.sh, test-dispatch.sh) never built under those names; (e) **deferred** docs named in ADR-0070 (compliance-profiles/eo-2026 + frontier-model-readiness). All added to `tests/cross-references-baseline.txt` (44→70) per the audit's documented grandfathering policy (ADR-0032). No cache-bound/skill-symlink reds baselined (none present with the cache materialized). Harness self-maintenance plane. | `audit-cross-references` 0 new vs baseline, exit 0; `tests/test-audit-cross-references.sh` 8/8 (was **pre-existing FAIL** on main); every one of the 26 verified illustrative/CTP-internal/app-tree/deferred (no real gap papered over); this ledger row introduces no new reds (missing paths named without extractable prefixes); no shipped ADR edited (Nygard); no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (readiness CL-γ). |
| TICKET-095 | CL-δ — standards-enforced divergence triage (grandfather 5 pre-epoch legacy claims via ADR-0071 baseline) | Closes ADR-0070 "Known follow-up" #3 / ADR-0068 KFU. The composite-engine re-run flags 5 divergences on **legacy kata tickets** (002/003/006/007/010): each res.json claims `g-aws-no-unrestricted-ingress=pass` on changed_files including ADR `.md`, but the W-B narrowed re-run routes `.md` through prose-judge → `not_enforced` without `LLM_JUDGE=1`. These are pre-epoch claims (written before the composite engine existed); rewriting them would violate the no-rewrites discipline (ADR-0070 §1) and `not_enforced` is still red. Applied the ADR-0071 mechanism: extended `scripts/audit-standards-enforced.sh` with epoch-baseline support (collect divergences → grandfather any in `tests/standards-enforced-baseline.txt` → only NEW divergences fail) and seeded the baseline with the 5. **Honest caveat:** grandfathering accepts an unverifiable-without-`LLM_JUDGE` claim; the real resolution is `LLM_JUDGE=1` (semantic moat) or an inner-loop re-run on those tickets — tracked follow-up, not closed by this CL. Harness self-maintenance plane. | `audit-standards-enforced` exit 0 (5 divergences all grandfathered as pre-epoch); with the baseline removed it still reports 5 NEW + exit 1 (behavior preserved); `tests/test-audit-standards-enforced.sh` 12/12 incl. 2 new hermetic cases (baselined divergence → grandfathered→0; **non-baselined divergence → still fails→1**, proving no masking); no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (readiness CL-δ; applies ADR-0071). |
| TICKET-096 | Readiness cleanup — refresh hook-security baseline (clear stale line-keyed entries + CL-α line shifts) | `test-audit-hook-security` was failing on `main` **pre-existing** (stale line-number-keyed baseline), and CL-α's epoch-lib source-block insertions shifted line numbers in 4 audits (the baseline keys findings as `file:line:content`), compounding the drift to 10 apparent-new findings. **Security-reviewed all 67** current S-3 findings — every one is a **bounded** `rm -f`/`rm -rf` on a `mktemp`-allocated or script-controlled temp var (`$..._sorted`, `$..._file`, `$TMP`, `$OUTDIR`, `$CLONE_DIR`, fixed test-artifact paths) with trap cleanup; **no eval/sudo/pipe/unbounded-path finding** — the same class already accepted in the prior 63-line baseline. Regenerated `tests/hook-security-baseline.txt` (63→67). **Follow-up noted:** the line-number keying is brittle (any edit re-flags unchanged patterns); content-keying would be more stable — deferred. Harness self-maintenance plane. | `audit-hook-security` exit 0 (67 accounted for); `tests/test-audit-hook-security.sh` 9/9 (was **pre-existing 2/9 FAIL** on main); every baselined finding verified bounded (no security regression swept in); no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (readiness cleanup; suite → 36/36). |
| TICKET-097 | CL-ε — architecture-v1.9 drift review `230e99d`→`4668c2e` (feeds ADR-0072 §15) | Produced `docs/architecture-v1.9-drift-review-230e99d-to-4668c2e.md` for the forthcoming pin-bump ADR. Non-mutating method: blobless scratch clone of CTP (plugin cache untouched — still `230e99d`, prime directive). Old-side hash verified against the lockfile (`b9051de8…`). **Verdict: purely additive, LOW bump risk** — 0 deletions, +346 lines, §28.40→§28.69 (30 appended subsections); §§1–27 core unchanged; §2 contract registry **unchanged** (§2.30/2.31/2.32 already present at `230e99d`); all 4 other contract-surface files (`CLAUDE.md` + the 3 consumed SKILLs) **byte-identical**. Catalogs the added capabilities (80-tool toolchain §28.42, single-config surface §28.44–52, native fallback + universal enforcer §28.56–57, govern-before-write + both-paths pre-write §28.60/68, co-design + path-tagging + agnosticism §28.62–64, EO-security build-out §28.54) and the Consumer Compatibility Contract §28.40 = the CTP side of P-9 (validates ADR-0071). Consumer-impact map → CL-B..N. Harness self-maintenance plane. | Drift doc present; additive-only evidenced (0 deletions; skills byte-identical); plugin cache NOT mutated (`230e99d`); the doc's `architecture-v1.9.md` reference baselined (precedent: 3 existing sources — ADR-0025/0041 + plugin-sync.md); no other new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (readiness CL-ε; feeds ADR-0072). |
| TICKET-098 | CL-A — plugin pin bump `230e99d`→`4668c2e` (adopt CTP v1.18 §28.40–§28.69; ADR-0072) | §15-gated pin bump adopting CTP v1.18 (**58 commits**, §28.40–§28.69: Consumer Compatibility Contract + 80-tool composite engine + single-config surface + native fallback/universal enforcer + govern-before-write + both-paths pre-write + co-design/path-tagging/agnosticism + EO-security build-out). **Purely additive** contract change (only `architecture-v1.9.md` hash `b9051de8…→a2c34ace…`; other 4 contract files byte-identical; 0 deletions; §§1–27 + §2 registry unchanged). **Integration-only** — new surfaces available but unwired; wiring lands CL-B..N. `active.json` regen 118→118 rules, **now all epoch-tagged** (`introduced_in`) = §28.40 (CTP side of P-9), forward-aligning ADR-0071. Records the operator §4 decision (**move tool-based enforcement to pre-write**; implemented CL-B..N). No handoff regen (no-rewrites). Lockfile edited by hand (`--update` refused on contract drift, by design). Harness self-maintenance plane. | `sync-plugin --check` **0 drift** (pin matches HEAD); smoke gates green (applies-to-parity rules=118; commercial-license **tools=80** violations=0); audit chain green except the standing **205** legacy applicable-rules (WARN, pre-epoch); `standards-enforced` + `cross-references` baselines **valid across the bump** (ADR-0071 grandfathered — no re-key needed); `tests/test-all.sh` **35/36** (1 deferred red — `test-audit-design-phase-md`, the CL-C W-D `.md`-bundle activation; dispatch-time WARN gate, documented in ADR-0072 "Known follow-up" #1 → CL-B..N); no `claude-tdd-pro` path edited (cache only re-materialized, prime directive); `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (pin `4668c2e`; ADR-0072). |
| TICKET-099 | Dependency-closure test-result caching — cache a suite until its code-under-test changes (ADR-0073) | Per operator directive (*"unit tests should be cached after the first run until the function(s) it tests are updated or deleted; same for integration tests"*). New `scripts/_lib/test-cache.sh` computes a per-suite **closure hash** = test file + referenced `scripts/*.sh` + their sourced `_lib/*.sh` + the plugin **pin** (`epoch_current_pin`); `tests/test-all.sh` serves a **passing** suite from cache when its closure is unchanged (default-on; `--no-cache` CI-safe escape + `--clear-cache`). File-granularity. **External epoch = pin only, NOT `active.json`** — `standards-sync` regenerates active.json non-deterministically (byte-unstable, proven this session), so it can't be a stable key; the pin captures the dominant driver (e.g. the `design-phase-md` verdict flip on the 4668c2e bump). Only passing results cached; a failing suite always re-runs. Cache in gitignored `.harness/test-cache/`. Mirrors CTP §28.53. Harness self-maintenance plane. | `tests/test-test-cache.sh` **13/13** hermetic (determinism; sensitivity to test/script/transitive-lib/**pin**; active.json proven NOT to affect the hash; lookup/store/clear); closure hash **stable across a standards-sync regen** (the root-cause fix); end-to-end 2 runs — run 2 serves **36/37 from cache**, only the failing `design-phase-md` re-runs; hook-security baseline refreshed for the 3 new bounded-`rm` patterns (67→70, security-reviewed); no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (ADR-0073). |
| TICKET-100 | CL-B — fix design-phase-md gate: parse-then-block the W-D composite-dispatch invocation (extends ADR-0068; files P-10) | First wiring CL adapting to the `4668c2e` composite engine; closes the ADR-0072 deferred red (`test-audit-design-phase-md`). **Root cause:** CTP's `rubric/composite-dispatch.sh` crashes on macOS bash 3.2 (`line 119: ra[@]: unbound variable` — empty-array expansion under `set -u`), exiting 1; the W-D block treated a bare exit 1 as a P0 → **false red** on architectural `.md`. **Fix:** applied the parse-then-block discipline W-C already established (ADR-0068 W-C in `post-tool-use-review-gate.sh`) to the W-D path in `scripts/audit-design-phase-md.sh` — it now reds ONLY on an authoritative `composite-dispatch … status=red` summary line, never on a bare crash exit. The `applies_to_prose` semantic verdict is already provided by the prose-rule loop, so no coverage is lost. Filed the CTP crash upstream as **P-10** (prime directive: no cross-repo edit — the harness defends + proposes). Harness self-maintenance plane. | `tests/test-audit-design-phase-md.sh` **13/13** (11 prior + 2 new: authoritative `status=red` → 1; bare crash exit → 0, proving parse-then-block does not over-suppress real verdicts); full suite **37/37** (CL-A deferred red cleared); P-10 filed in `docs/upstream-ctp-proposals.md`; no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (CL-B; extends ADR-0068 W-D). |
| TICKET-101 | CL-I — development-path tagging consumer: validate every rule resolves to iac/fullstack/both (ADR-0074) | Second wiring CL (CL-C..N backlog, ADR-0072 KFU #2); adopts CTP **§28.63** `commands/classify-path.sh`. New `scripts/audit-development-paths.sh` runs `classify-path.sh --audit` and reds if any rule is **unpathed** (a both-paths-partition gap per §28.68); vacuous on a pre-§28.63 cache or missing Ruby prereq (never crashes the WARN chain). Wired into `session-start.sh` (WARN) + `test-all.sh`; sources the epoch lib (ADR-0071 uniformity). The path-tag **consumption** (per-file iac+both vs fullstack+both) is deferred to CL-C (the pre-write governor) — this CL delivers the *validation* that the partition is total. Prime directive: classify-path.sh consumed by reference, never edited. Harness self-maintenance plane. | `tests/test-audit-development-paths.sh` **7/7** hermetic (help/unknown-flag; absent→vacuous; unpathed=0→green; unpathed>0→red; no-summary/Ruby-missing→vacuous; multi-digit unpathed); live run green (`classify-path total=118 … unpathed=0`; 42 iac / 30 fullstack / 46 both); hook-security baseline refreshed for the new test's bounded-`rm` trap; no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (CL-I; ADR-0074). |
| TICKET-102 | CL-C — pre-write govern-before-write governor: PreToolUse deny gate + §4 tools→pre-write (ADR-0075) | The headline wiring CL. New `.claude/hooks/pre-tool-use-govern.sh` (registered PreToolUse in `.claude/settings.json`, matcher `Edit\|Write\|MultiEdit`) governs proposed content **in memory before write**: (1) **native gate** delegates to the plugin's `enforce-standards-pre-write.sh` (§28.60) — P0/P1 → **deny (exit 2)**; (2) **§4 tools half** (operator decision) runs `composite-dispatch` on the reconstructed content with parse-then-block (ADR-0068 W-C) — forward-ready but **inert on bash 3.2 until P-10**. **Scope:** app_root only (agent-operating-compact — harness self-maintenance EXEMPT); **vacuous no-op in this repo** (no `.harness/app.json`), so registering it changes nothing here and never blocks harness edits. Fail-open throughout. Prime directive: plugin scripts consumed by reference, never edited. Harness self-maintenance plane. | `tests/test-pre-tool-use-govern.sh` **8/8** hermetic (non-edit→allow; no app_root→vacuous; outside app_root→exempt; inside+native-deny→2; §4 authoritative status=red→2; §4 crash→allow [P-10 inert]; native+composite-green→allow; unparseable→fail-open); live hook **vacuous** here (no app_root); `.claude/settings.json` valid JSON; hook-security baseline refreshed for the new bounded `rm` (reviewed); no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-06-30** (CL-C; ADR-0075; §4 tools half gated on P-10). |
| TICKET-103 | CL-D — on-save enforcement: app_root-scope the composite-dispatch corpus path; `enforce-standards-on-save.sh` intentionally not wired (ADR-0076) | Investigation finding: the on-save backstop is **already wired** via `post-tool-use-review-gate.sh` (W-C `composite-dispatch` + `runner`), which is a **superset** of `enforce-standards-on-save.sh` (native-only `enforce-file.sh`) — so that entrypoint is **intentionally NOT wired** (a redundant 2nd PostToolUse hook). Gap fixed: the on-save `composite-dispatch` (CTP app-architecture corpus `g-aws`/`g-k8s`/…) was **not app_root-scoped** — post-P-10 it would govern harness self-maintenance (compact violation, inconsistent with CL-C). Now app_root-scoped (matches CL-C); `rubric/runner` stays **harness-wide** (code-quality/TDD plane; ADR-0037 preserved) — clean plane separation. Also fixed `ABS_FILE` to handle absolute sibling-app_root paths. Forbidden-path fence unchanged (all writes). Vacuous in this repo (no app.json). Harness self-maintenance plane. | `tests/test-post-tool-use-review-gate.sh` **17/17** (14 prior + 3 new: composite fires under app_root → deny 2; skipped outside app_root → 0; no app_root → never fires 0; stubbed via `PTU_PLUGIN_ROOT`/`PTU_APP_ROOT`); hook-security baseline refreshed for the new test's bounded `rm` trap; no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-07-01** (CL-D; ADR-0076). |
| TICKET-104 | CL-E+F — per-file `--single-file-gate` + audit-time whole-tree gate `audit-app-tree.sh`; CL-G already present; app_root record correction (ADR-0077) | Batched composite-engine consumption (per batch-CL). **CL-E:** thread `--single-file-gate` into `enforce-standards.sh` W-B per-file re-verification (skip tree-context rules; support-guarded for older caches). **CL-F:** new `scripts/audit-app-tree.sh` wrapping `composite-audit.sh --root <app_root>` — the audit-time whole-tree strict gate (complement to CL-C/CL-D per-file hooks); app_root-scoped; parse-then-block (crash/P-10 → vacuous, never false-red); **NOT session-start-wired** (composite-audit walks a whole tree = slow → on-demand/CI). **CL-G:** SARIF 2.1.0 bus already present (`scripts/sarif-aggregate.sh`, TICKET-078) — closed as-present. **Record correction:** `.harness/app.json` IS configured (app_root=`../softarchcert-win25`), so the CL-C/CL-D governors are app_root-**scoped + active** for that tree (native live, tools inert until P-10), **exempt** for harness self-maintenance — the earlier "vacuous/no app.json" wording was imprecise (safety unchanged). Prime directive: entrypoints consumed by reference. | `tests/test-audit-app-tree.sh` **8/8** (help/unknown; no-app_root→vacuous; composite-audit absent→vacuous; green→0; red→1; incomplete→advisory; crash→vacuous); `--single-file-gate` no regression (standards-enforced still 5 grandfathered); `test-enforce-standards` + `test-audit-standards-enforced` pass; live composite-audit tiny-tree `status=green`; hook-security baseline refreshed; no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-07-01** (CL-E+F; ADR-0077; CL-G already present). |
| TICKET-105 | CL-H — single-config OPTIONS-surface completeness gate (`config-sync --check`); profile-honoring deferred (ADR-0078) | Adopts the §28.44–§28.52/§28.58/§28.59 single-config surface. New `scripts/audit-config-surface.sh` runs CTP `config-sync --check` and reds if any active rule lacks projectable tool options (`needs_mapping>0` — "capability present, data empty"); vacuous on a pre-§28.58 cache / missing Ruby; session-start WARN (fast ~166ms); sources epoch lib. Live: `config-sync rules=118 materialized=118 needs_mapping=0`. **Scope:** validates the OPTIONS surface is total. **Deferred (needs operator UX → ADR-0079):** (a) profile SELECTION — which of `profiles/` (financial/government/healthcare/…) a product applies + where configured; (b) threading `--profile` through the enforcement paths (enforce-standards/CL-C/CL-D) so a selected profile is honored at enforce-time. Prime directive: config-sync.sh consumed by reference. Harness self-maintenance plane. | `tests/test-audit-config-surface.sh` **7/7** (help/unknown; config-sync absent→vacuous; needs_mapping=0→green; >0→red; no-summary/Ruby-missing→vacuous; multi-digit); live green (118 rules, needs_mapping=0, ~166ms); hook-security baseline refreshed; no new cross-ref reds; no `claude-tdd-pro` path touched; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-07-01** (CL-H; ADR-0078; profile-honoring → ADR-0079). |
| TICKET-106 | GCTP→CTP handoff — P-10 composite-dispatch bash 3.2 crash (unblock routed-tool effectiveness the rule-compliant way) | Operator directive: unblock P-10 following the architectural rules. Verified there is **no harness-side unblock** — no newer bash on the machine (`/bin/bash` 3.2.57 only), and editing the plugin/cache violates the prime directive. The rule-compliant path is: CTP fixes `composite-dispatch.sh` in the `claude-tdd-pro` repo, then GCTP re-pins. Prepared a self-contained **GCTP→CTP handoff** (`docs/handoff-ctp-p10-composite-dispatch-crash.md`) for the CTP chat to assess: exact site (empty-array `"${ra[@]}"`/`"${toa[@]}"` under `set -u`, lines 116-119), deterministic repro, consumer impact (pre-write/on-save/audit-time tools paths inert, native unaffected, parse-then-block keeps GCTP safe), proposed guard (`"${ra[@]+"${ra[@]}"}"`) conforming to the `tdd-pro-bash32-portability` skill, and the re-pin coordination. Mirror of the CTP→GCTP handoffs that opened this adoption. Docs-only; harness self-maintenance plane. | Handoff doc present + self-contained; no harness-side plugin/cache edit (prime directive); P-10 (`docs/upstream-ctp-proposals.md`) remains the standing proposal record; no new cross-ref reds; `git diff docs/founder-directives.md` == 0 (D-6). **DONE 2026-07-01** (GCTP→CTP handoff; awaits CTP fix + GCTP re-pin). |

## Conventions

- Commit message format: `TICKET-NNN: <verb> <object>`
- One ticket per CL. If scope grows mid-ticket, split into TICKET-NNN.a / .b rather than expanding the original.
- Closing a ticket means the "Done when" column passes and is verified in the commit body.
