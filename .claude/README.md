# .claude/

Status: **stub — filled in by TICKET-004**.

This directory will hold the Claude Code harness config that loads the three `tdd-pro-*` skills (cl-workflow, batch-cl, bash32-portability) from the sibling `claude-tdd-pro` repo into prototype-repo sessions.
