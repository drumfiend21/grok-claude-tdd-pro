# Skill Consumption

Status: **stub — filled in by TICKET-004**.

This file will document how the three `tdd-pro-*` skills from `claude-tdd-pro/.claude/skills/` are surfaced inside prototype-repo `claude -p` sessions (path resolution, version pinning, invocation prompts).
