# claude-tdd-pro plugin lock file
#
# This file is the source-of-truth pin for the upstream claude-tdd-pro plugin
# consumed by this repo. It is the operational form of architectural rule R-2
# ("versioned consumption, by reference") and the unit-of-bump for rule R-5
# ("contract changes are bilateral and explicit").
#
# Update via:  scripts/sync-plugin.sh --update
# Check via:   scripts/sync-plugin.sh --check  (run automatically at SessionStart)
#
# Any --update that changes a contract_surface_files entry requires an ADR.
# See: docs/architecture-principles.md §15, docs/plugin-sync.md
#
# DO NOT edit by hand unless you also write an ADR.

schema_version: 1

upstream_repo:   https://github.com/drumfiend21/claude-tdd-pro
pinned_branch:   main
pinned_commit:   b8866583c5922ce6bfb2e04e5e1c6c7912ad1a74
pinned_at:       2026-07-10T22:24:38+00:00
pinned_message:  "Merge: §31 reconcile consult output to GCTP consumer contract (families_active/project_id/project_overlay_namespaces)"

# The contract surface — files in upstream this repo's architecture depends on.
# Each entry is hashed at pin time; sync-plugin.sh re-hashes upstream HEAD and
# compares. Hash drift on ANY of these files = a contract-surface change =
# requires an ADR before bumping the pin.
contract_surface_files:
  - path:   CLAUDE.md
    sha256: 814e43c20a0809582f56d4d597d20d2993ff51e180823ffac07f6cfb579c48da
  - path:   docs/architecture-v1.9.md
    sha256: 8caadb1427d46897e75d13e105fe883ecd9e07ca98c2c0df947fe991f3298f13
  - path:   .claude/skills/tdd-pro-cl-workflow/SKILL.md
    sha256: 82e6dc5d7e827e0bd83080a63869ce5de13a04c1570aeb2a794607ba8a4de924
  - path:   .claude/skills/tdd-pro-batch-cl/SKILL.md
    sha256: 9a004131782faf3477900984f26f36e6d1997cca7f04e6b672552048d1fd65b4
  - path:   .claude/skills/tdd-pro-bash32-portability/SKILL.md
    sha256: 10d07ac999b194b63291511ef8423ec2ef72012ec3d82c7ca6615944d0d427a7

last_synced_at:      2026-07-10T23:00:00Z
last_synced_session: "Pin bump 11126a8→b886658 (CTP CL-556..CL-560 covering S-58 family-registry + S-59 classifier widening + S-60 acquire pipeline + S-61 technology-fitness recommender + S-62 promotion PR flow + S-63 _project/ foundation at generated-code-quality-standards/_project/<id>/ + S-64 removal + CL-560 consumer-contract reconciliation aligning classify output to GCTP pre-wired validator — P-15 ADOPTED; per ADR-0092. architecture-v1.9.md +112/-0 additive §31 amendments; other 4 contract files byte-identical; active.json to be regenerated; Delta C ruled `umbrellas:` on registry tech entry / `families_active[]` on runtime profile — GCTP pre-wired for exact runtime name. Prior message retained below for chain continuity: Pin bump f39fcdc→11126a8 (CTP CL-550 §30.4 classify-from-answers + CL-551 §30.5 stack-driven progressive rule activation + CL-552 §30.6 stack entry-shape + idempotency alignment — P-13 four halves ADOPTED; per ADR-0091. Prior message retained below for chain continuity: Pin bump 43ea692→f39fcdc (CTP CL-549: §30.3 word-boundary classifier precision — signal matching changed from substring `hay.include?(sig)` to word-boundary `(?<![a-z0-9])<sig>s?(?![a-z0-9])` so `aks` no longer matches inside `leaks` and `ci` no longer matches inside `certification`/`accreditation`; alphanumeric boundaries preserve phrase matches like `amazon web services` and internal punctuation like `ci/cd`; optional trailing `s` preserves plural matches like `microservices`; classifier can only TIGHTEN (fewer false-positive types) never loosen; no schema/contract-key change — v1.1 business-profile shape identical; classifier now yields the clean pre-flight the KATA needs (real AI-credentialing prose → workload_types=[ai-governed, baseline-quality], no phantom azure-platform/container-orchestration/ci-cd); additive: 8× cl549-precision-*.json specs + §30.3 architecture amendment; also chore: .gitignore guard so consult/kata submission output can never leak into the plugin — run consult with --out into your own submission tree; architecture-v1.9.md +10/-0 §30.3 amendment; 3× SKILL.md + CLAUDE.md byte-identical) per ADR-0090."
