# Upstream proposals for claude-tdd-pro (CTP)

Per the prime directive (`CLAUDE.md`), this harness **consumes** CTP by pinned reference
and **never edits it in place**. When harness work or testing surfaces something that
should change in CTP, it is filed here as a **v1.11 amendment proposal** and routed to the
`claude-tdd-pro` repo separately — it is NOT patched from this repo.

Each entry: the finding, the evidence, and the proposed upstream fix. Status is tracked
until the corresponding CTP change lands and a pin bump (ADR-gated) adopts it.

---

## P-1 — `install.sh` bash 3.2 unbound-variable crash on a clean install (`conflicts[@]`)

- **Status:** ✅ ADOPTED — fixed in CTP CL-476 (§28.16); adopted at GCTP pin `6d2fe13` (ADR-0054). The installer now guards `${#conflicts[@]} -gt 0` before expanding the array.
- **Found:** 2026-06-15, fresh-machine install test of CTP's `install.sh` (CTP v0.4.0+).
- **Symptom:** first run in a clean directory prints
  `ctp-install.sh: line 233: conflicts[@]: unbound variable`.
- **Cause:** `printf '%s\n' "${conflicts[@]}"` under `set -u` when the `conflicts` array is
  empty (zero conflicts) — the classic bash 3.2 empty-array gotcha. Non-fatal here (script
  uses `set -uo pipefail`, not `-e`), so install proceeds, but it fires on exactly the
  fresh-empty-folder case, on macOS's default bash 3.2.
- **Proposed fix:** the standard guard — `"${conflicts[@]+"${conflicts[@]}"}"` (or seed the
  array / test length before expansion). Note: CTP ships a `tdd-pro-bash32-portability`
  skill that documents this exact gotcha; the installer should conform to it.

## P-2 — Ruby preflight is a hard wall on common targets (`exit 3`, nothing installed)

- **Status:** ℹ️ NOT-A-DEFECT (by design) — CTP review confirmed `preflight_check` intentionally hard-stops (`exit 3`) on missing/old ruby. The harness keeps the loud ruby warning in `docs/first-time-guide.md` (Path B) so operators on no-ruby cloud / stock-macOS targets know before they run the one-liner. No CTP change.
- **Found:** 2026-06-15, same test + a no-ruby simulation.
- **Symptom:** missing or `< 3.0` ruby → preflight `exit 3` before anything installs.
  Two real-world bites: (a) **Claude Code's web/cloud sandbox has no ruby** → dead stop;
  (b) **stock macOS ships ruby 2.6.10** (`/usr/bin/ruby`) → fails unless a newer ruby
  (e.g. Homebrew's) is ahead on PATH.
- **Proposed fix:** at minimum, make the ruby dependency loud *before* the one-liner is run
  (README/QUICKSTART), and consider a ruby-optional path that installs the non-ruby features
  and clearly reports which capabilities are gated on ruby — rather than an all-or-nothing
  `exit 3`.

## P-3 — `architect` is not a Claude Code slash command (no `commands/architect.md`)

- **Status:** ✅ ADOPTED — fixed in CTP CL-476 (§28.16); adopted at GCTP pin `6d2fe13` (ADR-0054). CTP added `commands/architect.md` (loads the `architect` skill, mirroring `onboard.md`), so `/architect` is now a real slash command. `docs/first-time-guide.md` restored to list it (TICKET-059).
- **Found:** 2026-06-15, cache inspection at pin `3432b52`.
- **Symptom:** `architect` ships as `agents/architect.md`, `skills/architect/SKILL.md`, and
  `commands/architect.sh`, but there is **no `commands/architect.md`** — so `/architect`
  never registers as a slash command. (The verified `.md` slash commands are `/analyze`,
  `/feature`, `/onboard`, `/doctor`, `/spec`, `/plan-first`, `/pr`, `/remediate`, …; `/help`
  is likewise script-only.)
- **Proposed fix:** if `/architect` is meant to be user-invokable, add `commands/architect.md`
  (delegating to the architect skill/agent). Otherwise document that "architect" is a skill
  triggered by describing an architecture need, not a slash command.
- **Harness-side action taken:** `docs/first-time-guide.md` corrected to stop telling users
  to type `/architect` (TICKET-058).

## P-4 — Standalone installer is Cursor-targeted; doesn't register with Claude Code

- **Status:** ℹ️ CLARIFIED (not a CTP defect) — CTP review notes its `commands/*.md` are Claude-Code-format slash commands; the Cursor-orientation GCTP's test saw is downstream installer packaging, not a missing command surface. The harness docs keep the empirically-verified note that the *standalone installer* (Path B) is Cursor-oriented. No CTP change required; revisit only if a `--with-claude-code` registration path is desired.
- **Found:** 2026-06-15, fresh-machine install test (Path B).
- **Symptom:** CTP's `install.sh` writes `.cursorrules` + a hooks `.claude/settings.json`,
  installs commands under `~/.claude-tdd-pro/commands/`, and its success line says "open in
  Cursor." It does **not** create a project `.claude/commands/` or otherwise register CTP as
  a Claude Code plugin, so CTP's slash commands do not surface in a plain Claude Code chat —
  only in Cursor.
- **Proposed fix:** either add a Claude-Code registration path (project `.claude/commands/`
  or plugin-marketplace entry) so `--with-claude-code` surfaces the commands there, or scope
  the docs to state the installer is Cursor-oriented.
- **Harness-side action taken:** `docs/first-time-guide.md` now states slash commands are a
  Cursor surface and that Claude Code uses the skills + hooks (TICKET-058).

---

## P-5 — Fix E: external-tree enforcement entrypoint (`rubric/enforce.sh`)

- **Status:** ✅ ADOPTED — shipped in CTP CL-477/478 (§28.17–28.18); adopted at GCTP pin `eb7b2af` (ADR-0058).
- **Found:** 2026-06-18/19, GCTP O'Reilly kata build (`softarchcert-win25`): only IaC was actually
  enforced by CTP detectors; TS code was scoped to two rules and never re-run (asserted, not enforced).
  Root cause was CTP-side: no single entrypoint dispatches a rule set against an external app tree.
- **GCTP corrections incorporated (invisible from inside CTP):** (1) dispatch by the
  `generated-code-quality-standards/` catalog, NOT `RUBRIC.yaml` — the bare ids collide (`g-ts-001` =
  `no-any` in the catalog vs `naming-style` in RUBRIC.yaml); (2) **4-state** per rule
  (`pass | fail | not_applicable | not_enforced`) + a `files_evaluated` count, so a rule that ran but
  matched no files is `not_applicable` (neutral) not a vacuous `pass`, and an un-run detector is
  `not_enforced` (red) not a pass; (3) generalize the proven `cloud-guidance-rule.sh --rule/--root`
  contract up to a dispatcher over all detectors.
- **Adopted contract:** `enforce.sh --root <app> --rule <id>… | --rules <csv> [--paths <glob>] [--json]`;
  exit `0` all-pass-or-NA / `1` any fail / `2` usage·unknown / `3` ≥1 not_enforced. This is the surface
  the GCTP-side Fix B (a forthcoming `enforce-standards.sh`) + Fix C (dynamic re-run gate) build against.

## P-6 — Fix F: prose enforcement (ADR-structural + citation detectors)

- **Status:** ✅ ADOPTED — shipped in CTP CL-480 (§28.20, option 1); adopted at GCTP pin `eb7b2af` (ADR-0058).
- **Found:** same kata: CTP enforced code + IaC but **prose** (ADRs/design docs — the bulk of an
  architecture submission) had no detectors; it could only be cited, never content-enforced.
- **Adopted:** a `documentation` namespace with `g-doc-001` (ADR structural conformance) + `g-doc-002`
  (doc-citation-presence), emitted **through `generated-code-quality-standards/`** so they sync into
  `active.json` and GCTP can scope them; `enforce.sh` maps `g-doc-*` → `*.md`. (GCTP Correction 4: doc
  detectors must flow through the catalog to reach the contract — honored.)

## P-7 — Fix G: `no-any` comment/string false positive

- **Status:** ✅ ADOPTED — shipped in CTP CL-479 (§28.19); adopted at GCTP pin `eb7b2af` (ADR-0058).
- **Found:** same kata: the reported TS "fails `no-any`" was a single finding on a *comment*
  (`// Fail-closed: any policy error…`); the grep matched `: any` inside the comment text (0 real `any`
  annotations; corroborated by GCTP's independent detector run).
- **Adopted:** the detector strips `//` line comments (line-number-preserving) before the `: any` greps;
  the `// allow-any:` affordance and real `x: any` flagging are unchanged.

---

## Routing

These are tracked here for the harness record. The actual fixes land in `claude-tdd-pro`
as amendments; once they ship, a pin bump (ADR per `docs/architecture-principles.md`
§15) adopts them and the relevant `Status` above flips to ADOPTED with the pin SHA.

The GCTP-side counterparts of the kata-feedback loop — **Fix A** (decompose-union), **Fix B**
(`enforce-standards.sh` driving `enforce.sh`), **Fix C** (dynamic re-run gate), **Fix D** (`app_root`
external-working-tree model) — are NOT upstream items; they land in this repo per
`proposals/PROPOSAL-002-app-enforcement-spine.md`.

---

## P-8 — `prose-judge.sh` tier-2 invocation contract-mismatch with `llm-judge.sh` (semantic tier non-functional)

- **Status:** ✅ ADOPTED — fixed upstream at CTP pin `230e99d` (CL-A pin bump per ADR-0070). `llm-judge.sh` ships the `--text <prose>` flag the wrapper needs; semantic tier is now functional under `LLM_JUDGE=1`. Re-running the 2026-06-20 kata to convert the 28 stuck `not_enforced` verdicts into semantic verdicts is operator-driven going forward (no harness substrate work needed).
- **Status (historical):** 🟥 OPEN — surfaced 2026-06-20 during GCTP architecture re-validation under the post-pin-bump 118-rule surface (ADR-0067 / CTP-ADR-0007). LLM_JUDGE=1 across 33 architectural docs yielded zero verdict changes; root cause was a flag-name mismatch in the CTP substrate, not an LLM problem.
- **Found:** 2026-06-20, attempting to convert 28 `not_enforced` verdicts on the kata's architectural `.md` files into explicit semantic verdicts via `LLM_JUDGE=1 rubric/enforce-file.sh --file <doc>`.
- **Symptom:** Every `not_enforced` verdict stayed `not_enforced` regardless of `LLM_JUDGE=1` + `claude` CLI on PATH + `--no-stream -p` working in isolation. Per-file runtime was ~1s (consistent with tier-2 skip, not a model call). Final tally with LLM_JUDGE=1: **3 green / 1 red / 29 incomplete** — identical to LLM_JUDGE=0.
- **Cause:** `rubric/detectors/prose-judge.sh` tier-2 invokes the LLM judge with `--text <prose>` (line ~102):
  ```ruby
  out = `LLM_JUDGE=1 bash #{File.join(plugin,"rubric","detectors","llm-judge.sh")} --rule #{rule} --text #{prose.inspect} 2>/dev/null`
  ```
  But `rubric/detectors/llm-judge.sh` only accepts `--target <file>` + `--rule <rule-id>` + `--model <name>` + `--dry-run` + `--explain`. The `--text` flag falls through to the unknown-arg path → `exit 2`. With stderr suppressed, the prose-judge case statement's regex `/\bYES\b|\bNO\b|\bABSTAIN\b/i` matches nothing → falls through to `verdict="not_enforced"`. The semantic tier is dead-coded.
- **Evidence:** `prose-judge.sh:102` (the `--text` invocation) vs `llm-judge.sh` argument parser (`case "$1" in --target | --rule | --model | --dry-run | --explain | * (echo "llm-judge: unknown arg")`). Direct repro on a clean install of the pin `39903da`.
- **Proposed fix (CTP-side, smallest possible):** add `--text <prose>` to `llm-judge.sh`'s argument parser as an alternative to `--target <file>` — when `--text` is given, skip the file-read step and use the inline prose verbatim. The rest of the dispatch (model selection, response parsing) is reusable. Roughly 5 lines of bash. Alternative: change prose-judge.sh tier-2 to write the prose to a tempfile and call `--target <tempfile>` (preserves llm-judge.sh's existing contract; ~3 lines of Ruby).
- **Workarounds operator-side (until fix):**
  1. **Allow-affordance comments** per ADR-0066 D-F for the 1 keyword-tier false-positive (ADR-0010 `0.0.0.0/0` deny-context citations).
  2. **Deviation rows** for any rule the operator decides genuinely cannot apply to a given ticket.
  3. **Accept `not_enforced`-as-RED** per ADR-0066 D-C until tier-2 ships fixed — the gate is honest (no silent green), just over-broad.
- **Impact on GCTP work:** ADR-0066 wiring (CL-A through CL-E) and ADR-0067 (pin bump) are unaffected — the gates correctly surface `not_enforced` per the no-silent-green invariant. The operator's expectation that LLM_JUDGE=1 would clear the 28 incompletes (recorded in `docs/kata-architecture-revalidation-2026-06-20.md`) cannot be satisfied until this fix lands upstream.

## P-9 — Consumer-compatibility contract on rule-schema-touching CTP ADRs (close the schema-additive ≠ state-additive gap)

- **Status:** 🟥 OPEN — filed 2026-06-25, retrospective on the `39903da → 230e99d` pin bump (ADR-0070 + CL-B..CL-E).
- **Found:** 2026-06-25, post-mortem on ADR-0070's three documented deferred reds at CL-A close:
  1. `audit-applicable-rules.sh` — 205 violations on TICKETS 001..014 (new `applies_to_prose: true` floor on 9 rules silently retro-required every legacy `.md`-scoped req.json to carry them);
  2. `audit-cross-references.sh` — 57 NEW broken refs / 35 baseline rows obsoleted (new top-level plugin entries + ADR-0068/0069 references to plugin-internal scripts);
  3. `audit-standards-enforced.sh` — TICKET-042 divergence (smoke fixture asserts `pass` blindly; the post-bump composite-engine detectors actually find things on the contaminated `app_root`).
- **Symptom:** every red after CL-A traces to one missing concept in the handoff. The handoff was **"consumer-compatible at the CLI signature layer"** (handoff §3.5: `enforce.sh` 4-state contract preserved byte-identically) but **NOT "consumer-compatible at the enforcement-state layer"** (the consumer's `active.json`-driven floor logic implicitly versioned against the pre-bump rule shape). These are different things and CTP-ADR-0008's design doc didn't separate them.
- **Cause (analytic — what's "additive" on CTP side vs. "breaking" on GCTP side):**

  | CTP-side change | GCTP-side breakage |
  |---|---|
  | Add 9 rules with `applies_to_prose: true` | Prose-projection floor in `audit-applicable-rules.sh` instantly demands all `.md`-scoped tickets carry these 9 → 205 retroactive reds on legacy req.json |
  | Add rules whose prose-judge tier-2 falls back to `not_enforced` | `audit-standards-enforced.sh` treats `not_enforced` as red (ADR-0066 D-C, no-silent-green) → previously honest greens now divergent |
  | Add new top-level plugin entries (`vendor/`, `COMMERCIAL-USE.md`) | `audit-plugin-surface.sh` reds until each is declared in `docs/plugin-surface-consumption.md` |
  | Re-route detector verdicts (composite engine) | Smoke fixtures (TICKET-042 stub) assert `pass` blindly; new detectors find things → divergence |
  | New CTP ADRs reference CTP-internal scripts | `audit-cross-references.sh` flags those refs as broken paths against the harness tree |

  Each row is "additive on the rule body" but "breaking on the consumer's enforcement-state derivations." CTP-ADR-0008's design contract has no field to surface this asymmetry at design time.

- **Proposed fix (CTP-side):** new **§"Consumer compatibility contract"** section in CTP-ADR-0008's design template — and required for any future composite-engine ADR or rule-schema-touching ADR — declaring four things up-front. Suggested YAML schema (concrete enough to template, loose enough to evolve):

  ```yaml
  consumer_compatibility:
    new_rule_classes:
      - field_name: applies_to_prose
        introduced_in: pin-230e99d           # epoch tag per CTP-side invariant below
        absent_default: false                # consumer dual-read shim unambiguous
        enforcement_impact: |
          Consumer prose-projection floors demand every .md-scoped ticket carry
          every rule with this flag. 9 rules introduced. Existing .md-scoped
          tickets produce N×T retroactive violations until /decompose is re-run
          (N = rules carrying the flag, T = pre-epoch tickets).
        grandfathering_required: yes
        grandfather_mechanism_proposed: |
          Consumer adds `applies_to_floor_version` marker on req.json; floor only
          enforces on marker >= 2. Prototype already shipped in GCTP CL-B (W-A).
    detector_behavior_changes:
      - script: composite-dispatch.sh
        change: "now routes by 4-axis applies_to instead of bare language string"
        smoke_fixture_impact: "TICKET-042 stub asserts pass blindly; live re-run on the kata tree finds hardcoded secrets — divergence is the gate working as designed"
        deprecation_window: none             # alternative: 1 pin / 2 pins
    plugin_tree_additions:
      - path: COMMERCIAL-USE.md
        consume_in_harness: no
        declare_in_registry: yes             # consumer must add a DECLARED-NOT-CONSUMED row
      - path: vendor/
        consume_in_harness: indirectly       # via composite-dispatch
        declare_in_registry: yes
  ```

  This makes the asymmetry visible at design time, before CTP writes any code, and gives the consumer a checklist to verify against before adopting.

- **CTP-side invariant (proposed): "schema-additive with epoch + default."** Every new feature is allowed to add to the schema (new rule fields, new rule classes, new detectors) provided:
  - Every new rule body carries an `introduced_in: <pin-or-version>` epoch tag.
  - Every new optional field declares its `absent_default` (so consumers reading old data get a defined answer).
  - No existing detector tightens behavior on inputs that previously passed without a `since: <version>` marker + deprecation window.
  - Top-level plugin tree additions are enumerated in the handoff (no surprise files).

- **Pairs with (GCTP-side, NOT an upstream item):** a forthcoming GCTP ADR codifying the **consumer-side dual invariant: "epoch-aware enforcement."** Every harness audit gate that derives requirements from `active.json` gates enforcement by the floor's `introduced_in` epoch vs. the ticket's issue-epoch; baselines are pin-keyed and re-baselined as part of the pin-bump CL; smoke fixtures opt into new contracts explicitly (W-A's `applies_to_floor_version: 2` marker generalized across all 14 gates). That ADR is harness-only and lands separately under the §15 ADR process.

- **Evidence:** ADR-0070 "Known follow-up surfaced by this CL" sections 1–3 enumerate exactly the three categories above with their precise counts. The W-A marker-gated floor (`applies_to_floor_version >= 2`) shipped in CL-B/TICKET-088 is empirical proof the grandfather mechanism in the YAML above is implementable and effective.

- **Why this matters going forward:** without this contract, every pin bump that touches enforcement-relevant schema reproduces the same pattern — three categories of red, the consumer-side agent is tempted toward the mass-rewrite shortcut, and the no-rewrites discipline (ADR-0070 §"No-rewrites discipline") has to fight that temptation case by case. The contract makes the cost visible at design time so CTP can choose between "ship with grandfather mechanism" vs. "ship as hard cutover with explicit pin-bump CL scope" with eyes open.

## P-10 — `composite-dispatch.sh` bash 3.2 empty-array crash (`ra[@]` / `toa[@]`) on any `--file` dispatch

- **Status:** ✅ ADOPTED — fixed in CTP **CL-538 (§28.70)** with the standard empty-array guard `${ra[@]+"${ra[@]}"}` `${toa[@]+"${toa[@]}"}`; adopted at GCTP pin `127804b` (ADR-0079 / TICKET-107). **Verified live on this machine's `/bin/bash` 3.2.57** (the definitive check CTP CI could not run): `composite-dispatch --file` now emits a real `status=` verdict (clean → green, violating → red) with no `unbound variable`. The routed-FOSS-tool paths (pre-write ADR-0075, on-save ADR-0076, audit-time ADR-0077) are now **active** on bash 3.2. Filed 2026-06-30 (GCTP CL-B TICKET-100), handed off to CTP (TICKET-106), fixed + re-pinned 2026-07-01.
- **Found:** 2026-06-30, GCTP CL-B design-phase-md wiring — invoking the cache's `rubric/composite-dispatch.sh --file <any-file>` at pin `4668c2e`.
- **Symptom:** every `--file` dispatch prints `composite-dispatch.sh: line 119: ra[@]: unbound variable` (to stderr) and exits 1 **without producing a verdict** — no `composite-dispatch … status=red|green` summary line.
- **Cause:** `rubric/composite-dispatch.sh` line 116 `ra=(); is_required "$t" && ra=(--required)` and line 117 `toa=()`, then line 119 `bash "$RUNNER" … "${ra[@]}" "${toa[@]}" …` — expanding an EMPTY array under `set -u` on macOS's default **bash 3.2**. The classic empty-array gotcha, identical in class to **P-1** (`conflicts[@]`). Fires whenever a routed tool is not `required` and carries no tool-options (the common case).
- **Impact on consumers:** any harness path that invokes `composite-dispatch --file` gets a bare exit 1 that is NOT a real verdict, so the routed FOSS-tool enforcement is effectively inert on bash 3.2. GCTP defends with **parse-then-block** (red only on an authoritative `composite-dispatch … status=red` summary line — ADR-0068 W-C, extended to the W-D design-phase gate in CL-B/TICKET-100), which prevents false reds but cannot recover the lost verdicts.
- **Proposed fix:** the standard guard — `"${ra[@]+"${ra[@]}"}"` and `"${toa[@]+"${toa[@]}"}"` (or test length before expansion). CTP ships a `tdd-pro-bash32-portability` skill documenting this exact gotcha (set -u + empty arrays); `composite-dispatch.sh` should conform to it.

## P-11 — Consult engines cite only an IaC/reliability grounding subset; the full `active.json` surface (all 43 namespaces / 118 rules) is not reasoned against at architecture-production time

- **Status:** ✅ ADOPTED — fixed upstream in **CTP CL-541..CL-545 (§29..§29.6)** and adopted in GCTP via **ADR-0086** at pin `0cf28fe` on 2026-07-03. CTP's `commands/full-surface-consult.sh` + `rubric/enforce-write-time.sh` deliver: (1) full aggregator-surface ingestion at consult time; (2) per-decision verdicts across every namespace (`needs_grounding=0`); (3) fail-closed on silent omission (`--require-complete` gate); (4) byte-identical native enforcement — the consult and write-time governor now share one primitive by construction. Both CTP return handoff docs (`handoff-ctp-to-gctp-p11-fixed.md` + `handoff-ctp-to-gctp-byte-identical-enforcement.md`) live in the CTP repo at `0cf28fe`.
- **Found:** first surfaced during GCTP kata attempts on `dev/kata-2026-07-02` — the operator observation (verbatim) was: *"Everything that CTP produces for GCTP must be enforced by one hundred and eighteen rules, not only the IAC rules."* Empirical diagnosis: `commands/architect-session.sh` / `architect-recommend.sh` / `well-architected-review.sh` grounded options only in 5 IaC/SRE sources (`aws-reliability-pillar`, `aws-rpo-rto-targets`, `nist-800-53`, `google-sre-book`, `opentelemetry-docs`) — `active.json` at pin `a69f380` carried 118 rules across 43 namespaces; none of `ts`, `node`, `react`, `owasp`, `oas`, `jwt`, `web-vitals`, `w3c`, `security-governance`, `arch`, `doc`, `k8s`, `helm`, `sbom`, `sarif`, `iam`, or 26 others were consulted at production time.
- **Impact on consumers (pre-fix).** The operator-declared standards regime (ADR-0037) required `active.json` govern *both* production and enforcement. At consult time only the IaC/reliability/SRE subset governed production; code-level, contract-level, and prose-level namespaces flowed in only at `/decompose` time. This produced *architectural rework loops* — a design option looked green at consult, then at code time a rule fired that would have changed the architecture had it been surfaced earlier.
- **Root cause (upstream, since fixed).** `commands/architect-session.sh` (and by transitivity `architect-recommend.sh` / `well-architected-review.sh`) had a hand-picked pillar/source list burned in at the engine layer — did not ingest `rubric/aggregator.sh`'s 118-rule surface as its grounding corpus. CTP's rule aggregator already built the full surface; the CTP-side architect engines simply did not consume it.
- **What the upstream fix delivered.** CTP extended its consult engines to (1) ingest the full aggregated standards surface; (2) emit per-decision verdicts (`applies` / `not_applicable` / `needs_deviation`) across all applicable namespaces; (3) fail-closed on silent omission; (4) emit design-time-vs-code-time hand-off notes per decision; (5) — beyond the P-11 ask itself — share one write-time primitive (`rubric/enforce-write-time.sh`) between consult and development-time enforcement so verdicts are byte-identical by construction (CL-543/§29.4 same-engine, CL-544/§29.5 parity, CL-545/§29.6 byte-identical). Additive amendment (§29..§29.6) per CTP's append-only discipline; 0 deletions to existing architecture content.
- **Coordination back to GCTP:** CTP notified GCTP of the re-pin target `0cf28fe` via `handoff-ctp-to-gctp-byte-identical-enforcement.md`; ADR-0086 landed the pin bump; contract drift = 1 file (upstream `architecture-v1.9.md` +59/-0, additive); `active.json` unchanged (118 → 118, byte-identical after regeneration); GCTP-side grep for `engine=enforce-file` = 0 hits (Step 2 of the CTP handoff a verified no-op).
- **Precedent:** P-10 → CL-538 → ADR-0079 (fix + re-pin cycle completed in 24 hours). P-11 followed the same pattern with additional §29.4–§29.6 hardening on top of the base fix.

## P-12 — `business-intake.sh` grounds only 4 of the 43 rule namespaces at the seed moment; full-surface expertise is silent at intake

- **Status:** ✅ ADOPTED — **four halves closed** at CTP pin `f39fcdc` (2026-07-06). **Half 1 (intake):** §30 / S-57 / §2.35 landed at `f060a8e` (CL-546) — full-surface probes at intake, adopted via ADR-0087. **Half 2 (design-engine consumption):** §30.1 landed at `c23e5fe` (CL-547) — S-33 `business-translate` reads `probes.<namespace>` and adds grounded concerns; S-34 `architect-recommend` lets decisive commitments modestly move the pick; both emit `probes_consumed=<n>`. Adopted via ADR-0088. **Half 3 (precise cloud classification + IaC probe coverage + transparency marker):** §30.2 landed at `43ea692` (CL-548) — closes the KATA-flagged silent-unprobed IaC-namespaces gap; new dedicated `aws-platform` / `azure-platform` / `gcp-platform` / `cloudformation` / `config-management` classifier types (an AWS-only kata is now probed for `aws`+`cfn` — precisely, not Azure/GCP); grounded probe groups added for `azure` / `gcp` / `cfn`; new `workload_classification.unprobed_in_scope` field reports remaining unprobed in-scope namespaces on `--classify` + run marker + persisted block — **no in-scope namespace can be silently unprobed again**. Adopted via ADR-0089 (GCTP-side: `--validate-profile` tolerates the new field as additive optional). **Half 4 (word-boundary matching precision):** §30.3 landed at `f39fcdc` (CL-549) — closes the KATA pre-flight misfires (`aks` matching inside `leaks`, `ci` matching inside `certification`/`accreditation`, `spa` matching inside `space`) by switching the classifier's signal-matching function from naive substring to a word-boundary regex `(?<![a-z0-9])<sig>s?(?![a-z0-9])`. Alphanumeric boundaries preserve phrase matches (`amazon web services`) and internal punctuation (`ci/cd`, `k8s`); optional trailing `s` preserves plural matches (`microservices`). Can only tighten the classifier, never loosen; no schema change (v1.1 shape identical — only classification behavior more precise); real AI-credentialing prose now yields `workload_types=[ai-governed, baseline-quality]` with no phantom cloud/orchestration/CI-CD types, while real cloud/orchestration/CI tokens still fire when actually present. Adopted via ADR-0090 (GCTP-side: consumer surface unchanged; `--validate-profile` / invariant 4 / `/consult` skill unchanged; pin bump adopts the plugin's own `.gitignore` submission-isolation guard by-pin). Four pin bumps in the same operator cycle, all under §15 discipline, each with its own ADR. Full technical handoff (proposal era) at [`docs/handoff-ctp-p12-full-surface-intake.md`](handoff-ctp-p12-full-surface-intake.md); CTP's shipped-shape handoff at `.harness/plugin-cache/claude-tdd-pro/docs/handoff-ctp-to-gctp-p12-fixed.md`. CTP shipped as **§30 / S-57 / §2.35 — Full-Surface Requirements Intake** (workload classifier + per-namespace probe groups + additive schema bump `1.0` → `1.1`) at pin `f060a8e` (CL-546). GCTP filed as "§27.16"; CTP relocated to §30 because §27.16 was already taken ("Layered multi-cloud advisor"). CTP shipped a NEW `commands/full-surface-intake.sh` composing S-32 (rather than modifying `business-intake.sh`) — more additive; v1.0 back-compat by construction. Adopted via ADR-0087 (§15-gated pin bump `0cf28fe → f060a8e`; `architecture-v1.9.md` +12/-0 additive §30; other contract files byte-identical; `active.json` 118→118 unchanged). Consumer-side reconciliation of the anticipated → shipped v1.1 shape (three key-name drifts: `signals_detected` → `workload_types` + `namespaces`; `activated_probe_groups` → `activated_probe_namespaces`; no `probes.universal` — universal-stays-universal in `answers`) landed in the same CL per the handoff-contract self-declared reconciliation policy.
- **Found:** 2026-07-04, during the O'Reilly "Certifiable, Inc." kata `/consult` run through GCTP at pin `0cf28fe` (post-P-11 adoption). Operator observation (verbatim): *"From the very beginning, I need the entire expertise of the entire plug-in to be interfacing with the user when asking the user about what they want to build. It creates the seed for the project from which it will divide and grow."*
- **Symptom (deterministic).** `bash commands/business-intake.sh --list-questions` returns **9 questions** cited to exactly **4 unique `source_id`s** (`azure-waf-business-requirements`, `aws-rpo-rto-targets`, `nist-800-53`, `aws-wa-tool-profiles`). Aggregated `active.json` at the same pin carries **118 rules across 43 namespaces**. So intake — the root of the decision tree — is informed by roughly **4 of ~43 expert namespaces**. Roughly 30 of the 43 namespaces (`security-governance`, `owasp`, `slsa`, `iam`, `observability`, `w3c`, `web-vitals`, `react`, `typescript`, `node`, `k8s`, `hashicorp`, `arch`, `testing`, `dependencies`, `documentation`, `sbom`, `us-government`, `eo`, `aws`, `azure`, `gcp`, `gitops`, `mesh`, `helm`, `bbp`, `gha`, `azdo`, `circleci`, `glci`, `jenkins`, `linux-foundation`, …) carry a first-order **business** question that today is never asked. Leaf-lint namespaces (`md`, `yaml`, `json`, `jsonschema`, `sarif`) have no first-order business question and legitimately stay out of intake — write-time enforcement suffices for those.
- **Impact on consumers (like P-11, one stage earlier).** Downstream engines (`business-translate.sh`, `architect-recommend.sh`, `architect-session.sh`) DO consume the full surface at write-time via §29..§29.6 (P-11), but they can only *check* what intake surfaced; they cannot invent a committed business posture (SLO level, WCAG target, SLSA level, AI-risk-tier, ASVS level, identity federation depth, human-oversight commitment) that was never asked for. So a design option looks green at recommend, then at code time a rule fires that would have changed the *architecture* had the committed business posture been surfaced at intake. Symmetric to P-11's rework loop — one stage earlier in the pipeline.
- **Root cause (upstream).** `commands/business-intake.sh` carries a hand-picked 9-question set grounded in one senior-architect probing catalog (`azure-waf-business-requirements` + `aws-rpo-rto-targets` + `nist-800-53` + `aws-wa-tool-profiles`). No workload classifier. No per-namespace probe groups. No mechanism to activate namespace-scoped questions based on the workload signal. The pattern *does* exist in miniature (`--with-data` conditionally activates 6 data-focused questions grounded in 4 additional sources) but is not generalized across the 43-namespace surface.
- **What the proposed fix delivers.** v1.14 §27.16 adds (1) a workload classifier stage (Stage 0) emitting `workload-classification.json` with detected signals (`web-ui`, `backend`, `cli`, `data-pipeline`, `ml-ai`, `regulated`, `multi-tenant`, `public-facing`, `container-first`, `serverless-first`, `iac-heavy`, `mobile`, `government`, `ai-high-risk`); (2) per-namespace probe groups (Stage 2) activated by classifier signal match, each question grounded in a `source_id` resolving in one of the five catalogs AND to at least one `active.json` namespace; (3) `business-profile.json` schema bump `1.0` → `1.1` — strict superset, backward-compatible (v1.0 profiles still validate + translate + recommend unchanged); (4) `grounded_in` monotonicity — v1.1 `grounded_in` ⊇ v1.0 `grounded_in` for the same workload. Universal 9 questions stay universal (never subtracted). Additive per ADR-0047 by construction.
- **Boundary contract (what crosses to GCTP).** Exactly three surfaces: `business-profile.json` schema (v1.1 additive); `--list-questions` JSON output (adds `probe_group` + `activation` per question); question `source_id` ↔ `active.json` rule namespace traceability. Prime-directive-preserving. Nothing else crosses.
- **Coordination back to GCTP (executed at pin `f060a8e`).** GCTP re-pinned via ADR-0087 (§15-gated); flipped `docs/handoff-contract.md §Business-Intake` PENDING → AUTHORITATIVE (reconciled to shipped keys); reconciled `scripts/consult.sh --validate-profile` v1.1 branch to check `workload_classification.{workload_types,namespaces,activated_probe_namespaces}` + gate the "every activated namespace answered" rule on `complete=true` (partial profiles legitimate); reconciled `scripts/audit-architecture-crosscheck.sh` invariant 4 to key on `activated_probe_namespaces`; updated the `/consult` skill (`.claude/commands/consult.md`) to walk the Stage 0 → Stage 1 → Stage 2 cascade against the shipped shape (`--classify` reveal → universal 9 forwarded to S-32 → `--probe-answer ns:key=value` per namespace). Landed as **TICKET-114** in `TICKETS.md`. Full suite green **42/42**.
- **Precedent:** P-10 → CL-538 → ADR-0079 (24h cycle). P-11 → CL-541..CL-545 (§29..§29.6) → ADR-0086 at pin `0cf28fe`. P-12 → CL-546 (§30 / S-57 / §2.35) → ADR-0087 at pin `f060a8e` (24h cycle again) → CL-547 (§30.1) → ADR-0088 at pin `c23e5fe` → CL-548 (§30.2) → ADR-0089 at pin `43ea692` → CL-549 (§30.3) → ADR-0090 at pin `f39fcdc` (all four landed by CTP across two operator cycles after successive GCTP readiness reviews and a real-prose KATA pre-flight). Same class, same discipline; symmetric fix one stage earlier in the pipeline (intake instead of design), with the coverage of that intake made precise (type-mapping, §30.2) and precise (signal matching, §30.3) and transparent (`unprobed_in_scope`, §30.2).

## P-13 — Stack-driven progressive rule activation (any technology committed at any stage should trigger rule + probe activation for its namespace; classifier haystack is vision-only, so cloud-agnostic visions never activate cloud probes even when operator explicitly names one)

- **Status:** ✅ ADOPTED — **three halves closed** at CTP pin `11126a8` (2026-07-08). **Half 1 (§30.4 Core Fix — classify from answers):** landed at `a2d05d4` (CL-550) — classifier haystack unions `vision + business-answer values` so an AWS-in-answer fires `aws-platform` and activates the `aws` probe group; §30.3 word-boundary matching preserved over the wider haystack; monotone. **Half 2 (§30.5 Structural Extension — `stack[]` mechanism):** landed at `a2d05d4` (CL-551) — new `--stack-add <ns>` CLI (repeatable) + `workload_classification.stack[]` field (additive optional); every append triggers namespace-keyed rule lookup in `active.json` + probe-group activation; cite-or-decline (unknown namespace → exit 2 with human-readable error). **Half 3 (§30.6 Entry shape + idempotency alignment):** landed at `6d68cc2` (CL-552) — closed the two gaps GCTP's pre-wired acceptance test asserted that CL-550/551 left underspecified. Each `stack[i]` is a provenance object with exactly four keys `{namespace, source, trigger, added_at}`, sorted-by-namespace; `source="stack-add"` for CLI provenance (`vision`/`answer` reserved for future haystack inference); dedupe-by-namespace at append (idempotent, first-write wins). Verified via GCTP's 19-assertion acceptance test at `11126a8`: **17 pass / 0 fail / 3 non-blocking skip** (Tier A §30.4 fully green; Tier B §30.5 fully green including T-B.2 entry-shape + T-B.3 idempotency). Adopted via **ADR-0091** (§15-gated pin bump `f39fcdc → 11126a8`; `architecture-v1.9.md` +30/-0 additive §30.4+§30.5+§30.6; other 4 contract files byte-identical; `active.json` 118→118 unchanged). Consumer-side reconciliation: `scripts/consult.sh --validate-profile` `stack[]` enum reconciled from speculative pre-wire `[classifier, universal-answer, probe-answer, design-decision, operator]` to shipped `[stack-add, vision, answer]` (same pattern as ADR-0087's v1.1 key-name reconciliation). Full technical handoff (proposal era) at [`docs/handoff-ctp-p13-cloud-classification-from-answers.md`](handoff-ctp-p13-cloud-classification-from-answers.md); machine acceptance surface at [`docs/handoff-ctp-p13-acceptance-test.sh`](handoff-ctp-p13-acceptance-test.sh); CTP's aligned return handoff at `.harness/plugin-cache/claude-tdd-pro/docs/handoff-ctp-to-gctp-p13-fixed.md`.

- **Status (historical):** 📋 FILED — 2026-07-07, at CTP pin `f39fcdc`. **WIDENED same day** from a narrow classifier-haystack fix to a general stack-driven mechanism after operator articulated the underlying model: *"there should be an array called `stack` with keywords for every technology being used; if anything gets added to it at any time, it should go hunting for the rules... look up by the kind keyword to find all rules that apply."* Target close-out: **§30.4 (Core Fix — haystack union)** + **§30.5 (Structural Extension — `stack[]` field + progressive namespace-keyed rule/probe activation from every decision stage)** as a v1.15 tag. This proposal takes CTP up on its standing offer in the resume-kata handoff §4 and generalizes it: the small signal addition is the Core Fix; the general mechanism it implies is the Structural Extension.
- **Found:** 2026-07-07, immediately after ADR-0090 (§30.3 word-boundary matching) landed. Operator observation (verbatim): *"Cloud identification and classification needs to work during consult."* The pre-flight at `f39fcdc` on the real Certifiable, Inc. kata vision (correctly cloud-agnostic — the AWS choice is an engineering decision, not a vision fact) fires no cloud platform type. This is correct given the current classifier input (vision-only), but leaves cloud probe groups (`aws`, `azure`, `gcp`, `cfn`) unactivated even when the operator explicitly names a cloud in a Stage-1 business answer.
- **Symptom (deterministic at pin `f39fcdc`).**
    ```bash
    CLAUDE_PLUGIN_ROOT=.harness/plugin-cache/claude-tdd-pro \
    bash .harness/plugin-cache/claude-tdd-pro/commands/full-surface-intake.sh \
      --workload "$(cat certifiable-vision.txt)" \
      --answer motivation="deploy on AWS Bedrock" \
      --classify
    # workload_types=ai-governed,baseline-quality  (aws-platform DID NOT fire despite operator's explicit AWS mention)
    # activated_probe_namespaces = documentation, european-union, observability, owasp, security-governance, us-government
    # (aws probe group NOT in activated set; aws-region-strategy / cfn-stack-policy questions never asked)
    ```
    Confirmed against the intake source at `.harness/plugin-cache/claude-tdd-pro/commands/full-surface-intake.sh @ f39fcdc`: the classifier haystack is constructed as `hay = wl.downcase` where `wl` is the vision text (or `answers.workload` as fallback), with **no** contribution from other universal answers.
- **Root cause (upstream, two layers).** (a) `commands/full-surface-intake.sh` builds the classifier haystack from `ENV["WORKLOAD"]` OR `u_answers["workload"]` (fallback), then lowercases it. All other universal answers, probe answers, and mid-consult design decisions are invisible to the classifier. (b) More fundamentally: rule + probe activation is a **one-shot** operation at Stage 0. There is no first-class record of "what technologies are actually in the stack at this moment" that downstream stages append to, and no mechanism for a mid-consult decision to trigger rule activation for the newly-committed technology. Every rule in `active.json` already carries `source_namespace: <kind>` — the four-axis namespace registry the operator described. The lookup is trivial (`rules.filter(r => r.source_namespace === ns)`); what's missing is the mechanism that WOULD look up.
- **What the proposed fix delivers.** Two composed changes, both additive per ADR-0047. **§30.4 Core Fix (haystack union):** one-line edit to make `hay = "#{wl} #{u_answers.values.join(' ')}".downcase` — an AWS mention in an answer fires `aws-platform` (unblocks the immediate bug; monotone; word-boundary matching from §30.3 preserved). **§30.5 Structural Extension (stack-driven progressive activation):** new `workload_classification.stack[]` field on the v1.1 profile shape (additive optional), each entry `{namespace, source, trigger, added_at}`; every technology-committing stage (Stage-0 classifier, Stage-1 universal answer, Stage-2 probe answer, `architect-recommend` design decision, explicit operator `--stack-add <ns>` injection) appends to `stack[]` idempotently; every append triggers namespace-keyed rule lookup in `active.json` + probe-group activation from `business-intake-question-bank.yaml` (moves the namespace out of `unprobed_in_scope` and into `activated_probe_namespaces`). Idempotent (same namespace can only be in stack once), monotone (stack only grows during a consult), precise (only namespaces resolving in `active.json` can be added — no phantom rule activation, cite-or-decline preserved).
- **Boundary contract (what crosses to GCTP).** `business-profile.json` v1.1 shape gains `workload_classification.stack[]` as additive optional. `--validate-profile` MUST NOT require `stack[]` on v1.1 profiles missing it (additive-optional discipline, already GCTP's default per ADR-0089 precedent). `decisions[].applicable_rules` propagation via invariant 4 generalizes from `activated_probe_namespaces` to `stack[].namespace` when present (one line of `audit-architecture-crosscheck.sh`). Prime-directive-preserving.
- **Full technical handoff:** [`docs/handoff-ctp-p13-cloud-classification-from-answers.md`](handoff-ctp-p13-cloud-classification-from-answers.md); machine-verifiable acceptance test at [`docs/handoff-ctp-p13-acceptance-test.sh`](handoff-ctp-p13-acceptance-test.sh) (Tier A: 12 assertions for §30.4 Core Fix — baseline, monotonicity, §30.3 word-boundary preservation over new haystack, multi-cloud disambiguation, undecided/on-prem/hybrid, vision+answer union, v1.0 back-compat, Extension shape. Tier B: additional assertions for §30.5 Structural Extension — `stack[]` presence, idempotence, entry shape, namespace-set updates on append, unprobed→activated migration, additive-optional v1.1 back-compat, cite-or-decline enforcement).
- **Coordination back to GCTP (post-adoption).** Filed as **TICKET-118** in `TICKETS.md`. Once CTP tags v1.15: §15-gated pin bump via new ADR; append a "Stack-driven progressive rule activation (§30.4 / §30.5)" bullet to `docs/handoff-contract.md §Business-Intake`; `scripts/consult.sh --validate-profile` tolerance for `stack[]`; `scripts/audit-architecture-crosscheck.sh` invariant 4 keys on `stack[].namespace` when present; `/consult` skill gains explicit "commit-to-stack" translation at each juncture (operator commits AWS → skill emits `--stack-add aws` and translates the new probe activation to plain language). Post-adoption GCTP work tracked as **TICKET-119**.
- **Precedent:** Symmetric to §30.2 (precise classification) and §30.3 (precise matching) — but one level up: §30.4 is precise *input surface* for the classifier, §30.5 is the *general mechanism* the whole intake→design pipeline needs to make rule activation dynamic. Same discipline: additive per ADR-0047, zero deletions, monotone, prime-directive-preserving. Extends the P-12 arc from four halves closed (§30 / §30.1 / §30.2 / §30.3) to six (add §30.4 + §30.5).
- **P-14** [Stage-0 full-surface reveal (non-committing)] (docs/handoff-ctp-p14-stage-0-full-surface-reveal-non-committing.md) — filed 2026-07-09 — FILED
- **P-18** [Acquisition-sufficiency threshold — every stacked technology in a project's effective rule set MUST carry ≥ 30 rules acquired from existing trusted sources; CTP owns capability (source-registry tech-specific canonical sources per registered technology + whole-source acquisition semantics + extraction-depth audit + sufficiency signal on the summary line) + GCTP owns enforcement (new `scripts/audit-acquisition-sufficiency.sh` refusing design cross-check when any stacked tech below threshold, per ADR-0037 TIER-1 operator-declared-standards regime)] (docs/handoff-ctp-p18-acquisition-sufficiency-threshold.md) — filed 2026-07-10 — FILED — empirical trigger: KA-2 (TICKET-126) `acquire-technology-live.sh --technology vue --project FEATURE-003 --cache <5-line-stub>` returned `acquired_total=3` — insufficient to design an architecture or develop a codebase on Vue; operator-declared floor: **≥ 30 rules per stacked technology**. Split honors prime-directive plugin-consumer roles (CTP owns rule content/extraction; GCTP owns operator-declared-standards enforcement); additive per ADR-0047 (source-registry entries added + extraction depth increased + GCTP audit is NEW — never relaxes an existing check); the two sides meet at a new `rule_count=<n> sufficiency=ok|below-threshold-<n>` signal on `acquire-technology-live.sh`'s stderr summary. Expected shape post-P-18: `vue` acquires ~60-195 rules across umbrella-general sources (typescript-handbook filtered by --only-mentioning ~5-15, google-tsguide ~3-10, node-* ~2-10) PLUS tech-specific canonical sources whole-source (vuejs.org/guide ~30-100, vuejs-api-ref ~20-60). GCTP parallel work per handoff §7: `audit-acquisition-sufficiency.sh` startable NOW at fail-loud pre-CTP-ship level (counts `_project/<id>/<ns>/*/rules[]` + `active.json.rules WHERE source_namespace == <ns>`; enforces threshold; wires into pre-consult / pre-roadmap / pre-decompose gates), landing as TICKET-127 + ADR-0094.
- **P-15** [Family umbrella activation + per-project tech-specific rule acquisition + PR-gated promotion — activate already-scraped umbrellas on family membership so Vue/Angular/Bun/Postgres get the framework-agnostic rules that already exist; acquire tech-specific rules on-demand from the same authoritative-source pipeline the plug-in uses for React, stored per-project at `_project/<project-id>/` with `origin: project` (fail-loud cross-project scoping — the "no silent globalization" spine); PR-only promotion into CTP `main` as the sole channel to global] (docs/handoff-ctp-p15-family-umbrella-per-project-provisioning-pr-promotion.md) — filed 2026-07-10 — ✅ **ADOPTED** at CTP pin `16e9623` (GCTP ADR-0093 supersedes ADR-0092, 2026-07-10) — P-15 §31/§31.1/§31.2/§31.3/§31.4 all seven S-tickets closed at ADR-0092 pin `b886658`; CTP then shipped CL-561 §31.8 same-day covering both post-adoption offers (production-fetch wrapper `commands/acquire-technology-live.sh` orchestrating resolve→select-umbrella-matched-sources→read-cache→acquire→filter-by-mentioning, plus `--explain` mode on the four commands emitting operator-friendly narrative alongside terse key=value markers); GCTP pin advanced to `16e9623` via ADR-0093 (additive-only architecture-v1.9.md +7/-0, active.json 118→118 byte-identical, all consumer-side tests unchanged and green — 43/43 suites). Boundary preserved: harness owns URL fetch into `--cache` dir; plugin does not download. — all seven S-tickets (S-58 family-registry + S-59 classifier widening + S-60 acquire pipeline + S-61 technology-fitness recommender + S-62 promotion PR flow + S-63 `_project/` foundation at nested path `generated-code-quality-standards/_project/<project-id>/<namespace>/*.yaml` + S-64 removal) closed at aligned SHA plus CL-560 consumer-contract reconciliation aligning runtime field names to GCTP's TICKET-121.a pre-wired validator; §15-gated pin bump `11126a8 → b886658`: `docs/architecture-v1.9.md` +112/-0 additive §31 amendments (§31/§31.1/§31.2/§31.3/§31.4), other 4 contract files byte-identical, `active.json` 118→118 byte-identical (no-silent-globalization spine A9 verified); consumer reconciliation near-zero — only `scripts/sync-plugin.sh` `_project/` preservation path corrected from top-level (TICKET-121.c speculative) to shipped nested `generated-code-quality-standards/_project/` per CTP answer #3; full test suite 42/42 green (`test-consult` 85/85, `test-audit-architecture-crosscheck` 33/33, `test-sync-plugin` 12/12 at corrected path); Delta C ruled `umbrellas:` on registry tech entry / `families_active[]` on runtime profile — GCTP pre-wired for the exact runtime name so schema tolerance lands unchanged. Filed 2026-07-10 — CONVERGED at four-round design consult: (round 1) GCTP filed the three-layer §30.8/§30.9/§30.10 proposal; (round 2) CTP shared its §31/§31.1/§31.2 five-move design + isolated five boundary questions; (round 3) GCTP answered B1–B6 + proposed four small deltas → CTP locked via `docs/handoff-ctp-to-gctp-p15-converged.md` + §31.4 architecture append @ CTP `main` `e4ec1a9` accepting Deltas A/B/D verbatim, holding Delta C (`umbrellas` vs `families` naming), publishing the S-63 shape (`.harness/plugin-cache/claude-tdd-pro/_project/<project-id>/<namespace>/<rule-id>.yaml` + `origin: project` + `project_id` + 4-axis + provenance; aggregator `--project <id>` scoping; byte-identical `active.json` without `--project`), and revising build order (family-activation FIRST — Phase 1 has zero `_project/` dependency); (round 4 — this repo) GCTP acked convergence, recommended keeping BOTH `families:` (on tech, membership) and `umbrellas:` (on family, namespace payload) as they name different rows, mapped 14+4 acceptance assertions (A1–A18) onto CTP's published §4 S-63 shape with machine-form jq/yq/exit-code expressions (A9 = byte-identical spine test; A15 = fail-loud cross-project leakage; A16 = `--project` required; A17 = reveal origin distinction; A18 = S-61 recommender source-cited shape), and confirmed the revised phase order (Phase 1 startable NOW via already-shipped §30.5 `--stack-add`; Phase 2 unblocked by published S-63 shape). Handoff triad in this repo: [`docs/handoff-ctp-p15-family-umbrella-per-project-provisioning-pr-promotion.md`](handoff-ctp-p15-family-umbrella-per-project-provisioning-pr-promotion.md) (round-1 three-layer proposal) → [`docs/handoff-ctp-p15-reconciliation-shared-design.md`](handoff-ctp-p15-reconciliation-shared-design.md) (round-3 six boundary decisions + four deltas) → [`docs/handoff-ctp-p15-b1-b5-decisions-and-assertion-map.md`](handoff-ctp-p15-b1-b5-decisions-and-assertion-map.md) (B1–B5 definitive answers + 14+3 assertion map onto §31 canonical shapes) → [`docs/handoff-ctp-p15-convergence-ack-and-final-shapes.md`](handoff-ctp-p15-convergence-ack-and-final-shapes.md) (round-4 convergence ack + Delta C recommendation + 14+4 assertion map onto §4 S-63 shape + phase mapping ack). CTP-side build order confirmed: **S-63** (`_project/` foundation + working/official write-plane split) → **S-58/S-59** (family registry + classifier widening — Phase 1) → **S-60** (acquisition pipeline — Phase 2) → **S-62** (promotion PR flow — Phase 3a) → **S-64** (removal — Phase 3b) → **S-61** (technology-fitness recommender — Phase 3c). GCTP-side companion tickets (to file on CTP shape publications): **TICKET-121.a** Phase 1 pre-wire (`standards-sync.sh` schema-validates `technology-family-registry.yaml`; `scripts/consult.sh --validate-profile` tolerates `families_active[]`; `/consult` skill translates umbrella activation to plain language; A1–A4 assertion scaffolding) — **startable NOW**; **TICKET-121.b** Phase 2 pre-wire (`standards-sync.sh` preserve-`_project/`-across-rebuild; aggregator `--project` propagation in `scripts/audit-architecture-crosscheck.sh` invariant-4; A15/A16 fail-loud tests; A5–A10 scaffolding) — **startable NOW** (unblocked by CTP §4); **TICKET-121.c** Phase 3 pre-wire (promotion-PR envelope validation; `docs/upstream-ctp-proposals.md` `P-15-followon:<tech>` status pattern; A11–A14 scaffolding; A18 recommender scaffolding); adoption tickets **TICKET-122** (pin bump for Phase 1 S-58/S-59), **TICKET-123** (pin bump for Phase 2 S-60/S-63), **TICKET-124** (pin bump for Phase 3a/b S-62/S-64), **TICKET-125** (pin bump for Phase 3c S-61) — each §15-gated per today's discipline. Prime-directive-preserving throughout: `_project/` is a DECLARED contract surface, CTP writes rule content via CTP-owned scripts invoked by GCTP with required `--project <id>` flag; GCTP writes only `.harness/consult-work/<id>/promoted-rules.log.md` provenance; the only cross-repo action is the sanctioned PR into CTP `main`. Additive per ADR-0047. Cite-or-decline preserved. No LLM. Compact-safe (agent originates no architecture; executes CTP's sanctioned commands + translates output).
